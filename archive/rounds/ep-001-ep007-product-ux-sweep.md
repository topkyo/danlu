# Product UX Rectification Sweep (R97~R108 EP-001..EP-007)

- 起止：2026-05-11（同日完成 R97 decision-grade report validator → R108 EP-007 PROGRESS 文档对齐）
- 范围：Decision-grade report validator 系列（R97~R98.3）+ Product UX EP-001..EP-007（md/txt 投放路由、ask note 格式、CLI legacy alias 清理、Product Shell UI 收敛、报告 subgraph、面板合并、文案白话化、PROGRESS 修订）
- 关系：本文件承载 PROGRESS.md 当前动态段中 R97~R108 共 11 条 entry 原文（byte-identical 搬移）

## 归档原文

- 2026-05-11 (R108 EP-007)：PROGRESS 修订与文档对齐（T9）— 纯 docs 校正，修正 R64-66 「ask 移除」字面漂移。**改动**：`PROGRESS.md:41` Quick Index Round 64-66 行尾「ask 移除」→「ask UI 锚点收敛」（粒度与表格其它条目一致）；`archive/rounds/round-64-66.md` 文件末尾追加 `## Addendum (R107 EP-007, 2026-05-11)` 块（13 行），列 5 条 ask 现状校正口径并交叉引用 EP-002b/c (R101/R102) / EP-003a (R103) / EP-003c (R104)：(1) `drop question` legacy CLI alias 已删（保留 dispatch.py `"question"` 短路触发 fail-loud SystemExit(2)）；(2) plugin `run-ask` palette entry 被 `showAdvancedCommands` gating，**非移除**；(3) Interaction Send / AskBox / Start Here Ask 三处冗余 UI anchor 已删；(4) **Ask modal 仍存在**，作 expert format/protocol/mode 入口，`runAskCommand` 签名不变；(5) ask CLI `--format` default `report`→`note`、plugin `defaultAskFormat:"note"`、modal format select 含 `note`。**R64/R65/R66 原始正文（`:1-36`）byte-identical 保留**，仅文件末尾追加 addendum；R66 副标题字面「ask 移除」按 contract 保留（历史保真），addendum 在同文件末尾校正，读者从 Quick Index 链入时正文与校正同屏可见。**Out-of-scope guard**：0 改动到 README.md / docs/ / src/ / .obsidian/plugins/ / schema / fixtures（exp-7 已确认 README / docs/Furnace Product Shell.md / docs/Furnace Next Direction Post-P4.md / src/README.md 仅引用当前 ask/run-ask 现状，无误删声明，不属本轮范围）。**1 轮 fresh oracle qa-review PASS**：ses_1e8b1c9acffesIWC3lsA2qX207，0 BLOCKER/MAJOR/MINOR + 1 NIT（archive 文件末尾多余空行，commit 前已修）；reviewer 逐条 grep 验证 5 条 addendum 事实与代码一致（`cli/parsers.py` 无 `drop question` subparser / `cli/dispatch.py:161` `"question"` 短路保留 / `plugin.js:67-75` `run-ask` 在 `showAdvancedCommands` 块内 / `render_primitives.js` / `render_input.js` 无 Interaction Send / AskBox / Start Here Ask anchor / `modals.js` `AskModal` + `runAskCommand` 仍在 / parser ask `default="note"` + plugin `defaultAskFormat:"note"` + modal format `[note,report,slides,figure]`）。verify PASS：**unit + 13 acceptance + 92% coverage + ruff**（纯 docs 改动无回归）；plugin jest 未受影响（无 plugin 改动）。**未动**：drop_* / receipt / audit / runtime-history schema / lock / Universal Input 协议 / `runAskCommand` 签名 / Ask modal / LLM backend / provider profile / key 管理 / plugin.js 注册（仍 4 `registerView` + 1 `addRibbonIcon` + 6 public addCommand）。Non-blocking：未来 ask 相关 EP 应在同一 addendum 块继续累积追加，保持向后兼容的校正轨迹；exp-7 仅扫描 `ask 移除` / `drop question` / `run-ask` / `Ask modal` / `showAdvancedCommands` 关键词，语义相近但措辞不同的历史 drift 可能未发现，按需补 addendum。EP-007 完成。**EP-001 ~ EP-007 全部闭环**，Product UX Rectification Sweep（T1-T9）收官。下一步等用户指示新方向。

- 2026-05-11 (R107 EP-006)：技术名词白话化（T8）— Product Shell 默认面板文案中文白话化，技术原文折叠在 advanced drawer 不动。**改动**：`src/render_today.js` 新增 `REVIEW_BUCKET_LABELS`（镜像 `today_feed.js REVIEW_BUCKET_COPY` 12 keys × [title,hint]）+ `reviewBucketLabel(key)` helper；`renderFurnaceActivityTimeline` review-backlog 分支不再渲染 raw bucket key（如 `l3_proposals`），改为中文 title（`处理 L3 提案`）+ hint summary；未知 bucket fail-soft 回退 raw string；内部 `target: "review:" + bucketKey` ID 保留不变。`src/constants.js` ZH_TEXT 表：删 5 处重复 key（`刷新炉子` / `运行` / `今天` / `新报告` / `系统动态`）；新增 jargon 映射 `stdout`→`标准输出` / `stderr`→`错误输出` / `error`→`错误`；更新带占位符的 `stdout: {value}`→`标准输出：{value}` / `stderr: {value}`→`错误输出：{value}` / `error: {value}`→`错误：{value}`；`Copy stderr`→`复制错误输出`（原 `复制诊断信息` 与新 stderr 术语冲突，统一）。`render_runs.js` 源码未改（所有标签已 `plugin.t()` 包装，ZH_TEXT 补全即生效）；`render_advanced.js` 故意保留英文 jargon（advanced drawer 是开发者视角，与用户视角隔离）。**i18n base english key 全部保留**，遵守 EP-004 R4 contract lock。**测试**：`__tests__/render/furnace-activity-timeline.test.js` l3_proposals 断言改为 `处理 L3 提案`；新增 unknown bucket fail-soft fallback case（8 → 9 cases）。**2 轮 fresh oracle qa-review**：R1 FAIL（1 MAJOR：`constants.js:806-807` ZH_TEXT 的 `stdout: {value}` / `stderr: {value}` value 仍是英文 token，且 `Copy stderr`→`复制诊断信息` 与新 stderr→错误输出 术语冲突）→ R2 **PASS**（ses_1e8c22af8ffee5nNhB6y96eLrT，0 findings，BLOCKER/MAJOR/MINOR/NIT 全空）。`bash build.sh` exit 0，main.js 重建。verify PASS：**unit + 13 acceptance + 92% coverage + ruff**；plugin jest **8 suites / 73 tests PASS**（+1 fallback case）。**未动**：drop_* 公共签名 / receipt / audit / runtime-history schema / lock 边界 / Universal Input 命令前缀协议 / `runAskCommand` 签名 / Ask modal 能力 / `today_feed.js` builder / `REVIEW_BUCKET_COPY` 表 / `render_advanced.js` / LLM backend / provider profile / key 管理 / plugin.js 注册（仍 4 `registerView` + 1 `addRibbonIcon` + 6 public addCommand）。Non-blocking：`REVIEW_BUCKET_LABELS` 与 `today_feed.js REVIEW_BUCKET_COPY` 双源镜像，单源整合留 follow-up；fail-soft 测试只断言 title；`selected`→`初选` 与 contract 描述的 `初始` 略偏（selected/final 上下文可接受）。EP-006 完成。下一步 EP-007 PROGRESS 修订与文档对齐。

- 2026-05-11 (R106 EP-005)：Product Shell 面板合并（T7）— Furnace Center 新增统一「炉子动态」时间线，合并 Recent Runs / Execution / Review 数据源，旧 ItemView 与命令保留兼容。**改动**：`src/render_today.js` 新增 `renderFurnaceActivityTimeline(plugin, parentEl)`，置于 `renderPendingSubmissionsGroup` 之后、`if (!summary)` 早返之前——即使 summary 缺失也能展示 `pluginState.recentRuns`。Renderer 合并 4 个源：(1) `buildTodayFeed(summary)` 全部 feed entries；(2) `plugin.pluginState.recentRuns` → `kind:"plugin-run"`；(3) **全部** `summary.recent_receipts`（不再被 today-date+elixir 关键词过滤）→ `kind:"receipt"`；(4) **全部**非零 `summary.review_backlog_counts` bucket（不再被 PRIMARY_REVIEW_BUCKETS 过滤）→ `kind:"review-backlog"`。`normalizeTs(ts)` 用 `Number.isFinite(Date.parse(ts))` 守 invalid/empty。Dedup：`seenTargets` Set 跨 `{elixir, decision, receipt, review-backlog}` 4 kind 按非空 `target` 去重（feed 先入先得，避免 curated feed 与 raw receipt 同 target 双显）；其它 kind 与空 target 不参与。按归一化 ts 倒序，cap 50。**i18n**：`src/constants.js` 新增 5 个英文 base + ZH_TEXT 映射：`Furnace activity` / `No recent furnace activity` / `Plugin run` / `Receipt` / `Review backlog`。**plugin.js**：旧 3 命令（`open-recent-runs` / `open-review-center` / `open-execution-center`）保留并加 `// EP-005: kept for backward compatibility` 注释；`addRibbonIcon` 仍**仅** 1 次（Furnace Center），`registerView` 仍 4 个。**测试**：新增 `__tests__/render/furnace-activity-timeline.test.js`（8 cases：空态/单 recentRuns/混合排序/cap=50/off-day receipt/raw review backlog/dedup 同 target/invalid timestamp 不崩）；新增 `__tests__/plugin/view-registration.test.js`（正则锁 plugin.js 源码 4 个 `registerView` + 1 个 `addRibbonIcon` + 6 个公开 `addCommand` id + EP-005 注释 marker）。**2 轮 fresh oracle qa-review**：R1 FAIL（ses_1e8f05a0affeuw01A78t96655G，1 MAJOR：timeline 仅消费 feed+recentRuns，未合并完整 Review/Execution 源；feed 已含 PRIMARY review bucket 与 today-elixir 子集，scope miss 在于"全量"）→ R2 **PASS**（ses_1e8db7b41ffeUysqCDpJmf1ZQO，0 hits）。`bash build.sh` exit 0，main.js 重建。verify PASS：**unit + 13 acceptance + 92% coverage + ruff**；plugin jest **8 suites / 72 tests PASS**。**SC#3** 既有命令未变、未加新 ribbon、4 个 ItemView 注册全保留。**未动**：`today_feed.js`（feed builder）/ receipt 与 recentRuns schema / ItemView 类签名 / `drop_*` 与 LLM backend / provider profile / key 管理 / 任何依赖。Non-blocking：invalid-timestamp 测试 fixture 排序位置巧合可能掩盖归一化退化（建议未来轮把 invalid 放在 valid 前）；`review-backlog` 渲染 raw bucket key（`l3_proposals` 等）留 EP-006 白话化；dedup 按字面 `target`，不同字符串表示同一记录不去重。EP-005 完成；下一步 EP-006。

- 2026-05-11 (R105 EP-004)：报告级 subgraph 浮上 UI（T6），后端 + plugin + advanced gating + 4 轮 fresh oracle qa-review 闭环。**后端**：`src/aiwiki/memory/graph.py` 新增 `ReportSubgraphError` + `build_report_subgraph(root, report_rel)` + `render_report_subgraph_markdown(subgraph)`。`build_report_subgraph` 内入口 `root_resolved=root.resolve()`，guard `absolute.relative_to(root_resolved)` 拒绝 vault 外路径（fail-loud `ReportSubgraphError`，含 symlink root 一致性），读 frontmatter `graph_anchor_node_ids` 列表，未编译态（`load_machine_memory` 非 dict / 缺 `compiled_at`）→ fail-loud；`build_machine_memory_graph(memory)` 包 `try/except Exception` → `ReportSubgraphError("machine memory is corrupt or incomplete (...); rerun \`aiwiki advanced compile\`") from exc`，阻断 corrupt-memory 经 generic exit 1 泄漏。未知 anchor id 同样 fail-loud。返回 `{report,anchor_node_ids,nodes,edges,neighbors}`（`report` 用 `absolute.relative_to(root_resolved).as_posix()`，symlink root 也一致）。**CLI**：`cli/parsers.py` 新增 `report-subgraph` subcommand（`--report` required / `--output` optional）；`cli/dispatch.py:355-395` 写 markdown 到 `output/reports/<stem>.subgraph.md`（或 `--output`），返回 JSON payload `{kind,report,anchor_node_ids,output_path,node_count,edge_count}`；`ReportSubgraphError` → stderr + `sys.exit(2)`。**Plugin**（`.obsidian/plugins/furnace-product-shell`）：`src/plugin.js` `registerAdvancedCommands()` 注册 `furnace-product-shell:report-subgraph`（advanced-gated）；`runReportSubgraphCommand({reportPath})` 调 `runPluginCommand` → 读 `payload.output_path` → `await this.openWorkspacePath(outputPath)`；`collectReportCandidates()` 从 `shellSummary.recent_outputs` 抽 `path.startsWith("output/reports/")` 候选去重；`openReportSubgraphPicker()` 候选非空时 `kind:"select"` 选 recent reports，候选为空回退 text input（带 fallback 文案）。`src/render/cards.js` `renderReportCard` 加 advanced-gated `View graph` 按钮。`src/plugin_helpers.js` `extractPrimaryPath` 候选含 `output_path`。`src/constants.js` 加 7 个 i18n keys（**英文 base + ZH_TEXT 中文映射**，修正 R2 hit 把 Chinese 当 base 的反向）。**测试**：`tests/test_report_subgraph.py` 10 cases（含 corrupt-memory / outside-root / symlink-root / uncompiled-memory empty+None）；`tests/test_cli.py` 5 cases（subcommand exists / parser args / dispatch success payload+output / dispatch fail-loud patched / dispatch uncompiled-memory 真实路径 exit 2）。`__tests__/render/cards.test.js` +2 cases；新建 `__tests__/render/report-subgraph-command.test.js` 6 cases（success → openWorkspacePath / no output_path skip / empty reportPath / plugin.js 源码 contract lock：`output_path` + `openWorkspacePath(outputPath)` + i18n English base / picker 候选 + cards.js i18n English base）。**4 轮 fresh oracle qa-review**：R1 FAIL（缺 openWorkspacePath wiring + uncompiled-memory guard）→ R2 FAIL（free-text picker / Chinese i18n base / corrupt-memory exit 1 leak / outside-root accepted）→ R3 FAIL（root.resolve() 不一致 / 误导文案）→ R4 **PASS**（ses_1e9068b09ffeQMP7tyHaGs8xJj，0 hits）。`bash build.sh` → `main.js` 8309 lines。verify PASS：**unit + 13 acceptance + 92% coverage + ruff**；plugin jest **6 suites / 63 tests PASS**。**SC#3** 主视图无全局 graph 入口（`render_home.js:18-28` 仅 onboarding/Universal Input/today feed/advanced drawer），graph 入口全部 report-scoped。**未动**：`drop_*` 公共签名 / receipt / audit / runtime-history schema / lock 边界 / Universal Input 命令前缀协议 / `runAskCommand` 签名 / Ask modal format/protocol/mode 能力 / LLM backend / provider profile / key 管理 / 任何依赖。Non-blocking defer：`collectReportCandidates` 不预检 `graph_anchor_node_ids`（无 anchor report 仍 fail-loud at runtime）；`--output` 绝对路径目前未做 root containment；`app_shell/helpers.py:126` fallback 仍 `"report"`；`modals.js:52`/`constants.js:198` 旧"生成报告"文案——留 EP-005/006 copy pass。EP-004 完成；下一步 EP-005。

- 2026-05-11 (R104 EP-003c)：Product Shell UI ask 入口收敛 + 死代码与 i18n 清理。删除 3 处冗余/无引用 UI：`src/render_primitives.js` `renderInteractionPanel` + helper `latestInteractionEntry`（dead, 0 callers）；`src/render_input.js` `renderAskBox`（dead, 0 callers）；`src/plugin.js` 对应 3 个 wrapper 暴露面。`src/render_primitives.js` `renderGettingStartedSection` 重写：去掉 Start Here 区的 Ask 按钮，引导文案改为「Universal Input 优先，Ask 弹窗用于 format/protocol 精细控制（mode 固定 run-ask）」。`src/constants.js` 删 11 个 0-ref i18n keys（含 `Interaction`、`Send`、`Ask anything...`、`Type a question...`、`Question cannot be empty.`、`Use Capture Note in Obsidian...`、`Use the Ask modal...` 等）。`src/README.md` line 9 同步描述：Universal Input 直接 submit、Ask modal 走命令面板。`main.js` 由 `bash build.sh` 重建（8227 lines，从 8432 缩 205 行）。测试同步：`tests/test_product_shell_universal_input.py` 删 `assertIn("renderAskBox", text)`（入口已退役，Universal Input 全覆盖 `render_input.js:65-98`）；`tests/test_vault.py` 删 `assertIn("drop note", plugin_source)`（保留 `Capture Note` 断言，note capture 链路仍完整：`modals.js:139-152` `runDropNoteCommand` → `plugin.js:2296-2302` `["drop","note","--text",text]`）。Oracle qa-review (fresh isolated session) APPROVE-WITH-NITS，8/8 检查 PASS，0 blocker；两个文案 nit（mode 固定 run-ask / README 不再"opens Ask modal"）已在 commit 前修复。**未动**：drop_* 公共签名 / receipt / audit / runtime-history schema / lock 边界 / Universal Input 命令前缀协议 / `runAskCommand` 签名 / Ask modal format/protocol/mode 能力 / LLM backend / provider profile / key 管理。verify PASS：**2077 unit + 13 acceptance + 92% coverage + ruff**；plugin jest 55 PASS。Non-blocking defer：`app_shell/helpers.py:126` recovery fallback 仍 `"report"`；`modals.js:52` / `constants.js:198` 旧"生成报告"文案，留后续 note UX 轮次。EP-003c 完成；EP-003b 已 SKIPPED。下一步 EP-004。
- 2026-05-11 (R103 EP-003a)：CLI 收口 — 删除 legacy `drop question` alias + plugin `run-ask` 命令面板降级为 advanced。`src/aiwiki/cli/parsers.py:1056-1058` 删 `question` subparser（3 行）。**关键决策**：`cli/dispatch.py:161` 的 `"question"` 短路**保留**——若移除会被 universal classifier 把 `drop question xxx` 当 NOTE/ASK payload 静默改写；保留短路后落到 argparse → `invalid choice: 'question'` → `SystemExit(2)`，符合 fail-loud 语义且无需新错误类型。`tests/test_cli.py`：`test_drop_question_dispatches_to_ask_handler` 改写为 `test_drop_question_subcommand_removed`（parser 层 SystemExit(2)）+ 新增 `test_drop_question_main_fails_loud_without_invoking_ask`（main 层锁短路，patch `ask_question` 确保未被调用）；choices lock `{...,"note","question"}` → `{...,"note"}`。Plugin `src/plugin.js:67-75` `run-ask` 命令注册包入 `if (this.settings.showAdvancedCommands)`（默认 false，常规用户命令面板看不到，与 `open-recent-runs` / `open-review-center` 同款 gating）；Ask modal 仍是 expert 入口能选 format/protocol/mode/触发 `run-ask`。`main.js` 由 `bash build.sh` 重建（8432 lines）。README/docs/archive grep `drop question` 无命中，无需同步。Oracle qa-review (fresh isolated session) APPROVE，0 critical/major：silent-misroute / bare-question / plugin gate / scope / docs / receipt-schema 6 项全 PASS。verify PASS：**2077 unit + 13 acceptance + 92% coverage + ruff**；plugin jest 55 PASS。EP-002 trilogy + EP-003a 完成。EP-003c（删 Start Here Ask + Interaction Send + AskBox 三处 UI ask 入口）留下一轮。
- 2026-05-11 (R102 EP-002c)：Obsidian plugin `defaultAskFormat` 默认值同步到 `note`，完成 EP-002 三段（a/b/c）闭环。改动 4 文件 +8/-6：`src/constants.js:13` `"report"` → `"note"`；`src/modals.js:64` Ask modal format select options 从 `["report","slides","figure"]` 扩到 `["note","report","slides","figure"]`，`:87` 提交 fallback `|| "report"` → `|| "note"`；`src/settings.js:96` Ask Defaults dropdown 加 `.addOption("note","note")` 于首位；`main.js` 由 `bash build.sh` 重新生成。`runAskCommand` (`plugin.js:2248`) 始终显式传 `--format`，settings 默认值会直达 CLI；Universal Input `drop <payload>` 不传 `--format`，问题型 payload 沿用 CLI default `note`（与 EP-002b 一致）。旧用户 `data.json` 已存的 `"report"` 受 `Object.assign({}, DEFAULT_SETTINGS, rawSettings)` 保留为显式选择，不做强迁移。`decision-memo`/`sop` 仍未在 plugin 暴露（最小 UI 策略），留后续 UX EP。verify PASS：**2076 unit + 13 acceptance + 92% coverage + ruff**；plugin jest 55 PASS。Oracle qa-review (fresh isolated session) APPROVE，8/8 硬约束 PASS，0 critical。Non-blocking：modals.js:52 / constants.js:198 旧文案"生成报告"可后续随 note UX 一起改；`app_shell/helpers.py:126` recovery fallback 仍 `"report"`，留后续清理。EP-002 三段全部 done。
- 2026-05-11 (R101 EP-002b)：ask / run-ask CLI 默认 `--format` 从 `report` 切到 `note`。纯配置级变更：`cli/parsers.py` 两个入口 (`_configure_ask_parser` line ~1125 + `run-ask` line ~306) `default="report"` → `default="note"`；EP-002a 的 renderer / validator / dispatch / SUFFIXES 全部复用，未动 runtime 行为。测试同步：`test_ask_note_format.py` 锁测试 `_default_is_report` → `_default_is_note`，新增两条显式 `--format report` regression（证明 R97-98.3 path 仍可达）；`test_cli.py` 三处 `ask_question` 调用断言（`test_bare_drop_question_routes_to_ask` / `test_bare_drop_ask_prefix_strips_prefix` / `test_main_dispatches_command_handlers::ask-load-learnings`）`"report"` → `"note"`。Oracle qa-review (fresh isolated session) APPROVE，8/8 硬约束 PASS，0 critical。Non-blocking 观察：`app_shell/helpers.py:126` recovery fallback 仍是 `"report"`（缺 receipt format 时的恢复命令）保留；plugin `defaultAskFormat="report"` 留 EP-002c。verify PASS：**2076 unit + 13 acceptance + 92% coverage + ruff**。 39 decision-grade tests 全过（直接调 render API，零影响）；13 acceptance 全过（显式 `--format report`，零影响）。
- 2026-05-11 (R100 EP-002a)：新增 `note` 作为 ask `--format` 第六个选项，给 md/txt 投放与广问场景一个 Q+A 体验，不改默认。`app_queries.render_note_answer()` 渲染 frontmatter (id/kind=output/format=note/query/protocol/generated_by/created_at) + `# {question}` + `## 回答` placeholder + 协议行 + 可选机器记忆提示 + `## 优先来源` / `## 优先概念`；不套用 report 六段骨架。`execution/ask.py` 接入 format 分发与 `OUTPUT_FORMAT_FILENAME_SUFFIXES["note"]="note"`，artifact 落 `output/reports/<seed>-note.md`。`runner/prompts.py:_validate_output_markdown` 把 `note` 加入 frontmatter+citation 检查白名单，但不跑 `_validate_report_sections`；`ASK_INDEX_PAGES_BY_FORMAT` / `ASK_PROTOCOL_PAGE_NAMES_BY_FORMAT` 各加 `"note":()`。`cli/parsers.py` `ask` + `run-ask` 两个入口 `--format` choices 加 `note`，default 仍 `report`。`prompts/ask.md` 增 note 行为段。`tests/test_ask_note_format.py` 新增 11 cases（render 1 + validator 4 + parser choices lock 5 + reject unknown 1）；`tests/test_cli.py` 已有 choices lock 同步。Oracle qa-review（fresh isolated session）APPROVE，6/6 硬约束 PASS。verify PASS：**2074 unit + 13 acceptance + 92% coverage + ruff**。EP-002b（切默认为 `note`）与 EP-002c（plugin 同步）留后续。
- 2026-05-11 (R99 EP-001)：md/txt 投放路由修复 + Universal Input fallthrough fail-loud 闭环。`input_router.classify_universal_input` 新增 `NOTE_TEXT_SUFFIXES=(.md,.markdown,.txt)` 分支（位于 `note:`/`ask:` prefix 之后、`multiline-text` 之前，`?` exclude），首页 Universal Input 投放 md 文件路径不再被误判为 ASK。`cli/dispatch._rewrite_universal_drop_argv` ASK 分支增加 `_looks_like_local_path()` fail-loud：检测 `./`/`../`/`/`/`~/`、POSIX 嵌套 `/`、Windows `\`、drive-letter（收窄到 `X:\` / `X:/`）、或实际文件存在，匹配则 `SystemExit(2)` 不再静默改写为 ask。Oracle qa-review 两轮：首轮发现 POSIX `/` 漏检 + `ask:` 被 suffix 抢先 + drive-letter false-positive（`Q: summarize` 被误杀），全数修复 + 回归测试锁定。`tests/test_input_router.py` +5 cases（md / MARKDOWN / .txt / `what.md?` 保持 ask / `ask: README.md` 保持 ask）；`tests/test_cli.py` +6 cases（md→drop note / txt→drop note / `notes/file.docx`→fail-loud / `notes\file.docx`→fail-loud / `Q: summarize` 保持 ask / plain text 保持 ask）。verify PASS：unit + 13 acceptance + 92% coverage + ruff。T1+T2 done；T3 (前端 `runDropFileCommand` md/txt 分支) 留 EP-003。
- 2026-05-11：Decision-grade report validator R97→R98.3 系列闭环。`_validate_report_sections()` 现 6 阶段（0 fence → 1 order/missing → 1.5 duplicate → 2 `_LLM:` → 3 bullets → 4 citations），覆盖骨架顺序 / placeholder 残留 / per-section bullet 下限 / `## 引用` 去重 + body⊆citations / 重复 required H2 / unclosed fenced code block。`tests/test_decision_grade_report.py` 39 cases。verify.sh PASS：**2051 unit + 13 acceptance + 92% coverage + ruff clean**。`investing-research` 推送 4 commits 至 origin（R97 `d84a575` / R98.1 `ae9e19f` / R98.2 `dba2962` / R98.3 `78c72d2`）。
