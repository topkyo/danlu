# Structural Consolidation Sweep (SC-001..SC-010)

- 起止：2026-05-12（同日 10 个 milestone 全部 done）
- 收官 commit：`ae6fd59`（PROGRESS SC-010 段），plan-side 锁定 commit `68cb835`
- 关系：本文件承载 PROGRESS.md 当前动态段中 SC-001..SC-010 共 9 条 entry 原文（byte-identical 搬移），完整 plan 全文见 `.codex/plans/archive/structural-consolidation-sweep.md`
- 后续接续：金丹复利 deterministic acceptance fixture（Gap P1）→ Investing dogfood

## 归档原文

- 2026-05-12 (SC-010 Structural Consolidation Sweep 完结)：清理 SC-008 fresh oracle qa-review 留的两条 non-blocking follow-up。**SC-010 改动**：(1) `src/aiwiki/drop.py` 把 `_LOGGER = logging.getLogger(__name__)` 从原 line 832（位于 `_rollback_created_paths` 之后的 lazy 定义）上移到 line 61 模块顶部 constants 区（紧邻 `_SUPPORTED_IMAGE_MIME_TYPES`），删除原位置 lazy 定义；logger name 保持 `aiwiki.drop` 不变，运行时无行为变化，仅消除 forward-reference 阅读阻力。(2) `tests/test_drop_phases.py` 新增 integration test `test_drop_pdf_finally_cleanup_logs_warning_on_rmtree_failure`：构造真实 `%PDF-1.4\n` source → `patch("aiwiki.drop._extract_pdf_text", return_value="text")` 让 collect/materialize 主路径成功跑完 → `patch("aiwiki.drop.shutil.rmtree", side_effect=selective_rmtree)` 用 prefix-selective wrapper 只对 `aiwiki-drop-pdf-` 前缀的 collect tmp dir 抛 `OSError`，保留其它 rmtree 路径（含 `tempfile.TemporaryDirectory` teardown）正常工作，避免污染测试环境 → `assertLogs("aiwiki.drop", level="WARNING")` 命中 `"drop tmp cleanup failed"`、断言 `result["note_path"]` 存在且 raw/inbox 落地（cleanup 失败不影响成功返回值，证明 best-effort 语义在 drop_pdf 整链路被 exercise）。phase tests 21 → 22。**SC-010 决策**：拒绝 `_warn_best_effort(...)` wrapper（stdlib `logging.Logger.warning` 默认已 try/except 内部 handler 异常，KISS / "不做过渡实现"）；拒绝 refactor collect helpers 把 cleanup 提到外层 wrapper（scope 扩散无 ROI，integration test 即可补 helper unit test 与主路径的 gap）。**SC-010 验证**：verify PASS（unit + 13 acceptance + 92% coverage + ruff clean）；fresh oracle qa-review **PASS** 0 blocker（ses_1e5fbd4cfffek862Ps5zD09BDA）。**Structural Consolidation Sweep plan（SC-001..SC-010）至此整体收官**：L3 strict state（SC-001）/ cli/dispatch 分治（SC-002）/ alchemy tx helper 下沉（SC-003）/ legacy elixir mutation 事务化（SC-004）/ drop_* 三相拆分 + snapshot rollback（SC-005）/ 死 helper 清理（SC-006/009）/ rollback OSError observability（SC-007）/ `_cleanup_tmp_dir` 通用化（SC-008）/ logger ordering + integration test（SC-010）。**未动**：drop_* 公共签名 / receipt / audit / runtime-history JSONL schema / lock 边界 / Universal Input 协议 / `aiwiki.app.*` re-export / CLI public surface / LLM backend / provider profile / key 管理。下一步：等用户决定 push / 新 plan / 其它方向。

- 2026-05-12 (SC-008 + SC-009 Structural Consolidation Sweep follow-up 收官)：清理 SC-007 fresh oracle qa-review 留的两个 non-blocking follow-up（rollback/cleanup OSError observability + `_download_asset_url` 死代码）。**SC-008 改动**：新增 `src/aiwiki/drop.py:_cleanup_tmp_dir(tmp_dir)` 通用 helper，docstring 写明 best-effort 契约（FileNotFoundError silent return，其它 OSError `_LOGGER.warning("drop tmp cleanup failed for %s: %s", tmp_dir, exc)` + no-reraise）；5 个 drop 主路径 collect tmp `shutil.rmtree(..., ignore_errors=True)` 站点（drop.py:300 PDF finally / 403 image finally / 432 image except / 598 repo cleanup_path finally / 1385 `_collect_binary_to_tmp` except）统一改调 `_cleanup_tmp_dir(tmp_dir)`；`_rollback_created_paths` 的 `contextlib.suppress(Exception)` 改为显式 `except OSError as exc: _LOGGER.warning("drop rollback unlink failed for %s: %s", path, exc)`（OSError 含 PermissionError/IsADirectoryError 全覆盖；non-OS 编程错误不再被吞）。**SC-008 out-of-scope**：drop.py:1058 browser `user_data_dir` rmtree（非 collect tmp 语义）/ drop.py:1562 git clone `destination` rmtree on TimeoutExpired（非 collect tmp 语义）/ drop.py:867 `contextlib.suppress(FileNotFoundError)` 已是 expected exception 类型不动。**SC-008 测试**：`tests/test_drop_phases.py` import 加 `_cleanup_tmp_dir` / `_rollback_created_paths`；扩 `test_drop_url_inline_image_failure_is_non_fatal_and_logged_after_materialize` 断言 `url_image_download_skipped` 四字段完整（`event` / `reason` / `url` / `error_type`，补回 SC-006 删除 `test_materialize_url_images_skips_failed_downloads` 时丢失的 url/error_type payload 覆盖）；新增 3 cases：`test_rollback_created_paths_logs_warning_on_unlink_failure`（`patch.object(Path, "unlink")` 注入 OSError，断言 warning + 文件留在盘上 best-effort）/ `test_cleanup_tmp_dir_logs_warning_on_rmtree_failure`（`patch("aiwiki.drop.shutil.rmtree")` 注入 OSError，断言 warning）/ `test_cleanup_tmp_dir_silent_when_already_absent`（`assertNoLogs` 确认 FileNotFoundError 静默分支）。phase test 18 → 21 cases。**SC-008 验证**：verify PASS（unit + 13 acceptance + 92% coverage + ruff clean）；fresh oracle qa-review **PASS** 0 blocking findings（ses_1e61a41c7ffekHBi7m5vlYN8OE，oracle 备注：`_LOGGER` 定义在 `_rollback_created_paths` 之后是 forward reference 惯用不优、`_cleanup_tmp_dir` 测试是直接 helper unit test 而非 collect 主路径 integration——两者皆 non-blocking）。**SC-009 改动**：删除 `src/aiwiki/drop.py:1324 _download_asset_url`（~22 行）——SC-006 已删其两个 production caller（`_materialize_url_images` / `_materialize_binary_source`），仅剩唯一一个 test caller `tests/test_drop_atomic.py:test_download_asset_file_url_uses_atomic_copy` 作 atomic-write probe；同步删除 `tests/test_drop_atomic.py` import 与该 redundant test（~15 行）。**SC-009 决策依据**：原 atomic-write probe 不变量（`atomic_copy_file` 写 `raw/assets/` + 无 `.tmp.` 残留）已被 `test_drop_image_local_copy_succeeds_and_no_tmp_residue`（drop_image 端到端 + `enable_vision=False`，exercise `_materialize_image` → `atomic_copy_file(tmp_path, asset_path)`）+ `test_drop_image_local_copy_replace_failure_leaves_no_partial`（os.replace failure）+ `test_drop_image_local_copy_uses_strict_atomic_tmp_pattern`（strict atomic tmp pattern）三方覆盖，redundant via 私有 helper；按 SC-006 主题"删 dead helper 而非加 docstring 警告"直接删。HTTP 分支原本只有 dead caller 链，删除合理。**SC-009 验证**：`rg "_download_asset_url" src/ tests/` clean；`_ASSET_MAX_BYTES` 仍被 line 57 (`_LOCAL_PDF_MAX_BYTES`) / 185 (URL inline asset collect) / 1343 (`_collect_binary_to_tmp` remote) 使用，not orphan；verify PASS（unit + 13 acceptance + 92% coverage + ruff clean）；fresh oracle qa-review **PASS** 0 findings（ses_1e60de109ffed2AXn7262CC7ex，逐条核对：grep zero refs / atomic invariant 等价覆盖 / `enable_vision=False` 不 short-circuit materialize / 无 orphan import or constant / 公开 schema 零变化）。**未动**：drop_* 公共签名 / payload schema / receipt / audit / runtime-history JSONL schema / 单写者不变量 / `aiwiki.app.*` re-export / CLI 表面 / 无新依赖 / 无 async/threading；rollback/cleanup best-effort 契约对外行为零变化（仅新增可观察面是 Python logging warning）。SC-008 oracle 给的 SC-010+ optional follow-up：`_LOGGER` 上移到 module constants 区 / 极小 `_warn_best_effort` 包装防自定义 logging handler 反向抛异常。Commits: `473b306` (SC-008 surface cleanup/rollback OSError via logger.warning) → `16ef945` (SC-009 remove dead _download_asset_url helper)。Plan **Structural Consolidation Sweep** SC-001..SC-009 全部收官。

- 2026-05-12 (SC-006 + SC-007 Structural Consolidation Sweep follow-up 收官)：清理 SC-005 R3 fresh oracle qa-review 提出的两个 non-blocking follow-up。**SC-006 改动**：删除 `src/aiwiki/drop.py` 中 SC-005 B-lite 拆分后已无 in-tree caller 的两个 legacy materialize helper `_materialize_url_images`（drop.py:1302-1319）+ `_materialize_binary_source`（drop.py:1387-1399），同步删除 `tests/test_drop.py` 对应 import 与 2 个 direct helper test（`test_materialize_url_images_skips_failed_downloads` / `test_materialize_binary_source_supports_remote_and_missing_local_file`）；`url_image_download_skipped` event 覆盖已由 SC-005 新增的 `tests/test_drop_phases.py` 提供；`_download_asset_url` 仍保留供 `tests/test_drop_atomic.py` 作 atomic-write probe 使用。**SC-006 验证**：grep `_materialize_url_images|_materialize_binary_source` clean；verify PASS（unit + 13 acceptance + 92% coverage + ruff clean）；fresh oracle qa-review **PASS** 0 findings（ses_1e646676cffeLnbz3pyciy2dKf）。**SC-007 改动**：`_snapshot_append_files` 与 `_truncate_append_files` rollback OSError 显式化——新增 `import logging` + 模块级 `_LOGGER = logging.getLogger(__name__)`；`_truncate_append_files` 的 `except OSError: pass` 改为 `_LOGGER.warning("drop rollback truncate failed for %s: %s", path, exc)`，仍保持 best-effort no-reraise（不遮蔽原始 materialize 异常）；`_snapshot_append_files` 的 `except OSError: sizes[path] = (False, 0)` 改为 `_LOGGER.warning("drop rollback snapshot stat failed for %s: %s", path, exc); continue` —— skip 语义比 absent 语义严格更安全，杜绝 existing-but-unstattable 文件在 rollback 时被误 unlink。**SC-007 测试**：`tests/test_drop_phases.py` 新增 2 个 `assertLogs("aiwiki.drop", level="WARNING")` cases：`test_truncate_append_files_logs_warning_on_oserror`（`patch.object(Path, "open")` 命中 `path.open("rb+")` seam，断言 warning 记录 + 原文件内容未变）/ `test_snapshot_append_files_skips_path_on_stat_oserror`（`patch.object(Path, "exists")` 命中 stat 失败分支，断言 log_path 被 snapshot 省略而 history_path 保留）。**SC-007 验证**：verify PASS（unit + 13 acceptance + 92% coverage + ruff clean）；fresh oracle qa-review **PASS** 0 findings（ses_1e6367f9fffeqo8MNtx4Ltx7Xx），并顺手修复 oracle 提到的 `drop.py:55` 内联注释贴行问题。**未动**：drop_* 公共签名 / payload schema / receipt / audit / runtime-history JSONL schema / 单写者不变量 / `aiwiki.app.*` re-export / CLI 表面 / 无新依赖 / 无 async/threading；rollback best-effort 契约不变（仅新增可观察面是 Python logging warning）。Non-blocking follow-up：`_rollback_created_paths` 仍 `contextlib.suppress(Exception)` 静默吞 unlink 失败；多处 `shutil.rmtree(ignore_errors=True)` tmp cleanup 同样静默；`_download_asset_url` 仅 test caller，可未来评估保留/删除；phase test 可补回 url_image_download_skipped 事件 url/error_type 完整 payload 断言。Commits: `abc8ec6` (SC-006 delete dead helpers) → `52164d4` (SC-007 surface rollback OSError via logger.warning)。

- 2026-05-12 (SC-005 Structural Consolidation Sweep 收官)：`drop_*` 长锁段拆分 collect/validate/materialize，慢 IO（fetch / browser render / MIME / OCR / vision / clone / snapshot）移出 `runtime_write_lock`，锁内只剩 raw 文件写 + `append_wiki_log` + `_append_raw_added_history`。**改动**：(1) `src/aiwiki/drop.py` 4 个 drop（url/pdf/image/repo）拆 `_collect_* / _validate_* / _materialize_*`；URL 内联图 fetch 失败 buffer 进 `skip_events`，锁内统一 `_append_run_event`（满足 `atomic_append_jsonl` caller-must-hold-lock 约束）；PDF/image 用 `tempfile.mkdtemp` + `atomic_copy_file` 二阶段拷贝，repo 远端 clone tmp 由 collect 内 try/finally 守护；image/repo collect 内部失败路径同样 `shutil.rmtree(tmp_dir, ignore_errors=True)` 再 raise，杜绝 `aiwiki-drop-image-*`/`aiwiki-repo-*` tmp 泄漏。(2) materialize rollback 升级：`_snapshot_append_files` 在锁内 mutation 前记录 `wiki/indexes/log.md` 与 `runtime_history_path(root)` 的 `(existed, size)` 二元组，`_truncate_append_files` 在 created_paths reverse-unlink 后按 snapshot 复原——原本不存在的文件被 unlink，原本存在的 truncate 回原 size，杜绝 stale log/history 行。(3) 删除 4 个无 in-tree caller 的 dead helper `_drop_url_unlocked` / `_drop_pdf_unlocked` / `_drop_image_unlocked` / `_drop_repo_unlocked`（`_drop_note_unlocked` 真实在用保留）。**测试**：`tests/test_drop_phases.py` 新增 16 cases 覆盖三相拆分边界、URL skip-events 锁内顺序、image collect MIME 失败 tmp 清理、repo clone 失败 tmp 清理、PDF history-append 失败 log truncate、history-append 失败 + log 原本不存在分支。**3 轮 fresh oracle qa-review**：R1 FAIL（ses_1e689b500ffeOFkouqGrSsh4s8，2 BLOCKER：skip_events 锁外 append / image-repo collect 内部异常泄漏 tmp + 1 MAJOR：log/history rollback 不复原）→ R2 FAIL（ses_1e680cadcffeHMJ7xG0O1kR1vc，2 MAJOR：snapshot 不区分 absent vs size=0 / `_drop_*_unlocked` dead helper 仍持 `atomic_append_jsonl` footgun）→ R3 **PASS**（ses_1e6795455ffez23LPdmVOWAfE7，0 BLOCKER/MAJOR/MINOR，逐条核对：snapshot existed 标记 + unlink absent 分支正确、4 个 dead helper 全删、`_drop_note_unlocked` 仍在 lock 内被 `drop_note` 调用、`append_wiki_log` 真实写路径 `wiki/indexes/log.md` 与 snapshot 候选一致、所有慢 IO 均在锁外）。verify PASS：**unit + 13 acceptance + 92% coverage + ruff clean**。closed-loop / finalize / run_plan 全部 PASS，**Structural Consolidation Sweep (SC-001..SC-005) 全部 done**。**未动**：`drop_*` 公共签名 + payload schema / receipt / audit / runtime-history JSONL schema / 单写者不变量 / `aiwiki.app.*` re-export / CLI 表面 / 无新依赖 / 无 async/threading。Non-blocking follow-up：`_truncate_append_files` 静默吞 OSError（best-effort rollback）；`drop.py` 仍有测试可直接调用的旧私有 helper（如 `_materialize_url_images`）当 public drop 路径不受影响时如直接调可能违反 lock-caller 约束，未来统一标注 "caller must hold lock"。Commits: `561edea` (B-lite split) → `931aeaf` (r1 fixes: skip-events lock + tmp leaks + log rollback) → `8dd4a9e` (r2 fixes: accurate log rollback + remove dead unlocked helpers)。

- 2026-05-12 (SC-004 v3 nested-def 收口)：v2 fresh oracle qa-review 残留 1 MAJOR — `execution/alchemy.py:431-439` `_rollback_migrated_candidates` 与 `:687-695` `_rollback_deleted_candidates` 仍是 nested `def`，与 v2 commit "Nested closures removed" 表述不符。**改动**：(1) `src/aiwiki/app_utils.py` 新增 `_restore_snapshots(snapshots: Mapping[Path, bytes | None])` 通用 helper（reverse-insertion 顺序复原 + best-effort 聚合 rollback 异常并 re-raise 第一个），紧邻现有 `_snapshot_file_bytes` / `_restore_file_bytes`，与 SC-003 helper 下沉意图一致；(2) `execution/alchemy.py` import 该 helper，删除两个 nested `def`，三处 rollback 站位（legacy mutation-phase except / legacy receipt-tx `rollback_data` / superseded mutation-phase except / superseded receipt-tx `rollback_data`）统一调 `_restore_snapshots(candidate_snapshots)`，receipt-tx 用 `lambda` 包装满足 callable 接口。**约束**：放宽 v2 自加的"no new helper" prompt 约束（contract 字面允许"复用 `app_utils` 事务 helper"，新增**一个**通用 helper 比保留两个 ad-hoc nested closure 更符合 SC-003 收敛意图）；alchemy public function 签名 / receipt schema / JSONL 写顺序 / patch seam 名称（`_restore_snapshots` 替代 `_restore_file_bytes` 作为 alchemy-module-binding rollback 注入点）均不变。**测试**：`tests/test_alchemy_legacy_cleanup_tx.py` 两处 rollback-failure 注入 patch target 从 `aiwiki.execution.alchemy._restore_file_bytes` 改为 `aiwiki.execution.alchemy._restore_snapshots`（caller binding 跟随实现迁移）；docstring 第 55 行同步。verify PASS：**unit + 13 acceptance + 92% coverage + ruff clean**；nested `def` grep 结果为 0。下一步：第三次 fresh oracle qa-review。

- 2026-05-12 (SC-004 Structural Consolidation Sweep)：legacy elixir cleanup 两条 P0 mutation 路径事务化。**Audit**：`.codex/audits/transactional-coverage-2026-05-11.md` 枚举全部 mutation 站点，按 P0/P1/P2/P3 分级（P2 段补 17 行 file:line 表覆盖 R96.0-R96.5 / R74-R78 / R82 / R95.4 现有保护）；本轮锁 `execution/alchemy.py:apply_legacy_elixir_migration` + `apply_superseded_elixir_cleanup`。**改动**：两个函数重写为 `plan → snapshot → mutate → receipt-tx` 形态，复用 `app_utils.atomic_write_text` / `_snapshot_file_bytes` / `_restore_file_bytes` / `_persist_receipt_transactionally`，**不新增** helper（mutation-phase rollback 与 receipt-phase rollback callable 内联在 try/except body 与 `_persist_receipt_transactionally` 的 `rollback_data` 参数）；新增 6 个 typed errors：`LegacyMigrationPlanError` / `LegacyMigrationApplyError` / `LegacyMigrationHalfWriteError` / `SupersededCleanupPlanError` / `SupersededCleanupApplyError` / `SupersededCleanupHalfWriteError`，按 plan vs apply 阶段语义分离；历史名 `*ReceiptError` 保留为 `*ApplyError` 别名（向后兼容）；apply 失败时 restore 全部 snapshot 并 re-raise，无半截 receipt。**测试**：`tests/test_alchemy_legacy_cleanup_tx.py` 8 failure-injection cases（legacy receipt-write/history-append/mutation/rollback-failure + superseded unlink/receipt-write/history-append/rollback-failure），patch seam 取 `aiwiki.execution.alchemy.*` 的 helper API binding（与 caller 实际调用对位），**不 patch `os.replace`**；unlink mock 用 `patch.object(Path, "unlink", autospec=True)` 正确注入第二个 unlink 失败（v1 误用 `patch.object` 不带 autospec 导致 mock 签名错误形成假阳性，v2 修复）。**2 轮 fresh oracle qa-review**：v1 FAIL（ses_1e77e787cffeGTuxR7bKgGo2mB，2 BLOCKER：error 类命名为 `*ReceiptError` 而非约束要求的 `*PlanError`/`*ApplyError`；unlink mock 签名错误是假阳性 / 1 MAJOR：引入两个 nested rollback closure helper `_rollback_candidates` / `_restore_candidates` 违反"no new helper" + audit doc P2 段未列 file:line）→ 修复：error 类重命名 + plan/apply 语义分离 + 别名向后兼容 / nested closure inline 到 try body / audit P2 补 17 行表 / unlink mock 加 autospec=True / v2 待 review。verify PASS：**unit + 13 acceptance + 92% coverage + ruff clean**；8 alchemy TX tests OK；134 alchemy 测试无回归。**未动**：alchemy public function 签名 / `_run_receipted_lane_primitive` / receipt JSON schema / JSONL 写顺序 / R96.0-R96.5 已落地事务边界 / drop_* 签名 / `aiwiki.app.*` re-export / CLI public surface。Non-blocking follow-up：protocol_learnings supersede（P0）/ ask.py main output（P0）/ runner/alchemy.py:1344-1599 SC-003b 残留（P1）/ nightly.py（P1）/ drop.py（P1）。下一步 SC-005（`drop.py:166-509` 采集→校验→materialize 三段式）。

- 2026-05-12 (SC-003 Structural Consolidation Sweep)：alchemy runner↔execution 边界收口 + 事务 helper 下沉。**改动**：(1) `src/aiwiki/app_utils.py` 新增 canonical `_snapshot_file_bytes` / `_restore_file_bytes`（byte-identical 迁移自 `execution/alchemy.py:89-110`）；(2) `src/aiwiki/execution/alchemy.py` 删原实现体，改为 `from aiwiki.app_utils import _restore_file_bytes, _snapshot_file_bytes` re-import，**全部 14 处内部调用站位不变**，`from aiwiki.execution.alchemy import _snapshot_file_bytes` 向后兼容（`is` identity 保持）；(3) `src/aiwiki/runner/alchemy.py:29` `from aiwiki.execution.alchemy import _restore_file_bytes, _snapshot_file_bytes` → `from aiwiki.app_utils import ...`，**全部 7 处 runner 调用站位不变**；(4) `runner/alchemy.py` + `execution/alchemy.py` 文件头加 boundary docstring：runner = orchestration only / execution = mutation only / tx helper 共享在 `app_utils`，runner docstring 列 SC-003b follow-up（runner 内残留 mutation 路径未下沉）。**测试**：新增 `tests/test_tx_helpers.py` 9 cases（None→non-existent / bytes→snapshot exact / empty file b"" / restore None+existing→remove / restore None+missing→noop / restore bytes→exact / restore overwrite→snapshot bytes / .restore.tmp 不残留 / canonical 与 backcompat re-import identity）。**Grep guard PASS**：`grep -rn "from .*execution.alchemy import _" src/` 返回 0 行（SC-003 success criteria #2 强制条件）。**1 轮 fresh oracle qa-review PASS**：ses_1e7a0a8e0ffe7dRiZoKgsgZYz3，**0 critical / 0 major / 0 minor**；逐条验证：`path.with_suffix(path.suffix + ".restore.tmp")` 保留、`path.unlink(missing_ok=True) + except FileNotFoundError: pass` 保留、`os.replace(tmp, path)` 原子 rename 保留、无 fsync 增删、re-import identity True True、public signatures 无变动、receipt JSON/JSONL schema 无变动、boundary docstring 与 README 模块分层一致。verify PASS：**2098 unit + 13 acceptance + 92% coverage + ruff clean**（+9 new tests）。**未动**：alchemy public function 签名（`run_alchemy_*` / `_run_receipted_lane_primitive` / lifecycle entry points）/ receipt JSON schema / JSONL 写顺序 / rollback reversed write-order 语义 / R96.0-R96.5 已落地事务边界 / drop_* 签名 / `aiwiki.app.*` re-export / CLI public surface / LLM backend。Non-blocking follow-up（runner docstring 已明记）：SC-003b 边界二次收紧 — runner/alchemy.py:524-650 / 832 / 985 区段仍有 apply/revert/snapshot 直接 mutation，未来若做"runner 纯 orchestration"目标，可下沉到 execution（本轮 Out Of Scope 明示）。下一步 SC-004（事务覆盖审计 + P0 gap 补盲）。

- 2026-05-12 (SC-002 Structural Consolidation Sweep)：`cli/dispatch.py` 分治 + universal-input router 独立模块。**改动**：(1) 新增 `src/aiwiki/cli/universal_input.py`（120 行）容纳 `_DROP_TYPED_SUBCOMMANDS` + `_looks_like_local_path` + `_rewrite_universal_drop_argv` + `_top_level_drop_index`（EP-001/EP-003a/EP-003c 三次 bug 的同一根因区域），独立模块化降低未来回归面；(2) `cli/dispatch.py` 1974→730 行（**-1144 行，-58%**），`main()` 由 678 行 if/elif 巨链改为 `_HANDLERS` dispatch table（16 个 `_handle_*` family handler）+ dispatch-and-finalize（≈30 行），早返 int handler 与 `(result, text_output)` tuple handler 两种 return convention；(3) 新增 `src/aiwiki/cli/dispatch_helpers.py`（1093 行）容纳全部 helper/command bodies（today/review_queue/trace/metrics/autonomy/resolver/batch-review/review-next/run-compile breadcrumb）；(4) `cli/dispatch.py` 显式 re-import block 保留全部历史模块级符号（46 names from dispatch_helpers + 4 from universal_input），`from aiwiki.cli import X` / `from aiwiki.cli.dispatch import X` 100% 向后兼容。**测试**：新增 `tests/test_cli_universal_input.py`（7 functions / 9 cases，覆盖 path heuristic / rewrite / fail-loud / top-level index / backcompat imports 三路径）。**2 轮 fresh oracle qa-review**：v1 FAIL（ses_1e7c26df9ffebHRU94w013r0k6，Critical 1：20 个 helper 符号被丢出 `dispatch` 模块表面，违反 backcompat hard requirement）→ 显式 re-import 修复 → v2 **PASS**（同 session，0 blocking findings；CLI public surface 100% 保留：subcommand 名/参数/退出码/stdout/stderr 字面、report-subgraph `sys.exit(2)`、file-back stderr breadcrumb、run-compile fail-fast、today/review-queue/trace/metrics/autonomy 早返 int、llm-check lazy import 全部一致；81 `_HANDLERS` keys vs 81 parser `handler_command` choices missing=[]）。verify PASS：**2098 unit + 13 acceptance + 92% coverage + ruff clean**（+7 new tests，total +13 since SC-001）。**未动**：CLI public surface / `drop_*` 公共签名 / receipt / audit / runtime-history JSONL schema / `aiwiki.app.*` re-export / argparse parser 定义 / LLM backend / provider profile / key 管理 / lock 边界 / Universal Input 命令前缀协议。Non-blocking follow-up（contract scope 外，oracle v2 major）：`dispatch_helpers.py` 1093 LOC grab-bag SRP 弱，建议未来拆 `dispatch_surfaces.py`（today/review-queue/metrics/trace/autonomy）+ `dispatch_review_helpers.py`（resolver + review-next）+ `dispatch_compile_helpers.py`（run-compile breadcrumb / model fallback）。下一步 SC-003（runner/alchemy.py + execution/alchemy.py 双源收口）。

- 2026-05-12 (SC-001 Structural Consolidation Sweep)：L3 proposal authoritative state 切 strict read，闭合 fail-closed 主线漏网。**改动**：`src/aiwiki/execution/l3_proposals.py:87-115` `load_l3_proposal_state()` 切 `load_json_document_strict`，corrupt JSON / non-dict top-level / non-list `proposals` 均 `CorruptStateError(path=..., reason=...)`；missing file 仍返回 `default_l3_proposal_state()`。import 清理：加 `CorruptStateError`，删未用 `load_json_document`。**关键修复**：`src/aiwiki/runner/auto_adopt.py:727-735` `auto_adopt_l3()` broad `except Exception` 前插 `except CorruptStateError: raise`，阻止 corrupt L3 state 被静默降级为 `{"degraded": True, "error": ...}`——这是 oracle fresh qa-review v1 发现的真实阻断 bug。其余调用点（`l3_proposals.py` 6 处内部 + `runner/alchemy.py:1167` + `trace.py:357`）无 broad catch，strict 语义自然传播。**测试**：新增 `tests/test_l3_proposal_state_strict.py`（6 cases：missing→default / corrupt JSON / non-object / non-list proposals / corrupt blocks create+list+apply+reject / corrupt propagates through auto_adopt_l3）。**2 轮 fresh oracle qa-review**：v1 FAIL（ses_1e7f3ee8effeplUzxth30I60nB，1 阻断：auto_adopt 吞 CorruptStateError）→ v2 **PASS**（ses_1e7ef8bd5ffeGXioGeW1FrPWpq，reviewer_mode: isolated，0 findings；9 个调用点全部确认继承 strict）。verify PASS：**2083 unit + 13 acceptance + 92% coverage + ruff clean**（+6 new tests）；closed_loop PASS。Non-blocking follow-up：`revert_l3_proposal()` valid-receipt + corrupt-state 路径未直接测试覆盖；`generate_l3_proposals_from_planner()` strict 传播未专项覆盖（无 broad catch，低风险）。**未动**：l3-proposals.json schema / receipt 行为 / runtime-history 写顺序 / R96.4 create_l3_proposal TX / CLI exit code 协议 / 公共签名。下一步 SC-002（`cli/dispatch.py` 分治 + universal-input router 独立）。
