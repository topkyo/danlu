# Archived scripts

This directory keeps historical operator tools that are no longer canonical
entry points for day-to-day development or verification.

Canonical verification remains:

```bash
bash scripts/verify.sh [target]
```

Archived scripts may still be useful as runbook evidence or migration history,
but new automation should depend on maintained scripts in `scripts/`.
