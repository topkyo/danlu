# 炼丹炉 Progress — Furnace 世代

> **结构 v2** (R68, 2026-05-04): PROGRESS.md 仅保留 Quick Index + 活跃 3 轮 + 改进方向指针。
> **PROGRESS.md 仍是当前任务状态唯一 SoT；archive/rounds/ 只是历史延伸。**
> 历史 round 详情：`archive/rounds/round-*.md` / `archive/rounds/p4-*.md`
> 机器索引：`archive/rounds/index.json`
> 切档历史：pre-Round 1 在 `archive/PROGRESS-pre-round1.md`（注意：里面也包含 Round 24/25 的早期记录，已重新落入 `archive/rounds/round-24.md` 和 `round-25.md`）

## SoT 引用

- 终局架构：`docs/Furnace Agent Architecture.md` + `docs/Furnace Evolution Mechanics.md`
- 当前方向：`docs/Furnace Next Direction Post-P4.md`
- Active contract：`.codex/contracts/active.md`
- 改进清单：见本文件底部「改进方向」段


## 当前动态

- 2026-05-14 (dogfood maturity Day2 自动 receipt)：`aiwiki-dogfood-maturity.timer` 在 `2026-05-14 08:15 CST` 自动触发成功，`aiwiki-dogfood-maturity.service` exit 0，写入真实 Day2 receipt `output/control/maturity-gate/run-20260514T001501Z.json`（`generated_at=2026-05-14T00:15:01Z`，`status=pass`）。timer 下一次计划为 `2026-05-15 08:15 CST`。当前 `summarize --days 3`：`status=warn`、`receipt_count=2`、`days=[2026-05-13, 2026-05-14]`、`consecutive_days=false`（未满 3 天）、`status_counts.pass=2`、`failed_runs=[]`、`prompt_hash_changed_runs=[]`、`backlog_total_delta=-18`、`judgment_review_new_receipts=5`、`judgment_review_exception_rate=0.0`、`judgment_review_failure_rate=0.0`、`l3_generated_total=0`、`l3_effective_candidate_count=21`、`operational_maturity.status=not-yet`、`budget_violations=[effective_l3_candidates]`。结论：自动化已证明 Day2 能无人值守产 receipt；仍不能宣称成熟，Day3 后需要看 `receipt_count=3` / `consecutive_days=true`，且当前新增的 effective L3 candidates=21 是 Day3 verdict 前的 blocker 观察点，不能通过调阈值绕过。

- 2026-05-13 (dogfood maturity 自动 timer 接入)：用户确认 Day2/Day3 应自动跑后，本轮在 M1 证据收口基础上补齐本机 user-level 自动化：新增 `scripts/run_dogfood_maturity.sh`（显式要求 `AIWIKI_DOGFOOD_VAULT`，默认等价 maturity runbook：preview/l3=1000、compile=0、no semantic lint；非 `AIWIKI_DOGFOOD_MATURITY_FORCE=1` 时按 UTC 自然日检查已有 `run-*.json`，同日 receipt 存在即 skip），新增 `systemd/aiwiki-dogfood-maturity.service.template` / `.timer.template`，并接入 `scripts/install_user_service.sh` / `scripts/uninstall_user_service.sh`。已执行 `bash scripts/install_user_service.sh` 安装当前用户 timer：`aiwiki-dogfood-maturity.timer` active/waiting，Next=`2026-05-14 08:15 CST`（`00:15 UTC`，避免本地 00:15 落入前一 UTC 自然日）；env 在 `~/.config/aiwiki/aiwiki-dogfood-maturity.env`，vault=`/home/tim/danlu/炼丹炉`，不含 secrets。手动 `systemctl --user start aiwiki-dogfood-maturity.service` 成功且只输出 skip：已有 Day1 `output/control/maturity-gate/run-20260513T030123Z.json`，未写同日新 receipt；`summarize --days 3` 仍为 WARN/NOT-YET：`receipt_count=1`、`days=[2026-05-13]`、`consecutive_days=false`、`operational_maturity.status=not-yet`。验证：`bash -n` 三个 service scripts PASS；focused `tests.test_app_misc tests.test_app_runtime tests.test_deploy_defaults` 116 tests PASS；`bash scripts/verify.sh` PASS（2180 unit + 17 acceptance + 92% cov）；oracle qa-review `APPROVE_WITH_NITS` 无 blocker（nits：uninstall 不删 env、默认 vault 是 machine-local、可继续加强 force/filename-day 测试；已折叠 machine-local 提示与 force/filename-day 断言）。后续无需手动跑 Day2/Day3；只需在 2026-05-14 08:15 CST 后检查 timer 日志与 `summarize --days 3`。

- 2026-05-13 (三天 unattended dogfood maturity 证据收口 M1)：按下阶段建议物化新 plan/contract：`.codex/plans/active.md` / `.codex/contracts/active.md` 现改为 `三天 unattended dogfood maturity 证据收口`，主线只做 proof-before-expansion，不扩 L3/Judgment/Product Shell 工程范围，不修改 maturity 阈值，不伪造自然日。M1 当前可完成项：固定 Day2 runbook 为 `set -a && source .envrc.dogfood && set +a; PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py run --root "$AIWIKI_DOGFOOD_VAULT" --preview-limit 1000 --l3-limit 1000 --compile-limit 0 --no-semantic-lint`；固定 Day3 verdict 为 `PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py summarize --root /home/tim/danlu/炼丹炉 --days 3`。本日只读 summarize 结果保持 NOT-YET/WARN：`status=warn`、`receipt_count=1`、`days=[2026-05-13]`、`consecutive_days=false`、`judgment_review_new_receipts=5`、`l3_generated_total=0`、`l3_skipped_total=520`、`l3_already_exists_total=260`、`l3_not_eligible_total=260`、`operational_maturity.status=not-yet`、budget violations 为空；证明当前不能宣称全自动成熟。验证：`tests.test_dogfood_maturity_gate` 14 tests PASS；`bash scripts/verify.sh` PASS（2178 unit + 17 acceptance + 92% cov）；oracle 复核先因 gate artifacts 沿用上一轮而 REQUEST_CHANGES，已刷新 M1 专属 `.codex/gates/qa-review.md` / `.codex/gates/qa-runtime.md`；`HARNESS_DIR=.codex bash scripts/closed_loop.sh --require-contract` PASS。M2/M3 阻塞于真实 wall-clock 自然日：到 Day2/Day3 才可执行写入 `run` receipt；同日重复 run 不计入成熟证明。

- 2026-05-13 (Product Shell UX checklist 自动化闭环)：按 `docs/Furnace Product Shell UX Test Checklist.md` 物化本轮 harness plan/contract（`.codex/plans/active.md` / `.codex/contracts/active.md`），将整份 checklist 收敛为可自动化 gate + 人工 UX residual。@explorer 审计确认现有覆盖主要缺 checklist §7 高收益项；本轮新增 Jest/jsdom 行为测试 `universal-input-interaction.test.js`：覆盖 `renderUniversalInput` 成功后 pending received/输入与附件清空/按钮恢复，失败后 pending failed/输入与附件保留，拖拽纯 URL 文本填 textarea 不误当文件，Today 无 summary/空 feed/有 pending 三分支，`shell-summary -> buildTodayFeed() -> DOM`，首屏只挂 Universal Input + Today + Advanced（不回退独立 DropZone/AskBox/旧 operator 噪声），长标题 DOM 文本非空，`llm_status.configured=false` 时 UI 显示 LLM 未配置但输入/CTA 仍可操作，以及 `recent_raw_inputs` 行为级 reconcile 可把 received pending 标 raw done。Python contract 扩展：`extractPrimaryPath` 覆盖 checklist 所列 path-like keys；`llm_status` configured/unconfigured JSON shape 稳定；unconfigured 环境不阻塞 deterministic `ask` / `drop note` dispatch；smoke script 的 `run-ask` backend resolution failure 走 deterministic ask fallback，write-path smoke 会显示 `note_path/stored_path`。验证：targeted Jest 8 tests PASS；full Product Shell Jest 90 tests PASS；targeted Python 57 tests PASS，后续补 reviewer blocker 后 `tests/test_llm_check_render.py tests/test_product_shell_smoke.py` 45 tests PASS；`bash scripts/verify.sh` PASS（2178 unit + 17 acceptance + 92% cov）；`bash scripts/product_shell_smoke.sh` PASS（当前环境 `opencode-api` 不可用，按脚本降级验证 deterministic ask fallback）；`bash scripts/product_shell_smoke.sh --with-note-write` PASS，drop note 返回 `raw/inbox/product-shell-smoke-*.md`。人工 UX residual：真实 Obsidian light/dark theme、窄窗口、侧栏折叠/展开和实际视觉密度仍需人工或未来视觉自动化确认，本轮不伪造 §6 全 PASS。

- 2026-05-13 (Product Shell 真实投料 + 提问闭环收口)：本轮把三段用户可见问题合并进 active contract 并闭环：外部普通本地 PDF/image 可通过 `drop_pdf/drop_image` 导入（复制到受控 tmp 后继续执行 PDF magic/size、image MIME/size/OCR/vision 校验；`file://` 仍拒绝 vault 外；`drop_repo` 仍不放开外部目录；uppercase `HTTPS://` 不误判本地路径）；Obsidian pathless browser `File` 由 Product Shell 写入 vault 内 `.aiwiki/tmp/product-shell-drop/`，pending retry 保存 resolved path，不再传裸文件名；Universal Input 在附件/URL/PDF/image/repo/note-like payload + 非空问题时先 drop 全部材料，再单次调用 `run-ask --format report --fallback-to-ask` 生成报告，纯投料不触发 LLM、纯问题保持原路径。qa-review 首轮抓出 3 个 blocker：contract scope 过窄、问题误传为 drop `--title`、`original_path` 可能进 LLM prompt；已修复：`.codex/contracts/active.md` 扩成完整范围，auto-ask drop 阶段不再传 title，`collectMaterialPathsFromPayload` 排除 `original_path` 并补行为测试。验证：Product Shell targeted Jest 11 tests PASS + full Jest 84 tests PASS + `node --check main.js` PASS；targeted Python 231 tests PASS；`bash scripts/verify.sh` PASS（2172 unit + 17 acceptance + 92% cov）；oracle rereview `APPROVE_WITH_NITS`（nits：`auto-ask` retry 死分支、tmp cleanup、ask 失败重试会重复 drop，均已作为 residual）；`.codex/gates/qa-review.md` / `.codex/gates/qa-runtime.md` 已写；`HARNESS_DIR=.codex bash scripts/closed_loop.sh --require-contract` PASS。

- 2026-05-13 (全自动成熟度收口 gate + dogfood 首轮)：新增 repo-local `scripts/dogfood_maturity_gate.py`（`collect/run/summarize`）和 `tests/test_dogfood_maturity_gate.py`（11 tests），把“连续 3 天 unattended nightly”物化为可复跑 maturity gate：`run` 写 `output/control/maturity-gate/run-*.json` receipt，采样 before/after `review_backlog_counts`、L3 proposal/generation、planner-log mode/decision、judgment-review receipt、`prompts/ask.md` sha256；`summarize --days 3` 按自然日去重取每天最新 receipt，校验连续 3 天、backlog 不升、L3 candidate 不升/去重、judgment review receipt 增量、prompt hash 不变和关键字段完整，避免同日 3 次误报成熟。真实 vault `/home/tim/danlu/炼丹炉` 首轮执行：`source .envrc.dogfood` + `dogfood_maturity_gate.py run --preview-limit 1000 --l3-limit 1000 --compile-limit 0 --no-semantic-lint` PASS，receipt `output/control/maturity-gate/run-20260513T030123Z.json`；judgment review receipt 15→20（+5，均有可复审 receipt），L3 `generated_count=0 / skipped_count=520 / already_exists=260 / not_eligible=260`，`prompts/ask.md` sha256 保持 `6f66cdf8...` 不变；`summarize --days 3` 当前 WARN（只有 1/3 自然日 receipt，非失败）。qa-review 经 oracle 两轮 REQUEST_CHANGES 修复后最终 `APPROVE_WITH_NITS`（唯一 nit 已补强断言）；`bash scripts/verify.sh` PASS（2159 unit + 17 acceptance + 92% cov）。后续两天复跑同命令，第三天执行 `PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py summarize --root /home/tim/danlu/炼丹炉 --days 3` 判定成熟度。

- 2026-05-12 (F-INV-19 accept→apply 真链路 + manual vs 自动对比收口)：F-INV-19 走完 5 步真链路。Step 1 `aiwiki apply auto-sig-20260512-8211e0ab1a7f` PASS（state=accepted, before=`6f66cdf8...`, after=`94c68cc6...`），CLI 表面校正：`review proposal --status` 词表只 `{rejected}`，accept 语义合并入 `aiwiki apply` 单事件（contract 文案在 N1 nit fold 时已改为 `l3-proposal-apply` 事件 + 说明 accept 合并）。Step 2 落盘验证：`prompts/ask.md` 2 → 22 行（原 stub + 20 行 auto-proposal block），sha256 与 receipt 一致。Step 3 runtime-history 3 条事件 create/apply/revert 全在。Step 4 revert smoke PASS：`restored_hash` 完美回到 `before_hash`，文件 22→2 行 stub。Step 5 receipt 段写入 vault `output/reports/dogfood-receipt-investing-v2.1.md`（147→273 行）。**主要发现**：auto generator 产 trace metadata block（planner decision + evidence ref）而非真 prompt diff，apply 后 ask.md 混入"planner decision 上下文"对 ask 行为无改进；F-INV-18a oracle 留的 N2 nit 与 F-INV-19 contract Residual Risks 段被实证。本轮 revert 已 deterministic 闭环，污染未持久；建议 v2.2 加 F-INV-20 改 `_automatic_l3_prompt_content` 模板。verify PASS（unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review（ses_1e31ddb30ffeRnDX2wDWA1MMPg）APPROVE_WITH_NITS 无 must-fix，3 nice-to-have：N1 contract 文案 fold；N2 revert receipt `after_hash` 字段语义偏绕（src/ 改动留 v2.2）；N3 立 F-INV-20 修 generator。closed_loop PASS。**v2.1 plan 四 milestone 全部收口**（NEW-5 `eba6fd1` → F-INV-18a `2f5c1c9` → F-INV-18 `347ea6e` → F-INV-19 本轮）。下一步等用户拍板：开 v2.2 plan（含 F-INV-20 + F-INV-18a N1 测试断言 nit + dogfood envrc backend 修） 或先 commit 收尾。

- 2026-05-13 (P0 harness 物化与 proof-before-expansion 收口)：已把 `.codex/plans/active.md` 物化为 P0 contract（`三天 maturity gate 收口`），复跑 focused tests 从 11→12（新增 prompt hash changed fail-path 直测）和 `bash scripts/verify.sh` PASS（2160 unit + 17 acceptance + 92% cov）。`summarize --root /home/tim/danlu/炼丹炉 --days 3` 仍为 WARN：`receipt_count=1`、`days=[2026-05-13]`、`consecutive_days=false`、judgment review +5、L3 `generated=0 / skipped=520 / already_exists=260 / not_eligible=260`，未误报三天成熟。qa-review oracle PASS，无 blocking；nits 已折叠：P0 `Out Of Scope` 正确物化、prompt hash fail-path 补测、后续新 receipt 同时写 `settings.skip_l3_generate` 与 `settings.apply_l3_generate` 以兼容首日字段名。P0 结论保持：当前只能证明 Day1 真实 receipt 与 gate 不误报，不能宣称全自动闭环已成熟；Day2/Day3 继续复跑 `set -a && source .envrc.dogfood && set +a; PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py run --root "$AIWIKI_DOGFOOD_VAULT" --preview-limit 1000 --l3-limit 1000 --compile-limit 0 --no-semantic-lint`，第三天再跑 `PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py summarize --root /home/tim/danlu/炼丹炉 --days 3`。

- 2026-05-13 (P1 L3 debt report 收敛)：P1 contract 已物化为 `L3 收敛与去重硬化`，在 `scripts/dogfood_maturity_gate.py` 增加只读 `l3_debt_report`，不写 `prompts/*.md` / `schema/policies/*`，不放开 L3 auto-apply。报告把 L3 候选拆成 preview eligible / not eligible / existing duplicate / low evidence / effective attention：真实 vault read-only collect receipt `output/control/maturity-gate/snapshot-20260513T044137Z.json` 显示 `preview_candidate_count=520`、`preview_eligible_count=260`、`preview_not_eligible_count=260`（`requires_execute_mode`）、`duplicate_existing_count=260`、`low_evidence_candidate_count=259`、`effective_preview_candidate_count=0`、`effective_attention_count=0`、`dedupe_or_noise_ratio=1.0`、`attention_noise_ratio=1.0`。focused tests 仍 PASS（12 tests，扩展 duplicate/low-evidence/not-eligible 断言）；`bash scripts/verify.sh` PASS（2160 unit + 17 acceptance + 92% cov）。P1 结论：L3 治理债主要是重复/低证据/不可执行噪音，不是可以安全自动 apply 的真有效候选；P2 只能推进 Judgment lane，L3 仍保持关闭。

- 2026-05-13 (P2 Judgment lane 受限放开指标)：P2 contract 已物化为 `Judgment lane 受限放开`，仍不自动生成新 judgment、不改写 thesis、不取消 limit。`src/aiwiki/runner/auto_adopt.py` 给 judgment review 增加 `limit`、`confidence_counts`、`conclusion_counts`、`exception_count/rate`、`exception_queue`，把 `weakened/refuted/low-confidence/error/unparsed/receipt_history_corrupt/failed` 明确上浮为异常；execution receipt 新增 `scan_generated_at/reviewer_model/conclusion/confidence` 元数据，保留 `review_id` 与 target path。`AIWIKI_NIGHTLY_AUTO_ADOPT_JUDGMENTS_LIMIT` 可显式调 daily limit，默认仍 5，最小 1。maturity gate 新增 `judgment_lane_report`，真实 vault read-only collect `output/control/maturity-gate/snapshot-20260513T045448Z.json`：`reviewed=5 / total_candidates=15 / failed=0 / exception_count=0 / failure_rate=0.0 / exception_rate=0.0 / receipt_count=20 / unique_review_ids=20 / unique_target_paths=6`，兼容旧 receipt（旧 20 条缺 conclusion/confidence，但最新 nightly item 补充 `upheld/high=5` 统计）。focused tests PASS（`tests.test_auto_adopt` + maturity gate 共 39 tests），覆盖 weakened/refuted/low-confidence/error/unparsed；后续继续 full verify + qa-review + closed_loop。

- 2026-05-13 (P3 operational maturity 指标收口)：P3 contract 已物化为 `人只看异常运行指标与异常预算`，`scripts/dogfood_maturity_gate.py summarize` 新增 `operational_maturity` summary：`human_only_exceptions`、anomaly budget、latest 指标和 3/7/14 trend windows。README 已改口径：人只看异常是成熟度目标，不是当前默认事实。修复 qa-review blocker：`summarize --days 3` 的主判定仍只看最近 3 个自然日 receipt，但 `trend_windows["7"]` / `["14"]` 现在独立按真实 7/14 自然日 receipt 计算，不再被 3 日主窗口截断；新增回归测试构造连续 8 天 receipt，断言主 `receipt_count=3`、trend 7=7、14=8。真实 vault summarize 当前仍 WARN/NOT-YET：`receipt_count=1`、`days=[2026-05-13]`、`consecutive_days=false`、`human_only_exceptions=false`、budget violations 为空；原因是缺 Day2/Day3 连续证明，不是异常预算失败。验证闭环：`tests.test_dogfood_maturity_gate` 14 tests PASS；`bash scripts/verify.sh` PASS（2165 unit + 17 acceptance + 92% cov）；oracle rereview `APPROVE_WITH_NITS`（nits：7/14 window 可补 days/consecutive_days、delta 断言可更强）；`HARNESS_DIR=.codex bash scripts/closed_loop.sh --require-contract` PASS。P0-P3 harness 均已收口，但不能宣称全自动成熟；后续自然日继续复跑 dogfood gate，第三天以 `summarize --days 3` 判定。

- 2026-05-13 (外部本地 PDF/image 导入根治)：修复 Product Shell 拖入/选择 vault 外 PDF 后后端报 `not within /home/tim/danlu/炼丹炉` 的低级路径问题。根因是前端传 Electron `file.path` 绝对路径，而 `drop_pdf/drop_image` 的 binary 收集层强制 `safe_resolve_within(..., root)`。本轮在 `src/aiwiki/drop.py::_collect_binary_to_tmp` 放开普通本地单文件 PDF/image：`Path(source).expanduser().resolve()` + `is_file()` 后复制到受控 tmp，再沿用既有 PDF size/%PDF magic、image MIME/size/OCR/vision 校验，`original_path` 记录 resolved path；`file://` URI 仍走 `safe_resolve_within` 并拒绝 vault 外文件，避免 URI 通道放开；`http/https` scheme 改为 `urlparse(...).scheme.lower()`，修掉 uppercase `HTTPS://` 被误判本地路径的问题。`drop_repo` 外部目录仍拒绝（递归读取风险不放开）；`drop_note` 允许 vault 外文本文件的既有行为新增显式测试记录。CLI help 同步说明 PDF/image 支持 vault 外本地路径、repo 为 vault-local path。排查到前端仍有 `runLauncherCommand` 手写 split 和 Product Shell 文案未同步两个 residual，未作为本轮 blocker。验证：targeted `tests.test_drop_safety tests.test_drop tests.test_cli_universal_input tests.test_cli` 183 tests PASS；`bash scripts/verify.sh` PASS（2170 unit + 17 acceptance + 92% cov）；oracle qa-review `APPROVE_WITH_NITS`（唯一 nit：Product Shell 文案同步）；closed_loop 待本条后执行。

- 2026-05-13 (Product Shell pathless File 附件根治)：用户复测后错误从 `not within <vault>` 变成 `Source not found: 20260513_...pdf`，证明上轮只修了“有绝对路径但在 vault 外”的后端分支，没修当前 Obsidian 环境 `File.path` 缺失导致 UI fallback 到裸 `file.name` 的分支。根因链路：`render_input.js` 只保存 `{name,path,type}`，提交 `file.path || file.name`；运行时加载的是 build 产物 `main.js`，只改 `src` 不生效。本轮新增 `resolvePluginFileSource(plugin, file)`：有有效 absolute/nested path 直接用；无 `file.path` 但有 `arrayBuffer()` 时，把内容写入 vault 内 `.aiwiki/tmp/product-shell-drop/<stamp>-<safeName>`，文件名清洗含 `/\\:*?"<>|` 与控制字符，再把真实路径交给 CLI。Universal Input paste/drop、dropzone PDF/Image、DropFileModal/DropImageModal picker 均接入该 resolver；pending retryArgs 保存 resolved path，不再保存裸浏览器 File name；`bash build.sh` 已同步 `main.js`。新增 Jest 行为测试真实模拟 pathless File：验证写入 vault tmp、内容正确、危险文件名不入路径；并锁定 absolute path 快路径不调用 `arrayBuffer()`。验证：Product Shell Jest 77 tests PASS + `node --check main.js` PASS；targeted Python `tests.test_product_shell_universal_input tests.test_drop_safety tests.test_drop tests.test_cli_universal_input tests.test_cli` 186 tests PASS；`bash scripts/verify.sh` PASS（2170 unit + 17 acceptance + 92% cov）；oracle qa-review code pass（nits：tmp cleanup 策略、retryArgs 字段名可更清晰）；closed_loop 待本条后执行。

- 2026-05-12 (F-INV-18 真投料主验收 PASS — L3 自动周期跑通)：F-INV-18 contract 物化后，三 step 真投料阻塞在「fresh trace 只有 medium signal，旧 trace 才有 high signal」陷阱（参见上一轮 explorer audit `ses_1e36f8bf3ffej48DaoY6pp4jDe`）。@explorer 复用 `ses_1e36f8bf3ffeEhYm8UFj2xiKQr` audit signals 子系统 → 5 个 router-eligible (kind, severity) pair 里只有 `elixir_dependency_break high` 是 deterministic + 入口直接（`src/aiwiki/execution/alchemy.py:1567-1582` demote → `_collect_dependent_elixir_ids` 833-867 → `signals/adapters.py:382-457` 固定 severity=high）。@explorer ses `ses_1e3677104ffecH26cfD9dNAdPx` 调查 vault 依赖图：只有 `b8dadbd3.derived_from` 指向 `97f9dc26`，无人指向 `b8dadbd3` → 只 demote 主丹能产 break。用户决策 B'：接受 v2 主丹 settled→candidate 不可逆 vault 写入，F-INV-18 闭环后 alchemy-promote 重提升回 settled。执行 `alchemy-demote --elixir-id elixir-ai-thesis-2024-97f9dc26 --note "F-INV-18 deterministic high-signal trigger; will promote back after milestone closes"` → execution-receipts.jsonl 写 `subject_kind=elixir_demotion / dependency_breaks=[{break_reason:source_demoted, dependent_elixir_id:elixir-vs-thesis-b8dadbd3}]`。`signals-replay --source archive` → 新 signal `sig-20260512-8211e0ab1a7f / kind=elixir_dependency_break / severity=high / trace_id=dfd72548-1513-48d4-a02a-fa683679bf00`（fresh trace ≠ 历史 `9a396669`）。`planner-log-replay --execute` → 1 record `decision=generate-proposal / mode=execute / side_effects_allowed=true / reason_codes=[elixir_dependency_break_observed, proposal_recommended, execute_mode_requested]`。`l3-proposal-generate --apply` → `output/_proposals/prompt/auto-sig-20260512-8211e0ab1a7f.md` 落盘，markdown 含 F-INV-18a 模板实质字段（`signal_id / trace_id=dfd72548-... / decided_at / dedupe_key / reason_codes` 三个 reason 全在 + Evidence references bullet）。**F-INV-18 主验收 6 条全 PASS**：(1) fresh trace 命中 generate-proposal ✓；(2) candidate `eligible=true blockers=[]` ✓；(3) proposal markdown 真落盘 ✓；(4) trace_id ≠ 历史 `9a396669` ✓；(5) markdown 含 F-INV-18a 模板字段 ✓；(6) 投料链路 alchemy-demote → execution-receipts.jsonl → signals-replay (archive) → planner-log-replay (execute) → l3-proposal-generate (apply) 完整 ✓。dogfood envrc `opencode-api` backend 解析失败（不在 `[codex-cli, copilot-cli, claude-cli]` 列表）使得 `run-nightly` 不可走，但 F-INV-18 真投料链路用 deterministic CLI 链替代，不依赖 LLM。下一步：写 v2.1 receipt 段 + 跑 verify + qa-review + finalize → 收 F-INV-18；后续 alchemy-promote 重提升主丹 settled；进 F-INV-19。

- 2026-05-12 (F-INV-18a 修 `_automatic_l3_prompt_content` 收口陷阱 2)：F-INV-18 audit 阶段经 @explorer 复用 ses_1e3c9ad51ffej48DaoY6pp4jDe 确认陷阱 2（b）命中——`src/aiwiki/execution/l3_proposals.py:410-421` 旧 `_automatic_l3_prompt_content()` 只在 `prompts/ask.md` 末尾追加 6 行 HTML 注释占位（`<!-- aiwiki:auto-proposal:start --> / signal_id: ... / Review ... / end -->`），完全不含 signal 摘要 / judgment 上下文 / evidence_refs / 操作建议；F-INV-18 success criteria 第 5 条「proposal 内容不是空洞」必然 FAIL。用户决策 A：拆 F-INV-18a 修函数（Recommended）。`.codex/plans/active.md` Milestone Index 插入 F-INV-18a，写完整 milestone 段（goal / context / success criteria 6 条 / scope / execution plan 8 步 / chosen approach / alternatives rejected / verification plan / fail gate / residual risks）+ 物化 `.codex/contracts/active.md`。@fixer 实施：`src/aiwiki/execution/l3_proposals.py` (+36/-4) 把 `_automatic_l3_prompt_content` 签名改为 `(current_content, *, candidate: dict[str, Any], planner_log_path: str)`，输出 deterministic markdown 模板（`## Auto-generated L3 proposal review` 标题 + `### Planner decision` 5 字段列表 `signal_id/trace_id/decided_at/dedupe_key/reason_codes` + `### Evidence references` bullet `<planner_log_path>#<signal_id>`），保留 `<!-- aiwiki:auto-proposal:start -->` / `end` 边界 byte 一致 + 尾部换行，缺失字段写 `(unknown)` / 空 reason_codes 写 `(none)`；同步唯一调用点 `generate_l3_proposals_from_planner` line 378-388 透传 `candidate` + `preview.get("planner_log_path")`，不动 `create_l3_proposal()` 任何参数。`tests/test_l3_proposals.py` (+50) 新增 `test_automatic_l3_prompt_content_includes_record_fields`（fixture record 全字段断言）+ `test_automatic_l3_prompt_content_handles_missing_fields`（空字段占位断言）。**不引入 LLM**，纯 string template，保证 deterministic + 可测。verify PASS（unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review（复用 ses_1e3d77e8fffe72lJpBGL7OQTEJ）单轮 PASS：6 success criteria 全 APPROVE / 4 红线全 PASS / 0 BLOCK / 2 NIT（N1：现有集成测试只断 boundary 未断 patch.content 字段，F-INV-18 顺手补；N2：模板成 proposed full replacement prompt 一部分，F-INV-19 真 apply 时需评估 prompt 污染）；closed_loop PASS。下一步进 F-INV-18：物化 contract → 真材料周期 → planner-log `mode=execute` 落盘 → fresh trace candidate → auto-adopt → proposal 落 `output/_proposals/`。

- 2026-05-12 (P4-INV-NEW-5 dogfood doc + CLI smoke 收口 F-INV-17 入口可见性)：v2.1 plan 起草 + 首 milestone 收尾。`.codex/plans/active.md` 三 milestone 串：NEW-5（execute-mode 入口可见性）→ F-INV-18（一次完整自动周期）→ F-INV-19（accept→apply + manual vs 自动对比），Global 嵌入 oracle ora-26 评估出的三个陷阱缓解（陷阱 1 = `AIWIKI_NIGHTLY_AUTO_ADOPT_L3` 不生成 candidate / 陷阱 2 = `_automatic_l3_prompt_content` 可能空洞 / 陷阱 3 = 878 候选均为单一历史 trace）。NEW-5 经 @explorer audit 重大发现：底层管线全通——`aiwiki planner-log-replay --execute` CLI flag 已存在（`src/aiwiki/cli/parsers.py:931-940` + `dispatch.py:582-595`）、planner-log `mode` 字段已存在（`src/aiwiki/planner/log_writer.py:125-138` 12 个 keys）、`requires_execute_mode` 解锁逻辑已正确（`src/aiwiki/execution/l3_proposals.py:310-315` `mode == "execute"` 即解锁）、既有 tests 已覆盖 `mode="execute"`（`tests/test_planner_log.py:426,1436`）；NEW-5 真实缺口在发现性 + dogfood 文档 + 端到端 CLI smoke。用户决策 A：纯 doc + smoke test，**不动 src/**。实施：`docs/Furnace Investing Dogfood Plan.md` 新增 §2.8 「执行模式（execute-mode）开启与 F-INV-18 复现」，含三入口职责区分（`planner-log-replay --execute` 前置条件 / `l3-proposal-generate --apply` 真正 candidate 生成入口 / `AIWIKI_NIGHTLY_AUTO_ADOPT_L3 + run-nightly` 已有 proposal auto-apply）+ 行为差异 + 风险边界（明确覆盖 `.aiwiki/state/` / `output/_proposals/` / `wiki/indexes/log.md` 经 `append_wiki_log` / `prompts/*.md` 或 `schema/policies/*` 含 wiki 资产层）+ F-INV-18 复现 5 步示例 + 成功判据（通过 `signal_id`/`evidence_refs` 反查 planner-log `trace_id` ≠ 历史 `9a396669-...`，因 proposal 本身不存 `source_trace_id`）。`tests/test_planner_log.py` 在 `TestCLI` 加 `test_cli_execute_flag_writes_execute_mode_records_to_disk` 真 e2e smoke：调 `main(["--root", root, "planner-log-replay", "--execute"])` + case_basic fixture，断言 `.aiwiki/state/planner-log.jsonl` 真落≥1 条 `mode=execute` record（无 mock）。**不动 src/**；verify PASS（unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review r1 FAIL（ses_1e3d77e8fffe72lJpBGL7OQTEJ）抓出 4 个 [BLOCK]：(1) §2.8 流程错误把 `run-nightly` 当 generator / (2) candidate generation vs auto-adopt 概念混淆 / (3) 风险边界遗漏 `wiki/indexes/log.md` 与 `prompts/*.md` 写入 / (4) `source_trace_id` 字段不存在；全 4 个 fix 后 r2 APPROVE。closed_loop PASS。**v2.1 首 milestone NEW-5 done**。下一步 F-INV-18：用 NEW-5 doc 入口在真 vault 跑 audit `_automatic_l3_prompt_content()` 真实输出 → 决策门 → 跑真材料完整自动周期。
- 2026-05-12 (Investing Dogfood v2 §2.7 收官，v2 升 6/7→7/7)：补做 v2 §2.7 L3 prompt proposal 闭环，走 **manual fixture path**。`aiwiki l3-proposal-create --kind prompt_proposal --proposal-id prop-inv-dogfood-v2-ask-confidence --target-file prompts/ask.md --pattern manual_fixture` 创建 candidate（提议 investing protocol `confidence_level` 按来源密度 + `counter_evidence_provenance` 自适应，根据 v2 实跑 752 页/16.5MB 真年报仍标 low 的 friction）；`aiwiki review proposals` 队列显示 v2 candidate；`aiwiki review proposal ... --status rejected --note "..."` reject 闭环。落盘四层完整：`output/_proposals/prompt/prop-inv-dogfood-v2-ask-confidence.md`（2007B）+ `.aiwiki/state/l3-proposals.json` state=rejected + `.aiwiki/state/runtime-history.jsonl` 双事件 `l3-proposal-create` / `l3-proposal-reject` + audit-mirror。**F-INV-17 实证**：v2 投料后 `l3-proposal-generate --dry-run` 扫到 878 planner records / 20 candidates returned，全部 `eligible: false` blocker=`requires_execute_mode`；`--apply --limit 5` `generated_count: 0`；所有 candidates 都引用历史 trace `9a396669-ab7d-4123-ad2c-0e5350fb05d0`（v0/v1 时代），v2 投料 0 触发新 candidate。根因 = `mode=observe_only` 默认，planner-log 中 `mode=execute` 记录 = 0。**v2.1 转 P4-INV-NEW-5**：investing protocol nightly auto-adopt L3 默认 opt-in / dogfood 文档说明 `mode=execute` 切换路径 / `shell-status` 或 `review proposal-generation` 暴露当前 mode。F-INV-18/19 N/A（因 F-INV-17 阻塞，自动路径未走，accept→apply 质量对比留 v2.1）。`output/reports/dogfood-receipt-investing-v2.md` 追加「§2.7 收官（2026-05-12）」段 + 7 步 flow 改 7/7 + L3 proposal 数 1。**v2 dogfood 完整 7/7 收官**，后续转 v2.1 + 多轮 dogfood。下一步等用户拍板：开 v2.1 plan（含 F-INV-NEW-5 + NEW-4 nits residuals + 真 accept→apply 质量对比）或先启动多轮 dogfood。
- 2026-05-12 (Investing Dogfood v2 收官 receipt 段追加)：`/home/tim/danlu/炼丹炉/output/reports/dogfood-receipt-investing-v2.md` 追加「v2 收官（2026-05-12）」段：5/5 摩擦点 + NEW-4 nits 闭环表（commits `10d7b74` / `cf0b512` / `275a522` / `96e8b76` / `05a1c58` / `fe9d68f`）+ gate 状态（每 milestone verify PASS + qa-review fresh-session 4 APPROVE / 1 APPROVE_WITH_NITS nits 已清 + trace 测试 23→25）+ 范围保持（未动 src/ 对外接口 / CLI / 既有 schema 字段 / 无新 protocol/backend/lane/UI/依赖）+ 结论（F-INV-1~16 复检状态保持 / §2.7 L3 proposal 转 v2.1 / 后续启动多轮 dogfood）。**v2 复审通过**：5 NEW + 11-revisit 全 done，历史 F-INV-* 状态保持。下一步等用户拍板 v2.1 L3 proposal plan 与多轮 dogfood 测试的执行顺序。
- 2026-05-12 (P4-INV-11-revisit counter_evidence_provenance 收口 F-INV-11-revisit)：promote receipt + settled elixir frontmatter 加 `counter_evidence_provenance: "real" | "none_found"` 字段。`src/aiwiki/execution/alchemy.py` `promote_elixir` 在 `_validate_promote_gate` 后基于已校验 frontmatter 静态判定：`counter_evidence_items == ["NONE_FOUND"]` → `"none_found"`，其他 → `"real"`；写入 settled frontmatter（与 `promoted_at` 同一 update）+ 传给 `build_elixir_promotion_receipt`。Tombstone（superseded candidate plane）刻意不带新字段——避免 promote-time 语义污染 candidate plane；receipt 已持久记录 provenance 无信息丢失。`src/aiwiki/app_execution.py` `build_elixir_promotion_receipt` 加必填参 `counter_evidence_provenance: str`，写入 `bundle.counter_evidence_provenance`（**追加字段**，向后兼容）。`tests/test_alchemy.py` 扩 `test_promote_receipt_records_counter_evidence_gate_fields`（real 路径双断言 receipt + settled frontmatter）+ 新增 `test_promote_marks_none_found_provenance`（none_found 路径 + 显式断言 candidate/tombstone `assertNotIn`）。D-3 acceptance golden `tests/fixtures/acceptance/D3/case_elixir_stage3_compounding/expected/files/.aiwiki__state__execution-receipts.jsonl.golden` refresh：bundle 追加 `counter_evidence_provenance: "none_found"` + primary_path_sha256 漂移（settled frontmatter 多字段，预期），secondary_path_sha256 不变正好证明 tombstone 未污染。verify PASS（2137 unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review APPROVE_WITH_NITS（ses_1e42429f1ffejENGS2kiX9COpa，两 nit 非阻塞：N1 receipt builder 未校验 provenance 取值集合 / N2 未来 demote 路径应 `pop("counter_evidence_provenance", None)`）。closed_loop PASS。**dogfood v2 五个摩擦点 P4-INV-NEW-1/2/3/4 + P4-INV-11-revisit 全部 done，v2 收官**。
- 2026-05-12 (P4-INV-NEW-4 trace 识别 derived 资产 kind 收口 F-INV-NEW-4)：`src/aiwiki/trace.py` `_classify` 在 concept 之后、judgment 之前加 `wiki/derived/` / `./wiki/derived/` / `derived-` 前缀 → `"derived"`；`_resolve_any` 路由加 `derived` 分支；新增 `_resolve_derived(root, asset_id, *, direction, depth, visited)`：用 `_find_curated(root, "wiki/derived", asset_id)` 找文件，parents (up/both) 走 `citations`（含 `#sha256` anchor fragment 剥离后再 `_resolve_any`），children (down/both) 扫 `wiki/elixirs/` + `output/_candidates/elixirs/` 的 `derived_from`，命中后作为 elixir child；metadata 仅当 frontmatter `protocol` 非空时挂载（与既有 sparse `to_dict` 行为一致）。新增 helper `_derived_ref_matches(ref_value, derived_rel, derived_id)`：剥 quote/anchor 后 match `derived_rel` / 文件名后缀 / id 子串。`tests/test_trace.py`：`_seed_minimal_vault` 加 `wiki/derived/derived-test.md`（citations 含 anchor `#sha256`）+ 把 candidate elixir `derived_from` 追加 `wiki/derived/derived-test.md`；`_case_classify_recognizes_known_prefixes` 加 3 个 derived 断言（path / `./` / bare id）；新增 4 个 case：`derived up → source → raw` / bare-id / `derived down → elixir` / `elixir up parents 含 derived`。D-3 acceptance golden `tests/fixtures/acceptance/D3/case_elixir_stage3_compounding/expected/stdout/05-trace-up.json` refresh：旧 golden 把 `wiki/derived/*.md` 标 `kind: unknown` + `not_found: true`，refresh 后改为 `kind: derived` + 有效 `label/path`（预期正向变化），diff `1 file, +8/-10`。verify PASS（2135 unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review APPROVE_WITH_NITS（ses_1e43d1fceffehHOsyzrX2VyjQ7，两个 non-blocking nit：`source down → derived` 反向边 + children 去重，留 follow-up）。下一步 P4-INV-11-revisit：promote receipt 加 `counter_evidence_provenance: "real"|"none_found"`。
- 2026-05-12 (P4-INV-NEW-3 `--include-elixir` doc/CLI 一致性收口 F-INV-NEW-3)：doc-only 修正。`docs/Furnace Investing Dogfood Plan.md` §2.6 §188 行移除错误的 `aiwiki ask "..." --include-elixir <elixir-id> --protocol investing`（CLI 中 `--include-elixir` 仅在 `alchemy-start` / `alchemy-distill` 注册，`ask` parser 不支持），改为 `aiwiki ask ... --protocol investing` + 注释「ask 当前不接 `--include-elixir`，旧丹复用通过下一步 alchemy-start `--include-elixir` 注入」；同步修正 §195 F-INV-14 描述把入口标注为 `alchemy-start/-distill --include-elixir`。决策候选 A（doc-only）vs B（给 ask 加 `--include-elixir`）：选 A，理由 KISS + Global Constraint「不动 CLI 表面」+ query-time compounding 与 alchemy 持久链复用语义不同，B 留 follow-up。**不动任何 src/**；`grep -rn "ask.*--include-elixir" docs/ README.md` 残留 check 通过。verify PASS（2131 unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review APPROVE_WITH_NITS（ses_1e44ceaccffeNbBjaKsgb5HvQk，nit：§187 注释措辞已 fold）。下一步 P4-INV-NEW-4：trace 识别 `wiki/derived/*.md` 资产 kind。
- 2026-05-12 (P4-INV-NEW-2 run-compile fail-fast per-job receipt 收口 F-INV-NEW-2)：`run-compile` 三条 fail-fast 分支（source page / concept page / concept-rewrite proposal）落 per-job JSON receipt 到 `output/control/execution-receipts/run-compile-<action_id>.json`，按 alchemy / file-back / l3-proposal-apply 既有 pattern。`src/aiwiki/runner/workflows.py` 新增 `_unique_run_compile_action_id(root, started_at_ms)`（`run-compile-<epoch_ms>` + `-<n>` 冲突递增）+ `_write_run_compile_failure_receipt(root, *, subject_kind, subject_id, target_file, source, item_audit, item_result, exc, started_at_ms, duration_ms, used_profile, item_retry_profile, fallback_stages, fallback_reason, extra=None) -> str` helper，`atomic_write_text` + `json.dumps(... ensure_ascii=False, indent=2, sort_keys=True)`，全包 try/except — write 失败 best-effort log warning，**绝不掩盖** 原 LLMError/RuntimeError。Receipt schema：`version=1` / `kind="execution-receipt"` / `generated_by="aiwiki-run-compile"` / `operation="compile"` / `status="failed"` / `action_id` / `applied_at` / `subject_kind ∈ {source_page, concept_page, concept_rewrite_proposal}` / `subject_id` / `target_file` / `source`(source-page only) / `prompt_profile` / `retry_prompt_profile` / `duration_ms` / `error_class` / `error_message` / `fallback_stages` / `fallback_reason` / `llm_audit` / `response_id` / `usage` / `receipt_path` / `revert_supported=false`；concept / rewrite 分支 extra 字段 `source_pages` / `concept_page` / `quality_priority` / `quality_issues`。`_stamped_record` 在三条 fail 分支的 event dict 内追加 `receipt_path` 字段（不动其它字段，**只追加**），打通 `.aiwiki/logs/llm-receipts.jsonl` ↔ `output/control/execution-receipts/run-compile-*.json` 交叉引用。**成功路径不写 per-job JSON**（保持 acceptance fixture byte-stable + vault footprint）。`tests/test_run_compile_fail_fast.py` 加 4 个 test：source-page fail e2e 写 receipt + 全字段 / 成功路径 0 receipt / receipt-dir 被文件占据时原异常不被掩盖 / parametrized helper test 覆盖 3 个 subject_kind 的 wiring + extra 字段。verify PASS（unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review r1 APPROVE_WITH_NITS（contract JSONL target 措辞 + 缺 concept/rewrite call-site 覆盖）→ r2 APPROVE_WITH_NITS（仅 contract 文档 nit subject_kind 三种漏一种，已修）（ses_1e45bb9c3ffexSd9b8FmqEhtyw）。下一步 P4-INV-NEW-3：`--include-elixir` 文档与 CLI 一致性收敛。
- 2026-05-12 (P4-INV-NEW-1 adaptive run-compile timeout 收口 F-INV-NEW-1)：`run-compile` 在 `create_client` 前按 pending 队列里最大 raw 字节自适应估算 per-call timeout。`src/aiwiki/runner/workflows.py` 新增 `_compute_adaptive_compile_timeout(root, pending)` helper，常量 `BYTES_PER_PAGE=30_000` / `SECONDS_PER_PAGE=60` / floor 240 / ceil 1800，公式 `clamp(ceil(max_bytes / 30_000) * 60, 240, 1800)`。语义：env `AIWIKI_LLM_TIMEOUT` 显式设置 → 返回 `None`（env 永远赢，`LLMConfig.from_env` 沿用）；pending 为空 → `None`；pending 非空但所有 stored_path 都不可 stat / 越出 vault root（含 abs path / `..` 穿越） → 回退 floor 240s。Containment guard 用 `root.resolve()` + 每条 `raw_path.resolve()` + `relative_to(root_resolved)`。Helper 在 paths filter 之后执行，只看真正会喂给 LLM 的子集。**不动** `LLMConfig.timeout_seconds=120` 默认 / CLI surface / receipt/audit/runtime-history schema / 现有 backend。`tests/test_runner.py` 加 6 个 adaptive test（empty→None / large→1800 cap / small→240 floor / env override→None / all-unstatable mixed (missing+abs+`..`+empty+non-dict)→floor / 3-entry largest-wins→600s）。`docs/Furnace Investing Dogfood Plan.md` §2.3 加 adaptive 公式 + 三条规则。verify PASS（unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review r1 REQUEST_CHANGES（docstring/docs 与 empty-pending 行为不一致 + 缺 containment guard + 缺 unstatable/largest-wins test）三处全修后 r2 APPROVE_WITH_NITS（ses_1e46b7d9affebphzcdKdjaolc9，唯一 nit：可选 integration test patch create_client，不阻塞）。下一步 P4-INV-NEW-2：run-compile fail-fast 写 execution-receipt。
- 2026-05-12 (Investing Dogfood v2 真实研报实跑 5/7 闭环)：3 份真 PDF（韦尔 274p / 宁德 229p / 恒瑞 249p）走完 §2.1-§2.6。backend=opencode-api/deepseek-v4-pro；run-compile 1211s（`AIWIKI_LLM_TIMEOUT=600`）→ 3 source pages 全 compiled；3 file-back judgment 全 confirmed；2 settled elixir（主丹 `elixir-ai-thesis-2024-97f9dc26` + 复利丹 `elixir-vs-thesis-b8dadbd3`，`derived_from` 跨丹引用 + trace --direction up 递归可见）。Step 4 drift_scan stale_judgments 闭环验证 PASS：手工 backdate 韦尔 `last_reviewed` 至 2 天前，`STALE_JUDGMENT_DAYS=1` + `AIWIKI_NIGHTLY_DETERMINISTIC_ONLY=1` 触发 nightly → `drift_aging.signals_appended=1`，新 signal 落 `.aiwiki/state/signals.jsonl`（kind=drift, severity=medium, judgment_refs=[韦尔]）。Receipt 落盘：`output/reports/dogfood-receipt-investing-v2.md`（vault），file-back→review confirmed。**4 个新摩擦点 F-INV-NEW-1~4**：(1) `AIWIKI_LLM_TIMEOUT` default 120s 对中文年报严重不足；(2) `run-compile` fail-fast 未落 execution-receipt；(3) `ask --include-elixir` 文档与 CLI 不一致（实际入口在 alchemy-start）；(4) `trace` 不识别 derived 资产 kind。**F-INV-11 仍开放**：counter_evidence 走 NONE_FOUND 兜底通过，非真材料反证。§2.7 L3 proposal 留 v2.1。
- 2026-05-12 (D L3 proposal apply→revert acceptance Gap D 收口)：L3 governance lane create→apply→revert 三步链上提到 acceptance 层。新 fixture `tests/fixtures/acceptance/D/case_l3_proposal_apply_revert/`（seed `prompts/test-prompt.md` + 7 byte-stable golden：target / `.aiwiki/state/l3-proposals.json` / 2 receipts (apply/revert) / runtime-history / audit / wiki log）。`tests/acceptance/case_runner.py` 加 `_run_l3_proposal_apply_revert(vault, *, target_file, new_content, proposal_id, kind)` helper 真调三函数（非 stub），并 `_copy_case_and_fix_clock_from` 补 `monkeypatch.setattr("aiwiki.execution.l3_proposals.utc_now", ...)`（module-local binding via `from aiwiki.app_utils import utc_now`）+ `monkeypatch.delenv("AIWIKI_DISABLE_AUTOMATION", raising=False)`（kill switch 不应影响 acceptance）。`test_acceptance_no_stop_line_violations` 给 D 子树加精确例外（`rel == case or rel.startswith(case + "/")`），仅放过 `l3-proposal-apply` 一词，其余四个 forbidden 词全树仍禁。测试除 7 golden 字节相等外，断言 hash invariants：`revert.restored_hash == apply.before_hash` + `apply.after_hash != revert.restored_hash`。acceptance 16 → 17；verify PASS（unit + 17 acceptance + 92% coverage + ruff clean）；fresh oracle qa-review 一轮 REQUEST CHANGES 三个 finding 全修后 APPROVE（ses_1e535c7a9ffeuU1bt4wGevlX0m）。**Gap A/B/C/D（PROGRESS slim / drift_scan / drop_url / L3 proposal apply-revert）全部收口**。
- 2026-05-12 (C drop_url acceptance Gap C 收口)：`drop_url` end-to-end materialization 上提到 acceptance 层。`tests/fixtures/acceptance/C/case_drop_url/` 新建（空 vault root + 4 byte-stable golden：`raw/inbox/agent-architecture-survey.md` / `wiki/indexes/log.md` / `.aiwiki/state/runtime-history.jsonl` / `.aiwiki/state/audit.jsonl`）。`tests/acceptance/case_runner.py` 加 `_run_drop_url(vault, monkeypatch, *, url, title=None, fetched=None)` helper（patch `aiwiki.drop._fetch_url` 唯一外部边界 + 默认 fetched payload `image_urls=[]` 避开 asset 不稳路径）。`test_drop_url_writes_raw_note_and_logs` 断言 result dict shape (material/note_path/final_url/asset_paths=[]) + 4 golden。范围窄：drop_pdf/image/repo 由现有 unit test 覆盖。acceptance 15 → 16；verify PASS（2125 unit + 16 acceptance + 92% coverage + ruff clean）；fresh oracle qa-review APPROVE 一次过（ses_1e570f42affeFj4yatk0zyZ6QF）。
- 2026-05-12 (B drift/aging acceptance Gap B 收口)：`drift_scan` 三个扫描器（stale judgments / changed evidence / dependency breaks）上提到 acceptance 层。`tests/fixtures/acceptance/B/case_drift_scan/` 新建，含 1 stale judgment + 1 elixir w/ drifted+stale citation_snapshots（`raw/evidence.md` digest 故意错 + `raw/missing.md` 不存在）+ 1 elixir w/ broken `derived_from`，golden 覆盖 `.aiwiki/state/{drift-aging.json, signals.jsonl, runtime-history.jsonl, audit.jsonl}` 字节稳。`tests/acceptance/case_runner.py` 加 `_run_drift_scan(vault, monkeypatch, now=...)` helper（函数级直调避开 CLI 不存在 + nightly 无关 determinism；显式 `delenv AIWIKI_STALE_JUDGMENT_DAYS` + `monkeypatch uuid.uuid4 version=4` 在 helper 内部，避免 leak 到 collector path）。acceptance 14 → 15；verify PASS（2125 unit + 15 acceptance + 92% coverage + ruff clean）；oracle qa-review APPROVE after fixes（ses_1e58d3070ffemOz03PdcVDGNxv）。
- 2026-05-12 (D-3 acceptance Gap P1 收口)：D-3「金丹复利」从 unit-only 上提到 acceptance 层。新增 fixture `tests/fixtures/acceptance/D3/case_elixir_stage3_compounding/`，跑 5 步 CLI 链（alchemy-start/distill/finalize/promote + trace up），断言新丹 `derived_from` 含旧丹 + derived anchor / promote bundle 含 counter_evidence / `trace up` 递归 parents / 3 state JSONL byte-stable。`tests/acceptance/case_runner.py` 加 monkeypatch 把 `app_execution.datetime` + `execution.alchemy.datetime` 也 patch 成 `_FixedDateTime`（之前只 patch `utc_now`，promote receipt epoch_ms 走 `datetime.now(timezone.utc)` 泄漏）。acceptance 13 → 14；verify PASS（2125 unit + 14 acceptance + 92% coverage + ruff clean）；oracle qa-review PASS（ses_1e5c14353fferejeB7qf8aa4uG）。
- 2026-05-12 (SC-010 Structural Consolidation Sweep 完结)：清理 SC-008 fresh oracle 两条 non-blocking follow-up。`src/aiwiki/drop.py` `_LOGGER` 定义上移到模块顶部 constants 区；`tests/test_drop_phases.py` 新增 integration test `test_drop_pdf_finally_cleanup_logs_warning_on_rmtree_failure`（prefix-selective rmtree wrapper 不污染测试环境）。verify PASS；fresh oracle qa-review PASS（ses_1e5fbd4cfffek862Ps5zD09BDA）。**Structural Consolidation Sweep（SC-001..SC-010）整体收官**。完整 SC-001..SC-010 详情见 `archive/rounds/structural-consolidation-sweep.md`。
- 2026-05-11 Product UX Rectification Sweep (R97~R108 EP-001..EP-007) 详情见 `archive/rounds/ep-001-ep007-product-ux-sweep.md`。
- 2026-05-09：Product Shell + runtime LLM provider profile 整理完成；默认 route `opencode-api/deepseek-v4-pro`；NVIDIA NIM fallback 保持 `nvidia-nim-api/openai/gpt-oss-120b`；verify PASS（2001 unittest + 13 acceptance，coverage 92%），Product Shell Jest PASS（55 tests）。

## Milestone Quick Index

> 每行只保留世代 / Milestones / 一句主旨 / 状态。完整实现细节、Stop Lines、Residual Risks 见 `archive/rounds/milestone-quick-index-detail.md`。

| 世代 | Milestones | 状态 |
|---|---|---|
| **P4 Dogfood-driven** (2026-04-28) | P4-1a~1d, P4-2~6, P4-9, P4-11, P4-15 | ✅ 全部 done |
| **D 系列** (2026-04-30) | D-1~D-4 + D-3 R1 + D-4 v0/v1 | ✅ 全部 done |
| **P4-INV** (Round 57-59) | P4-INV-1~4 | ✅ 全部 done |
| **Post-R61 改进** (2026-05-03) | harness 增量升级 / QA review 启用 / plans-merge / Round 62/63 UI Polish | ✅ Round 62/63 done / 其余 🔄 |
| **Round 64-66 UX Earnest** (2026-05-03) | 命名去时间戳 / 拖放修复 / 面板精简 / L3 自动采纳 / 图谱锚点 / 导航简化 | ✅ 全部 done |
| **Round 67 Auto-adopt Hardening** (2026-05-04) | judgment review / L3 audit / nightly aggregation / strict JSONL | ✅ done (`6711efd`) |
| **Round 67.5 Acceptance Fixture Refresh** (2026-05-04) | M6.1b prompt_hash drift refresh | ✅ done (`284f8af`) |
| **Round 68 Progress Slimming** (2026-05-04) | PROGRESS 三层瘦身 / rounds archive / stop_line_audit lint | ✅ done (`2c408f9`) |
| **Round 69 Atomic State I/O** (2026-05-04) | `atomic_write_text` + `atomic_append_jsonl` helpers / 4 saver 替换 | ✅ done (`7ee3ab8`) |
| **Round 70 Receipt JSONL 事务化** (2026-05-04) | 12 JSONL writers 全量原子化 / mm revert 双 receipt + `reverts/` 子目录 | ✅ done (`950f291`) |
| **Round 71 Fetch & Path 安全** (2026-05-04) | `safe_fetch` + `safe_resolve_within` / SSRF / repo symlink 跳过 / Playwright route 拦截 | ✅ done (`a6074b9`) |
| **Round 72 Lock 高优先级缺锁补齐** (2026-05-04) | drop_* 五入口 + nightly_health + receipt_history 全加 `runtime_write_lock` | ✅ done (`addd53d`) |
| **Round 73 LLM/notify HTTP 安全** (2026-05-04) | `safe_fetch` 扩 POST + headers + redirect strip auth / llm.py + notify.py 切换 | ✅ done (`c0cf944`) |
| **Round 74 L3 事务化 + audit auto-revert** (2026-05-04) | `apply_l3_proposal` 后半 5 步 TX / `L3PostApplyAuditError` 携带证据 / auto_reverted runtime_history | ✅ done (`b6a64f5`) |
| **Round 75 receipt_history TX** (2026-05-04) | `_durable_truncate` / `ReceiptHistoryAuditError` + `ReceiptHistoryRollbackError` / R74 残余关闭 | ✅ done |
| **Round 76 runtime_history TX + audit-mirror 上移** (2026-05-04) | `_durable_truncate` + AuditMirror* 上移 `app_utils` / R75 API 保持 | ✅ done |
| **Round 77 LLM receipt TX** (2026-05-04) | `_append_llm_receipt` snapshot-then-rollback / 复用 AuditMirror* | ✅ done |
| **Round 78 age audit single-file TX** (2026-05-04) | `_durable_restore_or_remove` / `_write_age_audit` snapshot bytes | ✅ done |
| **Round 79 auto_adopt 顶层 lock** (2026-05-04) | 4 个 `auto_adopt_*` 入口加 `@runtime_write_operation` / reentrant | ✅ done |
| **Round 80 safe_fetch response close** (2026-05-04) | urlopen response 用 `with` 包；R71-R73 残余关闭 | ✅ done |
| **Round 81 citation snapshot path guard** (2026-05-04) | `safe_resolve_within` + wiki/judgments\|decisions 白名单 | ✅ done |
| **Round 82 citation revert guard 对称收口** (2026-05-04) | `revert_machine_memory_action` citation 分支复用同 helper | ✅ done |
| **Round 83 safe_fetch DNS pinning + host allowlist** (2026-05-05) | pinned-IP connect / proxy 禁用 / SNI 保留 / opt-in allowlist | ✅ done |
| **Round 84 fail-soft 收口 + 事实层 strict read 迁移** (2026-05-05) | notify 双层 fallback / `load_runtime_history_strict` / 6 callers 切 strict | ✅ done (`c94cc87`) |
| **Round 85 history JSONL strict migration** (2026-05-05) | execution policy/receipt history strict variants / fact-layer 切 strict | ✅ done (`ea08d6e`) |
| **Round 86 safe_fetch 多 IP fallback** (2026-05-05) | R83 pinning 可用性回归修复 / resolver 顺序循环 TCP | ✅ done (`b345ecf`) |
| **Round 87 R85/R86 non-blocking 小补丁** (2026-05-05) | history strict 测试断言收紧 / HTTPS fallback `wrap_socket` 单次 | ✅ done (`0891a44`) |
| **Round 88 PM-UX 三件套** (2026-05-05) | Today 空态 CTA / "处理中" 卡 state machine / 面板文案白话化 | ✅ done |
| **Round 89 PM/UX 信任闭环 + 文案统一** (2026-05-05) | pending 持久化 / 状态机两段式 / Today→今天 / groupSpecs 中文化 | ✅ done |
| **Round 90 提交→状态→结果 闭环** (2026-05-05) | Today "刷新炉子" + last-updated / pending 行动卡 / done TTL 7d | ✅ done |
| **Round 91 Advanced 抽屉信息架构** (2026-05-05) | dev banner 外置 / 三组可折叠 section + 持久化 | ✅ done |
| **Round 92 Alchemy Apply Lock + Receipt Atomicity** (2026-05-05) | runner/alchemy.py 7 入口加 lock / 7 receipt write 切 atomic / preview 透传 | ✅ done |
| **Round 92.1 Alchemy Lock Audit (Tight)** (2026-05-05) | 3 残余 unlocked writers 加 decorator / `_walk_preview_lock_status` 双函数 | ✅ done |
| **Round 92.2 Machine-Memory Action TX** (2026-05-05) | `apply/revert_machine_memory_action` snapshot/rollback / 3 apply_mode / `MachineMemoryActionReceiptError` | ✅ done |
| **Round 92.3 Drop Input Safety** (2026-05-05) | 本地 ingestion 边界：PDF 50MB / image 25MB + MIME 白名单 / repo max_files normalize | ✅ done |
| **Round 92.4 Protocol/Manifest 单文件原子写** (2026-05-05) | 6 处 protocol/manifest JSON 切 `atomic_write_text` | ✅ done |
| **Round 92.5 Cache Layer Fail-Soft** (2026-05-05) | SQLite query cache 8 boundary fail-soft / 5 compile build-state save fail-soft | ✅ done |
| **Round 92.6 Lock Audit Wide** (2026-05-05) | 7 个 SoT/state writer 入口加 lock 保护（Tight 5 + Standard 2 conditional） | ✅ done |
| **Round 92.7 Deploy/Service Defaults Hardening** (2026-05-05) | systemd installer 默认 deny-by-default / `AIWIKI_VAULT` 强制 / lock timeout / remote repo opt-in | ✅ done |
| **Round 92.8 Feed Parity (Universal Input)** (2026-05-12) | `recent_raw_inputs` summary + plugin reconcile / extractPrimaryPath 扩展 / 不新增 FeedKind | ✅ done |
| **Round 92.9 DETERMINISM compile gate** (2026-05-10) | compile log timestamp-insensitive gate | ✅ done (`f153589`) |
| **Round 92.10 TEST SPLIT test_app.py** (2026-05-10) | 7984 行拆 7 个 focused 文件 / 总 test count 不变 | ✅ done (`00b41ad`) |
| **Round 92.11 PERF Benchmark** (2026-05-10) | measurement-only `compile_wiki` benchmark / 不接 CI gate | ✅ done (`0944726`) |
| **Round 93.0 autonomy_policy fail-closed** (2026-05-11) | malformed → `CorruptStateError` / `load_autonomy_policy` 切 strict | ✅ done (`7e43e4e`) |
| **Round 93.1 material-archive TX 雏形** (2026-05-11) | `apply/revert_archive_action` 早期 receipt-tier TX；R95.1 接续 | ✅ done (`37c9341`) |
| **Round 93.2 compile-artifact 原子写** (2026-05-11) | compile pipeline 派生 artifact 全切 `os.replace` 原子路径 | ✅ done (`6d2cee6`) |
| **Round 93.3 ingest subprocess timeout** (2026-05-11) | `drop.py` ingest subprocess 加 `timeout=` / `IngestionTimeoutError` | ✅ done (`137aa4e`) |
| **Round 94.0 automation.json 原子写** (2026-05-11) | `atomic_write_text` + `fsync=True` / `load_automation_state` strict + corrupt 回收 | ✅ done (`0ddc2c7`) |
| **Round 94.1 drift_scan → append_runtime_history** (2026-05-11) | drift_scan 改走 `append_runtime_history` 复用 R76 TX + audit-mirror | ✅ done (`6bf39e0`) |
| **Round 94.2 ingest_source raw/ atomic + orphan tmp 跳过** (2026-05-11) | raw write 切 atomic + fsync / raw scanner 过滤 `*.tmp.*` | ✅ done (`4ba29e9`) |
| **Round 94.3 mm-action read-then-write fail-closed** (2026-05-11) | `apply/revert_machine_memory_action` 切 `load_json_document_strict` | ✅ done (`a85f599`) |
| **Round 94.4 concept rewrite apply/revert TX** (2026-05-11) | file + state pair TX / reversed write-order rollback | ✅ done (`ecef27f`) |
| **Round 94.5 L3 Proposal apply/revert TX** (2026-05-11) | file + receipt + state triple TX / `L3{Apply,Revert,Reject}{ReceiptError,HalfWriteError}` | ✅ done (`b093241`) |
| **Round 95.1 archive apply/revert TX** (2026-05-11) | receipt+state pair TX / phase-2 derived audit swallow-with-warning（设计保留） | ✅ done (`a49ead8`) |
| **Round 95.2 drop.py raw note + asset atomic** (2026-05-11) | raw note + asset 全切 `atomic_write_text` / `atomic_write_bytes` + `fsync=True` | ✅ done (`0731823`) |
| **Round 95.3 L3 Phase-2 False-Write Truncate** (2026-05-11) | `_durable_truncate(path, snapshot_size)` 截回 commit 前长度 | ✅ done (`70ff772`) |
| **Round 95.4 Nightly Audit Reconciler** (2026-05-11) | `reconcile_execution_receipts` nightly 检 false-success / 仅扫 `revert` | ✅ done (`3679079`) |
| **Round 96.0 alchemy receipt persistence TX** (2026-05-11) | `_persist_receipt_transactionally` 收口 receipt-tier TX 范式 | ✅ done (`4b3e32f`) |
| **Round 96.1 alchemy review_apply TX** (2026-05-11) | `run_alchemy_review_apply` receipt-tier TX | ✅ done (`8a4dd91`) |
| **Round 96.2 alchemy lane primitive Receipt TX** (2026-05-11) | `_run_receipted_lane_primitive` wrapper TX | ✅ done (`44100e2`) |
| **Round 96.3 alchemy distill/propose_apply TX** (2026-05-11) | `run_alchemy_distill_proposal_apply` / `run_alchemy_propose_apply` TX | ✅ done (`1db7925`) |
| **Round 96.4 create_l3_proposal TX** (2026-05-11) | state + page + history + audit + wiki-log 全包 receipt-tier TX | ✅ done (`4df35ea`) |
| **Round 96.5 mm_action_batch Receipt-Tier TX** (2026-05-11) | `apply_machine_memory_actions_batch` snapshot/rollback 全包 | ✅ done (`5aa4a92`) |
| **Round 96.6/96.7 L3 Phase-2 Audit Swallow DROP** (2026-05-11) | fresh oracle pre-design DROP；决议归档 `.codex/contracts/archive/R96.6/96.7-*.md` | ❌ dropped |
| **Round 96.8 apply_l3_proposal Rollback 跨流一致** (2026-05-11) | 外层 rollback 顺序 reversed write-order；补 `runtime_history_path` 还原 | ✅ done (`f2c9f5a`) |
| **Round 96.9 L3 Apply Phase-2 Audit Reconciler 扩流 DROP** (2026-05-11) | fresh oracle pre-design DROP；决议归档 `.codex/contracts/archive/R96.9-*.md` | ❌ dropped |
| **Round 97 Decision-grade Report Skeleton** (2026-05-11) | `output_format=report` 升级到 6 区块固定骨架 + `_validate_report_sections` | ✅ done |
| **Round 98.1 Decision-grade Report Bullet Minimums** (2026-05-11) | 每段 `- ` bullet 下限 + 拒 `_LLM:` placeholder 残留 / fence-aware bullet counter | ✅ done (`ae9e19f`) |
| **Round 98.2 Report Citation Integrity** (2026-05-11) | `## 引用` dedup + body ⊆ citations 校验 / fence-aware path 提取 | ✅ done |
| **Round 98.3 Report Strictness Hardening** (2026-05-11) | Phase 0 unclosed fence 拒绝 + Phase 1.5 duplicate required H2 拒绝 | ✅ done |
| **Post-P4 D-2 PROGRESS Slim** (2026-05-12) | Round 73-92.7 + Milestone Quick Index detail 切档归档 / PROGRESS 117KB→18.5KB | ✅ done (`c472677`) |
| **Post-P4 B drift_scan Acceptance** (2026-05-12) | drift_scan 三扫描器函数级 acceptance / 4 byte-stable golden / 15 cases | ✅ done (`54d7e2c`) |
| **Post-P4 C drop_url Acceptance** (2026-05-12) | drop_url end-to-end materialization acceptance / 4 byte-stable golden / 16 cases | ✅ done (pending commit) |
| **Post-P4 D-3 Acceptance Gap P1** (2026-05-12) | 金丹复利 5 步 CLI 链 acceptance / 3 state JSONL byte-stable | ✅ done (`8cd2b25`) |
| **Post-P4 B Drift/Aging Acceptance Gap B** (2026-05-12) | drift_scan 三扫描器函数级 acceptance / 4 artifact byte-stable | ✅ done |


## 状态 — 当前活跃 3 轮

### Round 92.8 — Feed Parity (Universal Input pending closure) — 完成

- **目的**：闭合 P2 fresh oracle scoping 排为 SHIP first 的用户可见 feed-parity bug——Universal Input 投料（URL/PDF/image/repo/note）成功后，pending 卡长期停在 "已接收，等待生成报告"，因为 (1) `drop_*` 返回 payload 含 `note_path` 但 plugin `extractPrimaryPath()` 只识别 `path/output_path/receipt_path/state_path/index_path/report_path`；(2) shell summary 不暴露 raw ingest，`reconcilePendingSubmissions()` 永远 miss raw drop。
- **实现**：
  - `src/aiwiki/app_shell/summary.py:145-166`：新增 `_build_recent_raw_inputs(root, *, limit=8)` helper，从 `load_runtime_history(root)`（NON-strict，关键：strict 会 raise 破坏 fail-soft）过滤 `event_type == "raw-added"`，倒序取 8 条，映射 `stored_path / original_path / source_type / title / occurred_at / protocol`，全部 `str() or ""` coercion；body wrap broad `try/except (OSError, ValueError, TypeError, json.JSONDecodeError, KeyError)` 返回 `[]` 不 raise。`build_shell_summary` 在 line 241 预计算，line 312 dict literal 插入 `"recent_raw_inputs": recent_raw_inputs` 紧跟 `recent_runs`。
  - `src/aiwiki/today_feed.py:415-440`：新增 `_build_raw_input_entries(summary, today_date)` helper，仅当 `recent_raw_inputs` 是 list 时迭代；空 `stored_path` 跳过；`_date_part(occurred_at) != today_date` 跳过；构造 `FeedEntry(kind="action", title=f"已投料：{title or original_path or stored_path}", summary=f"已接收 {source_type or '材料'}，等待编译/刷新", target=stored_path, ...)`。`build_today_feed` line 82-83 在 `_build_action_entries` 之后追加。**关键**：未新增 FeedKind，复用 `kind="action"`（priority 6 最低），避免 schema 改动。
  - `.obsidian/plugins/furnace-product-shell/src/plugin_helpers.js:178`：`extractPrimaryPath()` `candidateKeys` 数组扩展为 `["path", "output_path", "receipt_path", "state_path", "index_path", "report_path", "note_path", "stored_path", "asset_path"]`，**新增 keys 排末尾**保留既有 ask/run path 优先级。
  - `.obsidian/plugins/furnace-product-shell/src/plugin.js:2150-2213`：`reconcilePendingSubmissions()` `rawCands = summary.recent_raw_inputs`，guard 允许 raw-only proceed；`matchAgainst` `fields` 数组扩展 `stored_path / original_path / note_path`；hit 顺序保持 outputs → receipts → raw fallback；命中 raw → `target = "raw"` + `targetPath = String(hitCand.stored_path || hitCand.path || "")` → `markPendingSubmissionDone(entry.id, "raw", targetPath)`。`markPendingSubmissionDone` 不 whitelist target 字符串（plugin.js:2085-2095 直接 `entry.reconcileTarget = String(reconcileTarget)`），"raw" 不 no-op。注释 comment 同步更新（plugin.js:2082）。
  - `.obsidian/plugins/furnace-product-shell/src/today_feed.js:58-59,286-305`：JS mirror 同步新增 `buildRawInputEntries`，被 plugin 加载，与 Python 渲染契约一致。
  - `tests/test_feed_parity.py` 10 测试 + `load_tests` bridge：4 summary（drop history 收录 + 全字段映射 / 无 history 空 / 损坏 JSONL fail-soft 返回 [] / 12 events 限 8 条 most-recent-first / 混 event_type 仅 raw-added）+ 3 today_feed（today date 渲染 kind=action / 昨天跳过 / 空 stored_path 跳过）+ 2 JS grep（plugin_helpers.js candidateKeys 含三新 key / plugin.js reconcile 含 `recent_raw_inputs` + `target = "raw"` + `stored_path` token）。
- **验证**：`bash scripts/verify.sh` PASS — **1848 unittest**（+10）+ 13/13 acceptance + branch coverage 92% / ruff + compileall clean。
- **qa-review**：fresh oracle session `ses_2055cf69dffeSzSAEpOCXG597D` PASS（首次 retry，非阻塞 concerns：JS 测试是 grep 级 vs 真执行级 / `markPendingSubmissionDone` 注释 comment 已 fix）。End-to-end 链路 trace 完整：user → Universal Input → `aiwiki drop <payload>` → `dispatch.py` rewrite → `drop_*` → `_append_raw_added_history` JSONL → `build_shell_summary` `recent_raw_inputs` → plugin reconcile `rawCands` → match via `stored_path` → `markPendingSubmissionDone("raw", stored_path)`。
- **Stop Lines**：0 `drop_*` 返回签名改 / 0 CLI signature / 0 `runtime_history` 写语义 / 0 schema / 0 lock primitive / 0 新 FeedKind（`today_feed.FeedKind` Literal 不变）/ 0 `general` default protocol / 0 LLM defaults / 0 notify defaults / 0 `safe_fetch` allowlist / 0 Universal Input 命令字符串（plugin.js:2239 仍 `["drop", normalizedPayload]`）。
- **Migration**：纯加层；既有 dogfood vault 重启 plugin 后 raw drop pending 卡可被 reconcile 自动 mark done；既有 ask/run path 优先级不变。
- **Residual Risks**：JS grep 测试只覆盖 token 存在，不覆盖顺序 / 行为变化 → 未来 plugin 大改时回归防护偏弱；可在后续 round 补 node 真执行测试（DEFER 不在本轮 scope）。
- **归档**：contract `.codex/contracts/archive/round-92-feed-parity.md`。


---

> 更早的 round 详情请参考 `archive/rounds/round-*.md` / `archive/rounds/p4-*.md`，或读 `archive/rounds/index.json`。
