---
name: devloop
description: AgentStack L1 flow for small scoped code changes with targeted verify.
---

# Devloop

Orient, edit narrowly, run `scripts/agentstack-verify --target auto`, record retry attempts with `scripts/agentstack iteration` when checks drive another fix, fix focused failures, then finish.
