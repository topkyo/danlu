"""Read-only shared corpus helpers (paths, scoring, ranks, parse, sections, snapshots, link_state).

Owned symbols used by both ``content`` and ``memory``.
MUST NOT import ``content``, ``memory``, ``execution``, or ``runner``.
"""
