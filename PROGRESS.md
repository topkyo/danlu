# 炼丹炉 Progress — Furnace 世代

> **结构 v2** (R68, 2026-05-04): PROGRESS.md 仅保留 Quick Index + 活跃 3 轮 + 改进方向指针。
> **PROGRESS.md 仍是当前任务状态唯一 SoT；archive/rounds/ 只是历史延伸。**
> 历史 round 详情：`archive/rounds/round-*.md` / `archive/rounds/p4-*.md`
> 机器索引：`archive/rounds/index.json`
> 切档历史：pre-Round 1 在 `archive/PROGRESS-pre-round1.md`（注意：里面也包含 Round 24/25 的早期记录，已重新落入 `archive/rounds/round-24.md` 和 `round-25.md`）

## SoT 引用

- 终局架构：`docs/Furnace Agent Architecture.md` + `docs/Furnace Evolution Mechanics.md`
- 当前方向：`docs/Furnace Next Direction Post-P4.md`
- Active contract：`.codex/contracts/active.md`
- 改进清单：见本文件底部「改进方向」段


## 当前动态

- 2026-05-20 (文档漂移复核 + dogfood proof 历史口径校准)：按用户要求基于 git 日志、`PROGRESS.md` 与当前清仓后的 dogfood vault 状态复核文档漂移。结论：2026-05-13~15 的三天 unattended dogfood maturity 已由旧 session 真实跑通并在 git/`PROGRESS.md` 固化为 `PASS`；2026-05-19 P1 dogfood compounding proof 已补齐 `knowledge_compounding_proof.status=pass` 与非空 `compounding_sample`。当前 `/home/tim/danlu/炼丹炉` 已被清仓恢复为干净初始 vault，旧 `output/control/maturity-gate/*` receipt/snapshot 文件不再存在，因此当前 summarize 读到 `0 receipts/not-yet` 只说明当前 clean vault 没有历史 proof 文件，不能推翻历史 pass。文档修订：`README.md` 已同步“普通 CLI/runtime 默认不做跨 backend fallback、失败显式暴露、不再伪装 deterministic fallback 成功”；`.git/info/exclude` 本地排除 `.antigravitycli/`；`docs/Furnace Runtime Operations.md` 修正 `codex-cli/gpt-5.5` 为手动 interactive route 而非隐藏 backend fallback；`docs/Furnace Agent OS Slimdown Plan.md` 将 AOS-001~004 标记为 completed 并勾选对应 success criteria；`docs/Furnace Next Direction Post-P4.md` 与 `docs/analysis/*AOS*` 补入 AOS-004/P1 proof 已 PASS 且 clean vault 文件缺失不否定历史证据的校准；二次复查继续修正 `docs/analysis/A-Subsystem-Deep-Dive.md`、`B-AOS-005-Next-Slimdown-Round.md`、`E-AOS-005-Followup-Backend-Subsystem-Audit.md` 中把 AOS-004 proof、AOS-005a 或 AOS-006 follow-up 当作未来待办的表述；`docs/Furnace Product Shell UX Test Checklist.md` 修正旧 deterministic fallback checklist；三次复查补齐 `docs/deepseek-comprehensive-evaluation-2026-05-03.md` 与 `docs/Furnace Investing Dogfood Plan.md` 的当前状态校准，避免日期化评估/原始 contract 被误读为当前 backend 或 investing dogfood 状态。验证：`git diff --check` PASS；本轮仅文档与本地 exclude 变更，未改 runtime code，未重跑真实 LLM/dogfood vault。

- 2026-05-20 (Obsidian 原生图谱过滤 + 投料文件名修复)：按用户截图反馈继续 build 收口。根因：截图里的“关系图谱”是 Obsidian 原生 graph，不是 `output/graph/machine-memory.html`；dogfood `.obsidian/graph.json` 当时 `search=""`、`showOrphans=true`，所以把 `wiki/indexes`、`machine-memory`、`output/lint/lint-*` 等 runtime/operator Markdown 全部拉进图谱。修复：新增 bootstrap 默认 `.obsidian/graph.json`，原生 graph 只查 `path:"output/reports" OR path:"raw/inbox"`，`hideUnresolved=true`、`showOrphans=false`，并给 reports/raw inbox 上色；`DEFAULT_OBSIDIAN_APP.userIgnoreFilters` 补入 `raw/assets`、`raw/normalized`、`wiki/indexes`、`output/lint`、`output/packs`、`output/pilots` 等内部目录；真实 dogfood vault 的 `.obsidian/graph.json` / `.obsidian/app.json` 已同步，未触碰 Product Shell LLM/API key。`lint-20260520-071528` 定位为 lint/nightly 生成的 operator QA artifact，适合开发/运维排障，不应出现在用户图谱；本轮通过 graph 搜索过滤和 ignore filters 排除，并在新 vault README/HOME 说明 `output/lint/` 不是用户报告证据。投料文件名：Product Shell 上传的浏览器 File 先写入 `.aiwiki/tmp/product-shell-drop`，此前 auto-drop/modal 只把临时路径传给 CLI，未传原始文件名作 `--title`，导致 raw note 可能以时间戳/hash 命名；现 auto ask、composer、Drop modal 文件选择与拖拽 modal 均将 `file.name` 作为 drop title 传入 universal input，临时文件也改为“原始安全文件名 + stamp”，Python drop 落盘 stem 改为保留中文标题并去掉 `.pdf/.png` 等上传后缀，raw/inbox 文件名会优先来自标题/主题。验证：Python targeted `tests/test_vault.py tests/test_app_compile.py tests/test_runner.py tests/test_llm.py tests/test_app_misc.py tests/test_app_lifecycle.py tests/test_drop.py` PASS（285 tests）；Product Shell targeted Jest `input-attachment-source` + `universal-input-interaction` PASS（35 tests）。隔离 QA review 复查后补齐 modal title propagation、`wiki/indexes` ignore 和中文 title filename 回归；提交前需继续确认 `data.json` 不在 diff/stage。

- 2026-05-20 (ProtocolState LSP + 报告证据图谱 + LLM retry/timeout)：按用户三项反馈继续 build 收口。LSP：`ProtocolState` 是 `total=False` TypedDict，Pyright/Pylance 对 `state["active_protocol"]` / `state["state_path"]` 报错是类型层面正确告警；已将 `runner/prompts.py::_protocol_context()` 改为 `.get()` + 默认值，并对 `app_protocol.py` 的 runtime schema/state/descriptor 返回做 `cast(...)` 收敛，`resolve_protocol()` 不再直接索引 optional key。图谱：默认用户入口从“机器记忆/wiki 资产图”改为“报告证据图谱”，`wiki/indexes/graph-view.md` 和 `output/graph/machine-memory.html` 首屏强调从报告卡片进入，点击证据锚点追溯材料/判断/概念；机器记忆、拓扑和健康页降为高级维护入口。HTML report cards 现在即使锚点节点被中文/文件过滤挡掉，也会展示带 `graph_anchor_node_ids` 的报告卡片并标明可点开锚点数，避免用户看不到自己报告。LLM：有效 API key 仍可能被 provider/proxy 按账号额度、RPM/TPM、burst concurrency、模型容量限流；OpenAI-compatible 后端新增 429/5xx 短重试与 `Retry-After` backoff，`run-ask` report 在未显式设置 `AIWIKI_LLM_TIMEOUT` 时默认 timeout 从 120s 提到 240s，显式 `--timeout` / env 仍优先；不恢复跨 backend 自动 fallback。测试：`PYTHONPATH=src python3 -m pytest tests/test_llm.py tests/test_runner.py tests/test_app_compile.py tests/test_app_misc.py tests/test_app_lifecycle.py -q` PASS（256 tests）。未重跑真实外部 LLM 报告，未改 Product Shell LLM/API key，未自动 commit/push。

- 2026-05-20 (raw/inbox 导航 + 引用报告 report 上下文修复)：按用户对真实 dogfood 重跑测试的四点反馈继续收口。导航口径调整为普通用户左侧显示 `raw` / `raw/inbox` 作为投料入口，默认仍隐藏 `wiki` 和 operator/runtime 分层；`output/reports` 继续扁平化为“报告”一层，不再显示“全部报告”二级目录。实现上 `USER_HIDDEN_FOLDER_PATHS` 不再隐藏 `raw`，改为只隐藏 `raw/assets` 与 `raw/normalized`；dogfood vault 的 `.obsidian/snippets/danlu-zh-folders.css` 已定向同步，未触碰 Product Shell LLM key 配置。引用报告 report 根因修复：`run_ask()` / `run_ask_submit()` 对 `引用报告：output/reports/*.md` 和投料 material hints 统一收集 `material_refs`，report 路径使用清洗后的问题生成 artifact 文件名/标题，并把被引用报告摘录注入 `_build_ask_prompt()` 的 `Quoted Report / Material Context` section；后台 resume 可从 job artifact 的 `material_refs` 重新读取上下文。新增回归测试覆盖 quoted report 生成 report 不再把引用前缀写进 prompt/文件名/正文，并确认 report prompt 能看到被引用 note 内容。验证：`PYTHONPATH=src python3 -m pytest tests/test_vault.py tests/test_runner.py -q` PASS（73 tests）。未自动 commit/push。

- 2026-05-20 (dogfood vault 边界 + run-ask fallback 移除 + local/vault context 修复)：按用户指出的 5 月 20 真实 Shell 提问问题收口。先把 vault 边界写入 `AGENTS.md`：`/home/tim/ai-wiki` 是 runtime 代码仓库/开发 vault，`/home/tim/danlu/炼丹炉` 是真实 dogfood vault；分析 Product Shell 提问、报告质量、LLM receipt、run notes 或 vault 内容时默认查 dogfood vault，避免再次误用当前 repo 的 `output/control/shell-summary.json`。根因确认：02:29 引用报告权限追问当时从 `opencode-api/deepseek-v4-pro` 自动落到 `codex-cli/gpt-5.5`，是旧 canonical backend fallback + Product Shell `--fallback-to-ask` 共同导致；失败后 `run-ask` 写出 `delivery_mode=deterministic-fallback` 的占位式产物。现已移除跨 backend fallback：默认不再 opencode→codex，`AIWIKI_BACKEND_FALLBACK` 被忽略，`BackendFallbackClient` 路径删除；保留同 backend 的 model fallback。CLI/Product Shell 移除 `--fallback-to-ask`，source 与 `main.js` 已 rebuild 同步；`run-ask` direct/frontdoor LLM 失败时记录 `llm-failed` receipt/run notes/失败说明并抛错，不再返回 deterministic fallback payload 或占位答案。新增本地 Markdown 文件数统计 intent：`炼丹炉/vault/仓库 有多少 md/Markdown 文件` 类 note 问题在 LLM 前由本地文件系统统计可见 `*.md`，写出 `delivery_mode=local-deterministic` note，不调用 LLM；口径明示为“当前 vault 内可见 Markdown 文件”，排除 `.git/.obsidian/.aiwiki/.codex/.claude/.opencode/node_modules/.venv`。新增 direct note vault context：无显式材料时从 `wiki/sources`、`wiki/judgments`、`wiki/decisions`、`wiki/concepts`、`wiki/elixirs` 检索相关摘录投喂 LLM；若问题含“联网/搜索/调研”，在未配置 real-time web search 时显式说明只能基于本地 vault 摘录。验证：expanded targeted pytest 20 PASS；Product Shell targeted Jest 26 PASS；`bash scripts/verify.sh python-static` PASS；`bash scripts/verify.sh product-shell-static` PASS；`bash scripts/verify.sh scripts` PASS；oracle QA review PASS，无 blocker。未自动 commit/push。

- 2026-05-20 (关系图谱产品口径 + 引用报告 LLM 追问 + 报告导航扁平化)：按用户三项新反馈收口。图谱产品判断：默认主图应继续是“文件化知识资产图”（`wiki/sources`、`wiki/concepts`、`wiki/judgments/decisions`、`wiki/elixirs`），展示知识如何沉淀和复用；不应退化成只展示 raw 投料和 output 产出的文件流，raw/output provenance 更适合作为报告局部图/交付追溯图；当前实现大方向合理，后续建议把“中文相关”做成默认可切换过滤器，并统一 judgment/decision 口径。引用报告追问：`run_ask()` 现在能识别 `引用报告：output/reports/*.md`，清洗掉引用前缀，把被引用报告正文作为 material context 交给 direct note LLM 路径，避免再次生成空模板/无实质回答；artifact 标题/文件名使用清洗后的人类问题，不泄漏 `output-reports` 噪音。安全边界：quoted report refs 增加 `_safe_quoted_report_reference_paths()`，resolve 后必须仍在 vault root 且仍在 `output/reports/` 内，拒绝 `../../raw/...`、反斜杠和 reports 内 symlink 指向外部报告目录的情况，避免把 vault 内非报告 md/txt 外送给 LLM。左侧导航：移除 `output/reports -> 全部报告` label，新增 Obsidian snippet flatten 规则，只隐藏 `output/reports` folder title/self，不隐藏 reports 文件本身；当前 vault 已启用 `.obsidian/snippets/danlu-zh-folders.css` 并在 `.obsidian/appearance.json` 打开 snippet，使左侧只保留 `output` 的“报告”一层入口。测试/验证：新增 quoted report LLM material context 回归、安全路径负例、vault 导航断言；focused pytest 4/5 PASS；`bash scripts/verify.sh python-static` PASS；oracle blocker review 初次发现 quoted report vault 内越权读取风险，修复后二次复核 blocker closed，无新 must-fix。未自动 commit/push。

- 2026-05-20 (Product Shell Ctrl+Enter + 中文 md 图谱 + Obsidian 属性隐藏收口)：按用户反馈对三处仍未达效的问题做二次修复并补测试。Product Shell：`render_input.js` 的 `Ctrl/Cmd+Enter` 提交改为优先走真实 `form.requestSubmit()`，保留 `handleSubmit()` fallback，并增加 `keyup` fallback 与 800ms chord 节流，`bash build.sh` 已同步 `main.js`；Jest 交互测试新增真实 keyboard event 覆盖 `keydown`/`keyup`、`Ctrl+Enter`/`Cmd+NumpadEnter`。图谱：`build_machine_memory_graph()` 为 file-backed `.md` 节点写入 `chinese_related`，HTML 渲染只展示中文相关且对应现有 Markdown 页面的节点和两端均保留的连接；概念页自动生成模板中文已过滤，避免纯英文 sample 被误判展示，同时保留“英文标题但正文中文”的 md 节点；关系说明面板不再可见泄漏 raw edge type 机器码。输出属性：`styles.css` 扩展 `.aiwiki-output` 与 `output/` markdown leaf 下的 Obsidian Properties selectors（metadata heading/title/content/add button/key/value 等），默认隐藏 note/report 属性 UI，同时保留 frontmatter 审计元数据。为修复 JS 测试环境，将 Product Shell `package.json` 的 `jest` / `jest-environment-jsdom` 更新到与既有 lockfile 匹配的 `^30.3.0`，未改 lock；`npm ci` 后 audit 仍提示 1 个 moderate vulnerability，未自动 `npm audit fix`。已重新 `PYTHONPATH=src python3 -m aiwiki.cli --root . compile`，重建 `output/graph/machine-memory.html`，确认页面文案为“中文相关、且对应现有 Markdown 页面”，且不含 `Transformer Scaling`。验证：targeted Python graph/product-shell 回归 15 PASS；Product Shell targeted Jest 23 PASS；`bash scripts/verify.sh product-shell-static` PASS；`bash scripts/verify.sh python-static` PASS；oracle qa-review PASS，无 blocking findings；`.codex/gates/qa-review.md` / `qa-runtime.md` 已刷新。剩余非阻塞风险：Obsidian Properties 隐藏仍依赖当前 Obsidian DOM/主题，若实机仍失效，下一步应给 output markdown leaf 主动打稳定 class，减少对内部 `metadata-*` DOM 的依赖。未自动 commit/push。

- 2026-05-20 (open-harness large-codebase explorer sync)：按用户要求将 `/home/tim/ai-wiki` 的 local-only harness 同步到 `/home/tim/open-harness` 最新提交 `2ee5fb5 Add OpenCode large-codebase explorer`。执行 `bash scripts/setup_local_harness.sh --apply --tier standard --platforms claude,codex,opencode`，刷新 `.claude/`、`.codex/`、`.opencode/` standard-tier scaffold、shared scripts/libs/platform registry，并新增 `.opencode/agents/open-harness-explore.md`；现有 `.opencode/agents/open-harness-review.md` 保留。手工合并 `AGENTS.md` / `CLAUDE.md` 的通用 harness 协议，补入大仓库 read-first 工作方式：未知区域先局部只读探索、优先最小相关目录和符号/路径/局部 grep、默认排除 generated/vendor/build/third-party/secrets/`.env`、宽任务先 planner 或 contract、只读探索可并行但不接管主闭环。保留当前 `.codex/contracts/active.md` 业务 contract 与现有业务 dirty worktree，不用 harness sync 覆盖 gate artifacts。验证：`bash scripts/setup_local_harness.sh --status` 显示 `exclude_block=present`、`local_scaffold=present`；`bash scripts/verify.sh scripts` PASS；`HARNESS_DIR=.codex bash scripts/closed_loop.sh --verify-target scripts` 的 verify 阶段 PASS，但随后因当前业务 contract 既有 `.codex/gates/qa-review.md` `contract_sha` mismatch 停止，本轮未覆盖该业务 gate。

- 2026-05-19 (Shell Ctrl+Enter + 中文图谱二次清理)：按用户继续反馈修复三处真实残留。Product Shell：`render_input.js` 保留真实 `<form>`，新增 `submitFromChord()`，在 form capture-phase `keydown` 捕获 `Ctrl/Cmd+Enter`，同时支持 `NumpadEnter`，并给提交按钮补 `requestSubmit()` / `handleSubmit()` click 兜底；去掉 `keyup` 兜底以避免极快失败后二次触发；`bash build.sh` 已同步 `main.js`。属性隐藏：`styles.css` 将 `.aiwiki-output` 属性隐藏选择器从单一 `.metadata-container` 扩展到 `.metadata-container / .metadata-properties / .metadata-property`，覆盖 preview/reading/source/mod-cm6、通用 `.aiwiki-output` ancestor，以及 `output/reports/` markdown leaf，并统一 `display: none !important`；runtime frontmatter 仍保留。图谱：`render_machine_memory_graph_html()` 不再把 raw health 的“核心概念/核心来源/修复候选/可安全执行”面板塞进默认图谱页；HTML 展示层只显示标题含中文且对应现有 Markdown 的节点，边也只保留展示节点之间的连接，summary 改为展示节点/边计数；机器图 JSON 仍保持全量 file-backed graph，避免破坏 report anchors / report-subgraph / digest 下游。已用真实 dogfood vault 重新 `compile`，重建 `output/graph/machine-memory.html`；compile 输出中的英文/hash concept 仍存在于机器内存与 rewrite proposals，但默认关系图谱页已不展示。验证：targeted Product Shell + graph tests 13 PASS；`bash scripts/verify.sh product-shell-static` PASS；`bash scripts/verify.sh python-static` PASS；`bash scripts/verify.sh scripts` PASS；oracle blocker-only 复审 APPROVE。non-blocking：属性隐藏仍取决于 Obsidian 当前 DOM 是否使用 `.metadata-*` property 区；若用户在 source mode 直接看 YAML 文本，这是审计元数据，不能删除，只能 UI 隐藏。

- 2026-05-19 (Shell submit + 金丹图谱 + local stats repair)：按用户五点反馈完成 Product Shell、关系图谱、输出属性和“当前 vault 有几个金丹”链路修复。本轮 `.codex/contracts/active.md` 已替换为 `Shell graph output and elixir stats repair`：watcher 仍只负责“等待投料”的 deterministic compile/lint，不默认跑 LLM；Product Shell composer 是显式用户触发的 `run-ask`/local stats 入口，可以走 LLM 或本地确定性回答，因此不与 watcher 边界冲突。Product Shell：`render_input.js` 将 composer 容器改为真实 `<form>`，提交按钮 `type="submit"`，新增 robust `Ctrl/Cmd+Enter` 判定（key/code/keyCode）与 duplicate-submit guard；`styles.css` 解除 Furnace Center/readable width 约束，composer 使用 `width: min(96rem, 100%)` + `max-width: none`，保留低矮 textarea；`bash build.sh` 已同步 `main.js`，并补 `tests/test_product_shell_universal_input.py` 锁定宽度、form submit、Ctrl+Enter 与元数据隐藏 CSS。图谱：`build_machine_memory_graph()` 继续坚持“每个节点都是现有 `.md` 文件”，并新增 settled `wiki/elixirs/*.md` 金丹节点，节点标题优先取 Markdown frontmatter `topic/title/id` 或 H1，避免 slug/hash/`# Elixir` 英文占位泄漏；`derived_from` 中的 elixir→elixir 依赖渲染为 `ELIXIR_DERIVED_FROM` / “金丹承接”；HTML 图谱新增金丹节点样式、筛选项、图例、金丹关联 section，并把统计改为基于过滤后 graph nodes。输出属性：report/note 仍保留 runtime frontmatter 供审计/回滚，但统一加入 `cssclasses: [aiwiki-output]`，Product Shell/Obsidian CSS 隐藏 `.metadata-container`，做到“UI 隐藏属性，文件元数据不丢”。金丹数量问答：`run_ask()` 在 direct LLM 前识别“金丹/elixir + 数量/几个 + vault/炼丹炉/本地”类问题，先剥离 `引用报告：output/reports/...` 噪音并修正 `valut`→`vault`，再本地统计 `wiki/elixirs/*.md` settled 金丹和 `output/_candidates/elixirs/*.md` 候选，生成 `delivery_mode=local-deterministic` note，不调用 LLM；真实 dogfood 命令已重跑，产物 `output/reports/当前炼丹炉vault这个仓库有几个金丹-note.md` 正确回答“已沉淀金丹 2 个，候选金丹 6 个”，并列出两个 settled 金丹文件与六个候选路径；随后 dogfood `compile` 重建 graph/index（只改 dogfood vault，不进 repo）。文档：README 与 graph-view 模板说明默认图谱是文件形态 Agent OS asset graph：`source / concept / judgment / elixir(金丹)`，并明确 Shell 显式 ask 与 watcher 常驻投料监听的边界。验证：targeted 70 tests PASS；`bash scripts/verify.sh product-shell-static` PASS；`bash scripts/verify.sh python-static` PASS；`bash scripts/verify.sh scripts` PASS；acceptance replay 17 PASS；最终 full `bash scripts/verify.sh` PASS（2258 unit + 17 acceptance + 92% coverage）。non-blocking：当前 dogfood vault 上游 concept slug 仍有 `text/raw/pdf/hash` 等历史噪音，图谱已优先中文标题与金丹节点，但进一步清洗概念生成/重命名应另开 targeted contract；Product Shell 宽窄屏视觉仍建议人工看一眼；oracle 复审曾指出 local stats 分支不应先触发 backend preflight，已将金丹统计分支前移到 preflight 之前，并用无 client 回归测试与真实 dogfood 命令确认不再 probe backend。

- 2026-05-19 (full furnace service defaults + maturity timer cleanup)：按用户新要求把 Product Shell composer 在低矮样式基础上再拉长约两倍，并把本机服务默认值改为“常驻等待投料 + nightly 五层炼化 + dogfood maturity 验证 opt-in”。本轮 contract 覆盖为 `Sprint Contract`：watcher 仍 deterministic-only，不默认跑 LLM；nightly 新安装默认五层 `AIWIKI_NIGHTLY_AUTO_*` 全开（light/L1/L2/L3/Judgments）；fallback 仍默认 `AIWIKI_NIGHTLY_FALLBACK_ENABLED=0`，需要 repo 外凭据和显式开关；不删除 dogfood vault receipt/data，不自动 commit/push。Product Shell：`.obsidian/plugins/furnace-product-shell/styles.css` 将 composer `max-width` 从 48rem 扩到 `min(96rem, calc(100vw - 48px))`，保持 `rows=1`、auto-resize 300、低矮 24px 起始高度；`bash build.sh` 已同步 `main.js`，并补 `tests/test_product_shell_universal_input.py` contract 断言避免回退。服务脚本：`scripts/install_user_service.sh` 默认只启用 `aiwiki-watch.service` 与 `aiwiki-nightly.timer`，dogfood maturity 仅在 `AIWIKI_INSTALL_DOGFOOD_MATURITY=1` 时安装；默认安装路径会清理遗留 maturity unit；`scripts/uninstall_user_service.sh` 新增 `--dogfood-maturity-only` / `--maturity-only`，只移除 maturity service/timer，保留 env files、vault 数据和 receipt。本机服务状态已更新：`~/.config/aiwiki/aiwiki-nightly.env` 五层 auto flags 均写为 `1`；`aiwiki-dogfood-maturity.timer` 已 disable/remove（`is-enabled` 为 not-found，`is-active` 为 inactive）；`aiwiki-nightly.timer` 仍 enabled，下一次 2026-05-20 00:00 CST；`aiwiki-watch.service` 仍 active。文档：README 与 Runtime Operations 说明默认路径为“等待投料 → 炼丹 → 产出 → 回馈 → 受控学习”，区分默认 watcher/nightly 服务、full furnace nightly profile、dogfood maturity validation harness；Architecture 和 A-Subsystem 分析同步为分层可开关模型；Slimdown Plan 标注 AOS-001..006 + P1 已完成，停止 broad AOS campaign，后续只做 targeted simplification / ops hardening。验证：shell syntax PASS；targeted service pytest 28 PASS；`bash scripts/verify.sh product-shell-static` PASS；`bash scripts/verify.sh scripts` PASS；本机 systemd cleanup/state check PASS。oracle qa-review 首轮唯一 blocker 是本条 PROGRESS 口径未更新，已修复；non-blocking：systemd 脚本测试仍偏文本断言，Product Shell 视觉仍需人工窄屏/宽屏确认，整个 dirty worktree 若一起提交建议再跑 full verify。

- 2026-05-19 (Product Shell compact composer + file-backed graph)：按用户反馈撤回上轮“放大输入框”的视觉方向，改为 ChatGPT-like 低矮但拉长的 composer，并清理 machine-memory 关系图谱为真实 Markdown 文件节点。本轮覆盖 `.codex/contracts/active.md` 为 `Product Shell composer and file-backed graph`，不改 backend/credentials/deploy/dogfood data，不扩 L3/Judgment automation，不删除 machine-memory data，不自动 commit。Product Shell：`.obsidian/plugins/furnace-product-shell/src/render_input.js` 将 textarea 默认 `rows=1`、auto-resize 上限 300；`styles.css` 将输入框恢复单行低高度、48rem 居中宽度、24px 圆角和轻阴影，底部 wrapper 去掉笨重大底座；`bash build.sh` 已同步 `main.js`。图谱：`build_machine_memory_graph(memory, *, root=None)` 现在只保留能解析到现有 `.md` 的 source/concept/judgment 节点，节点带 `page_path`，所有边过滤为两端均 retained；compile/report-subgraph 调用传入 root；symlink root 归一化修复；`wiki/indexes/graph-view.md` 模板文案同步说明“只展示真实 Markdown 页面支撑的关系”。测试新增 graph filtering regression，更新 report-subgraph fixture 以符合真实 md 节点规则，并刷新 acceptance replay prompt hash（graph digest 合法联动）。本机 user systemd 只读状态：`aiwiki-watch.service` enabled + active/running（自 2026-05-03 起，命令含 `watch --interval 5 --compile-limit 5 --deterministic-only`）；`aiwiki-nightly.timer` enabled + active/waiting（下一次 2026-05-20 00:00 CST）；`aiwiki-dogfood-maturity.timer` enabled + active/waiting（下一次 2026-05-20 08:15 CST）。默认自动化口径：watch 默认 deterministic compile/lint，不 inline 阻塞跑 LLM；nightly auto-apply / auto-adopt L1/L2/L3/Judgments 默认全关；dogfood maturity timer 只写成熟度 receipt，因此当前是 guarded automation / unattended maintenance，不是默认全自动自治。验证：focused graph/report-subgraph 11 PASS；acceptance replay focused 2 PASS；`bash scripts/verify.sh` PASS（2254 unit + 17 acceptance + 92% cov）；oracle qa-review APPROVE，无 blocker。non-blocking：ask anchor 仍基于 raw memory，手动删除页面后新报告 anchor 可能被过滤图谱判 unknown；HTML 顶部统计仍基于 raw memory/health，可能大于过滤后图本体。

- 2026-05-19 (verify-auto harness planner sync)：继续按用户要求解决 targeted-first 的残余风险：先在 `/home/tim/open-harness` 落地 deterministic verify-auto，再同步到 `/home/tim/ai-wiki`。上游 `closed_loop.sh` 现默认 `HARNESS_VERIFY_TARGET=auto`，调用 local scaffold `scripts/resolve_verify_targets.sh` 按 git touched paths 选择 targets，缺 resolver / 空输出时 fallback `scripts`，显式 `--verify-target <target>` / `--verify-full` / `HARNESS_VERIFY_TARGET=all` 仍保留；`deploy_gate.sh` 继续 full verify 默认。ai-wiki 已刷新 local scaffold，并新增项目自有 `scripts/verify_target_rules.sh`：协议/文档走 `scripts`，Python runtime / tests 走 `python-static` + `unit`，acceptance fixtures 走 `acceptance`，Product Shell 走 `product-shell-static`。`scripts/verify.sh` 现支持 `scripts`、`python-static`、`unit`、`acceptance`、`cli-smoke`、`product-shell-static`、`all/full` 单 target，供 closed_loop 逐个调度。当前工作区已有 Product Shell / runtime / tests / fixture unrelated dirty files；auto resolver 会按这些真实 dirty paths 选择较重 targets，因此 harness-only 验证可显式 `--verify-target scripts`，不把 unrelated worktree 误当本轮必须全量跑。验证：`bash scripts/verify.sh scripts` PASS；`bash scripts/verify.sh python-static` PASS；`bash scripts/verify.sh product-shell-static` PASS；`bash scripts/resolve_verify_targets.sh` 输出当前 dirty worktree 的真实选择为 `scripts`、`product-shell-static`、`python-static`、`unit`、`acceptance`；`HARNESS_DIR=.codex bash scripts/closed_loop.sh --require-contract --verify-target scripts` PASS，确认 scoped harness 收口不会触发 coverage / acceptance；qa-review same-context PASS，qa-runtime not-required。

- 2026-05-19 (targeted-first harness verification sync)：按用户要求先在 `/home/tim/open-harness` 落地 targeted-first 闭环，再同步到 `/home/tim/ai-wiki`。上游 `closed_loop.sh` 现默认执行轻量 `scripts/verify.sh scripts`，只有显式 `--verify-full` / `HARNESS_VERIFY_TARGET=all` 才跑全量；`run_plan.sh` / `finalize_task.sh` 可透传 verify target；`deploy_gate.sh` 保持 full verify 默认，不削弱发布门禁。已执行 `bash scripts/setup_local_harness.sh --apply --tier standard --platforms claude,codex,opencode` 刷新本地 scaffold，根 `AGENTS.md` / `CLAUDE.md` 补充 targeted-first 规则；项目 `scripts/verify.sh` 新增 `scripts` 目标，只做 shell syntax check，不触发 ruff / compileall / coverage / acceptance。当前工作区已有多项 unrelated runtime/UI/test 改动，未触碰也未回滚。验证：`bash scripts/verify.sh scripts` PASS；`HARNESS_DIR=.codex bash scripts/closed_loop.sh --require-contract` PASS，输出确认 `running scripts/verify.sh target=scripts` 和 `verify passed (target=scripts)`，未跑 coverage / acceptance；qa-review same-context PASS，qa-runtime not-required。默认本地闭环不再主动全量跑测试；全量验证只用于 runtime 行为变更、跨模块高风险、发布 / 推送前或 contract 明确要求。

- 2026-05-19 (open-harness local scaffold + multi-platform protocol sync)：按用户要求将 `/home/tim/ai-wiki` 的 local-only harness 同步到最新 `/home/tim/open-harness`，覆盖 `.codex/contracts/active.md` 为本轮 `ai-wiki harness sync` contract；不改 runtime/CLI/tests/Product Shell/backend/凭据/部署，不提交 generic scaffold。执行 `bash scripts/setup_local_harness.sh --apply --tier standard --platforms claude,codex,opencode`，刷新 `.claude/`、`.codex/`、`.opencode/` standard-tier skills/runbooks、contracts/plans/gates templates、`review-capabilities.env`、`CALIBRATION.md`、shared scripts/libs/platform registry，并生成 `.open-harness.conf`（`HARNESS_TIER="standard"`、`HARNESS_PLATFORMS="claude,codex,opencode"`）；`bash scripts/setup_local_harness.sh --status` 显示 `exclude_block=present`、`local_scaffold=present`，scaffold 继续通过 `.git/info/exclude` 保持 local-only。手工合并 `AGENTS.md` / `CLAUDE.md`，新增多平台 harness 规则、显式 `HARNESS_DIR` 用法、`.open-harness.conf` SoT、`/office_hours` / `/planner` / `/plan_review` / `/run_plan` 流程、local scaffold bootstrap 命令，以及“不主动 commit/push，除非用户明确要求”的沟通规则，同时保留炼丹炉项目事实和 `.codex` primary contract 约定。验证：`bash scripts/verify.sh` PASS（2253 unit tests、17 acceptance、coverage 92%）；qa-review same-context fallback PASS（isolated reviewer 未配置，fallback reason 已写入 `.codex/gates/qa-review.md` / `.codex/CALIBRATION.md`，无 blocking findings）；qa-runtime 标记 `not-required`，原因是本轮仅 protocol/scaffold 同步且 full verify 已通过。未自动 commit。

- 2026-05-19 (Product Shell chat quality + LLM profile unification)：按用户反馈继续修复“引用报告：output/reports/你好-今天天气如何-note.md 那好吧，你的llm是什么模型？”仍进入无实质内容/模板化产物的问题，并同步处理 Shell/CLI LLM 配置、对话耗时、输入框尺寸与自动化能力评估。本轮覆盖 `.codex/contracts/active.md` 为 `Product Shell chat answer quality, LLM config unification, latency/UI review, and automation assessment`；不改外部凭据/服务，不伪造 timeout 成功，不扩 L3/Judgment auto-adopt，不自动 commit。根因确认：Product Shell 的 `引用报告：<path>` 前缀被 `inferAutoAskFormat()` 中的“报告”关键词误判为 report intent，导致短身份追问走 `run-ask-submit --format report --lean --fallback-to-ask` 后台报告路径；真实 dogfood artifact 为 `delivery_mode=deterministic-fallback`、`llm_status=timeout_or_unavailable`、`llm_backend=codex-cli`、`llm_model=gpt-5.5`，正文是诚实 degraded notice 而非最终回答。修复：Product Shell intent detection 先剥离 `引用报告：<path>` 前缀，再判断真实用户文本；同一类短追问改走 `note + --direct` 快路径，显式“生成研究报告/深度报告”等仍保持 report；`run_ask` direct answer 增加模板/空答案校验，拒绝 `_LLM:`、deterministic note prompt、误返回的 `generated_by: aiwiki-ask` frontmatter，并触发 backend fallback 或 degraded note，避免模板被写成成功答案。LLM 配置统一：`LLMConfig.from_env()` 对 canonical interactive profile 默认使用 `opencode-api/deepseek-v4-pro` 主路由 + `codex-cli/gpt-5.5` backend fallback，显式 `AIWIKI_BACKEND_FALLBACK=` 可关闭；`.envrc.dogfood` 和 `README.md` 已同步说明 Shell/CLI 共享 `AIWIKI_LLM_*` / `AIWIKI_BACKEND_FALLBACK*`。响应耗时结论：慢点主要来自 report/background 路径的 deterministic baseline（manifest/compile/memory/ranking/prompt）、材料摘录、LLM 网络/CLI 调用与 fallback 重试；本轮把短引用追问改为 direct note，并保留 run notes/receipt 中的 `duration_ms`、fallback stage 与 degraded 边界。Product Shell 输入框已放大拉长：textarea 默认 3 行、80px-400px、字体更大，主视图底部 composer max-width=100%，source 与 `main.js` 已用 `bash build.sh` 同步。自动化能力评估：当前是 `guarded pass`，维护/治理自动化可无人值守，Judgment/L3 仍应保持 guarded copilot；下一步应提升可靠性、安静度、7/14 天 proof 和 deterministic/reversible routine automation，不扩大 self-edit/autonomy 半径。验证：focused Python `tests/test_runner.py tests/test_config.py tests/test_llm.py tests/test_app_shell.py -q` PASS（143 tests）；Product Shell source/bundle/tests `node --check` PASS；full `bash scripts/verify.sh` PASS（2253 unit + 17 acceptance + 92% cov）。

- 2026-05-19 (dogfood maturity + Today/report quality de-noise)：按用户下一步要求覆盖 `.codex/contracts/active.md` 为 `Dogfood maturity and Product Shell Today de-noise`，目标是把 `operational_maturity` 从 `not-yet` 打到 `pass`，同时收敛 Product Shell / `today` 首屏和修复 degraded/template 报告误曝光。本轮未降低 maturity gate limits/thresholds/budget，未改 backend/model/timeout/schema，未改 L3 apply prompt mutation 语义。`execution/l3_proposals.py` 新增 automatic L3 issue key，将同一 `prompt_proposal:prompts/ask.md:<pattern>` 的 concrete planner records 归并为一个可行动 issue，并让已有 rejected/reverted 同类 metadata proposal 覆盖后续重复 noise；`dogfood_maturity_gate.py` 同步把 preview raw count 与 effective issue count 分开，并将 `operational_maturity` 判定改为连续 receipt + receipt integrity + anomaly budget，不再被旧 top-level trend summary 的 backlog/semantic-path 条件误挡。真实 dogfood：`collect --preview-limit 1000` 从 `preview_raw_candidate_count=736`/历史 `effective=87` 收敛为 `preview_candidate_count=2`、`duplicate_existing_count=2`、`effective_preview_candidate_count=0`；随后 `run --preview-limit 1000 --l3-limit 1000 --deterministic-only` 写入 pass receipt `output/control/maturity-gate/run-20260519T022903Z.json`，不生成/应用 L3；`summarize --days 3` 显示 `operational_maturity.status=pass`、`budget_violations=[]`、`human_only_exceptions=true`、`l3_effective_candidate_count=0`、`routine_primary_debt_count=0`，但顶层 trend `status=warn` 仍如实保留（3 日 backlog +14、judgment receipt delta 0）。Today/Product Shell 收敛：primary 不再展示 automation/metric trend action，degraded/pending/template/`_LLM:` artifact 不进入 `todays_reports`；dogfood `today --json` 当前 `todays_reports=1`（仅 `hello-note.md`），`automation_status=0`、`l3_proposals=0`、`suggested_next_actions=0`，待拍板 review exception 保留。`collect_recent_output_artifacts()` 增加 `artifact_quality` / `contains_llm_placeholder` 元数据；Product Shell source 与 `main.js` 过滤 degraded/placeholder reports，并让 `quoteFileToComposer()` 对同一 `引用报告：<path>` 幂等，避免重复引用前缀。`run_ask` degraded 文案从 PDF-only 改为通用“材料投喂、引用解析或上下文准备已完成”。排查真实 vault 发现历史同类产物仍存在（`_LLM:` placeholder 42 个，degraded/timeout 线索 52 个），本轮不重写历史文件，只阻止它们继续作为普通今日报告曝光。验证：focused Python `tests/test_l3_proposals.py tests/test_dogfood_maturity_gate.py tests/test_today_feed.py tests/test_app_shell.py tests/test_runner.py` PASS（191 tests）；Product Shell `bash build.sh` 已重建 bundle，`node --check` 覆盖 source/tests/bundle PASS；JS Jest 因既有 `package-lock.json` 与 `package.json` 不同步且本地无 `jest` 未执行；full `bash scripts/verify.sh` PASS（2251 unit + 17 acceptance + 92% cov）。不自动 commit。

- 2026-05-19 (AOS-006 signals adapter targeted slimming)：按用户“继续解决后续肥阻塞、采用最优解方案”继续 slimdown，选择不盲拆 `memory/execution_surfaces.py` 大 hub，而是执行低风险、高确定性的 `signals/adapters.py` targeted slimming。已覆盖 `.codex/plans/active.md` 为 `Engineering Plan — AOS-006 signals adapter targeted slimming`，并物化 `.codex/contracts/active.md` milestone `AOS-006-SIGNALS-ADAPTER-SLIM`。本轮删除无 repo-local production/test caller 的 private `_iter_elixir_dependency_break_seeds()`，保留 collector archive owner path；新增 `ElixirDependencyBreakItem` 与 `normalize_elixir_dependency_break_item()`，将 `dependent_elixir_id` / `break_reason` 判定收敛为单一窄 helper，`_elixir_dependency_break_to_signals()` 与 collector invalid reason 复用同一规则。未改 signal schema、kind、severity、dedupe key、CLI、planner、backend routing 或 maturity gate。新增 focused tests 覆盖 mixed dependency_breaks 顺序稳定、batch duplicate dedupe、非 elixir subject ignored、缺/空 dependency break bundle 不 invalid、invalid reason 字符串稳定。验证：`tests/test_signals_collector.py` PASS（101 tests）；`tests/test_planner_log.py tests/test_cli.py -k elixir_dependency_break` PASS（4 tests）；full `bash scripts/verify.sh` PASS（2245 unit + 17 acceptance + 92% cov）。oracle qa-review PASS，无 blocker；`.codex/gates/qa-review.md` 与 `.codex/gates/qa-runtime.md` 已刷新；`HARNESS_DIR=.codex bash scripts/closed_loop.sh --require-contract` PASS；`HARNESS_DIR=.codex bash scripts/run_plan.sh --plan-file .codex/plans/active.md` 已将 milestone 标记为 done，当前 plan 无剩余 milestone。non-blocking hardening：后续可单独补 `dependency_breaks` 非 list 的 invalid regression test。不自动 commit。

- 2026-05-19 (P1 dogfood compounding proof)：按用户要求执行 P1，并将 plan 自动执行为单 milestone `P1-DOGFOOD-COMPOUNDING`。本轮覆盖 `.codex/plans/active.md` 为 `Engineering Plan — P1 dogfood compounding proof`，并物化 `.codex/contracts/active.md`；目标是补齐 AOS-004 缺失的真实 `trace_provenance_backed_compounding_sample`，不是降低 gate、伪造 receipt、删除 backend、改 backend routing/config/schema/CLI、扩 L3/Judgment/Product Shell。实现最小 runtime 修复：`ask_question()` 基于本次 ranked source ids 与 machine memory `source_to_judgment` 边，只收真实存在的 `wiki/judgments/<page_id>.md`，稳定去重最多 4 条，并把 runtime-owned curated refs merge 到 output frontmatter `source_files`；`run-ask` 覆盖后继续只恢复 deterministic artifact provenance，丢弃 LLM forged `derived_from/source_files`。真实 dogfood 执行中 `opencode-api/deepseek-v4-pro` 出现超时/格式失败，均未计入 pass；随后显式手动选择已 probe compatible 的 `codex-cli/gpt-5.5`，成功生成 `output/reports/基于已有-investing-dogfood-judgment-和-settled-elixir-复盘炼丹炉投资研究闭环有哪些已验证复利-哪些仍-4.md`，artifact frontmatter `source_files` 含 4 条 `wiki/judgments/*`，execution receipt `output/control/execution-receipts/run-ask-investing-dogfood-judgment-settled-elixir-4.json` 为 `operation=run-ask` / `status=success` 且 `target_file` / `primary_path` 精确匹配。`scripts/dogfood_maturity_gate.py collect --root /home/tim/danlu/炼丹炉 --write` 写入 `output/control/maturity-gate/snapshot-20260518T220253Z.json`，`knowledge_compounding_proof.status=pass`、`compounding_sample != null`，sample 指向上述 artifact/receipt。Focused provenance tests PASS（4 tests），maturity gate tests PASS（21 tests），full `bash scripts/verify.sh` PASS（2241 unit + 17 acceptance + 92% cov）。oracle qa-review PASS；non-blocking hardening：未来可补 `limit=4`/去重/缺失 judgment 文件跳过等单测，且 `source_files` 语义已扩大到 curated judgment refs。下一步：写 gate artifacts、closed_loop/run_plan 收口；不自动 commit。

- 2026-05-19 (AOS-005-followup backend/subsystem audit report)：按 active contract `Backend statistics and subsystem risk audit report` 完成 report-only 阶段，不删除 backend、不改 config schema/CLI options、不拆子系统。新增 `docs/analysis/E-AOS-005-Followup-Backend-Subsystem-Audit.md`：repo-local `.aiwiki/logs/llm-receipts.jsonl` 与 `.aiwiki/logs/runs.jsonl` 只有 1 条真实 backend/model 样本（`nvidia-nim-api` / `moonshotai/kimi-k2.5` / `run-ask`），acceptance fixtures 只证明 `codex-cli` / `stub-model` 成功/失败路径兼容，不能作为生产使用率证据；结论是 AOS-005b 只能先做真实 dogfood backend usage telemetry + deprecation/compatibility 评估，不能删除 backend。子系统审计：`memory/execution_surfaces.py` 1329 行、12 个 public renderer/audit 函数，最大函数 `render_concept_quality` 约 170 行，其次 `reconcile_concept_rewrite_proposals` 约 163 行、`collect_execution_consistency_signals` 约 156 行，适合后续 targeted helper extraction，不适合盲拆；`signals/adapters.py` 518 行，`SUPPORTED_SOURCES=(runtime_history,llm_receipt,archive)` 被 collector 覆盖，主 adapter 有测试，但 `_iter_elixir_dependency_break_seeds` 当前无 production/test caller 证据，应在后续 contract 中补证明或删除，不在本轮动代码。下一步：最终 full verify、qa-review/closed_loop，并在所有 harness milestone done 后按用户要求提交。

- 2026-05-19 (AOS-005a surface ownership consolidation)：按用户要求将四份 `docs/analysis/A-D` 通过 harness 流程继续推进。先用 `run_plan.sh` 收口 AOS-004 proof closure，并将 active contract 推进到 `AOS-005a Surface ownership consolidation and facade slimdown`。本轮 AOS-005a 执行受控削薄而非强删 facade：`app_surfaces.py` 抽出 `_compile_state_string_list`、`_compile_phase_lines`、`_source_link_lines`、`_concept_link_lines` 四个 helper，降低 `render_compile_status` review 复杂度，并把该文件的 `app_content` / `app_memory` facade imports 改为直接 owner imports；`app_memory_surfaces.py` 在 `src/aiwiki` 内的直接 runtime import 已清零，但因 public/test patch seam 仍保留 facade；`app_content.py` 已迁移一批 owner 明确的低风险 runtime imports，但 app shell / compile / lint legacy 面与 `content.*` 内部 patch seam 仍真实存在，因此本轮不退役；`app.py` 未触碰，继续作为 external compatibility shim。Focused AOS-005a 回归：`PYTHONPATH=src python -m pytest tests/test_app_misc.py tests/test_app_runtime.py tests/test_app_shell.py tests/test_runner.py tests/test_run_compile_fail_fast.py tests/test_app_compile.py tests/test_compile_artifact_atomic.py tests/test_compile_log_determinism.py -q` PASS（291 tests）。下一步：full `bash scripts/verify.sh`、qa-review/closed_loop gate，然后推进 AOS-005-followup report-only 阶段。

- 2026-05-18 (AOS-004 proof closure: runtime provenance + authoritative execution receipts)：按用户“review 四份分析文档，有问题就修订，没问题按优先级推进”执行；四份 `docs/analysis/A-D` 经 oracle 独立 review 后修正事实口径：AOS-004 当前 gate 不读 `trace.provenance.wiki_refs` / `parent_receipt_id`，而是要求 output frontmatter `derived_from/source_files` 引用 `wiki/judgments/` / `wiki/decisions/` / `wiki/elixirs/` 并有同 path 的成功 execution receipt；AOS-005 不能越过 active contract，`app.py` 是 README 承诺保留的 `aiwiki.app` compatibility shim，不是 delete-now 候选；LoC 口径修为全 runtime `65,518` 行、顶层 `24,289` 行、核心 loop 子系统 `24,506` 行。随后按最高优先级推进 AOS-004 proof/closure：新增 `src/aiwiki/execution/receipts.py::write_execution_receipt()`，将 action-level receipt 写入 `output/control/execution-receipts/*.json` 并 append `.aiwiki/state/execution-receipts.jsonl` / universal audit，且防止 `extra` 覆盖核心字段、history/audit 失败时清理 JSON receipt；`run_ask` 成功路径把 `derived_from/source_files` 作为 runtime-owned provenance，只从 deterministic artifact 回填并丢弃 LLM 注入 provenance，同时写 `operation=run-ask`、`target_file=primary_path=<output artifact>` 的 authoritative execution receipt，submit 阶段不写 success receipt，resume 完成后才写；`file_back` 成功路径写 `operation=file-back` receipt，`target_file/primary_path` 指向原 output artifact，wiki 目标放 `secondary_path`。补齐 rollback：`run_ask` receipt/provenance/候选/graph anchor/background 完成任一步失败会恢复 artifact；`file_back` receipt 失败会恢复 destination、`.aiwiki/state/output-candidates.json` 与 `wiki/indexes/log.md`。新增/更新测试覆盖 run_ask provenance 保留、LLM forged provenance 丢弃、receipt failure rollback、run_ask submit/resume receipt 时序、file_back receipt 与 rollback、acceptance golden 的 execution receipt/audit 事件，以及 alchemy receipt history line-number 脆弱断言修复。oracle 首轮 review 抓出 forged provenance 与非事务 receipt 两个 blocker，修复后二轮 `APPROVE`；残留风险：direct/simple run-ask path 未扩展；`file_back` 未回滚 `compile_wiki(root)` 可能产生的全部副作用，仅回滚主要 mutation surface；gate 仍兼容 legacy empty status 为 success，后续可硬化。验证：focused blocker tests PASS（5 tests）；相关回归 `tests/test_runner.py tests/test_app_misc.py tests/test_dogfood_maturity_gate.py` PASS（140 tests）；`bash scripts/verify.sh` PASS（2239 unit + 17 acceptance + 92% cov）。本轮未 commit/push。

- 2026-05-18 (Report true background jobs + degraded recovery boundary)：按“剩余边界最优解”把 report 从 UI long-running 标签升级为 repo-truth 两段式后台任务。新增 `run-ask-submit` / `run-ask-resume` 与 `.aiwiki/state/background-jobs/<job_id>.json` manifest：submit 只生成 deterministic artifact、run notes、job manifest 并 spawn background resume；resume 复用 manifest 中的 artifact/path/run_id，不重新 `ask_question()`。submit artifact frontmatter 标记 `background_job_id`、`background_status=submitted|running`、`delivery_mode=background-pending`、`llm_status=pending`；`collect_recent_output_artifacts()` 过滤 pending/running placeholder，避免 Product Shell 过早 reconcile 成 done；resume 成功后写 `background_status=completed`，degraded fallback 写 `background_status=degraded` + `delivery_mode=deterministic-fallback` + `llm_status=timeout_or_unavailable`。Product Shell report 默认走 `run-ask-submit`，pending poller 按 repo summary 轮询，reconcile 优先 `run_id`，并在 degraded retry 时清空旧 job/run/run-notes 后写回新 job metadata，防止重命中旧 artifact。shell summary 暴露静态 `backend_fallbacks` readiness，submit 时做 targeted backend preflight，不在 shell-status 每次探测；Product Shell Advanced/System status 已显式展示备用 LLM readiness。run-ask prompt 侧已剥离 `run_id/run_notes_path/background_* / delivery_mode / llm_*` 控制 frontmatter，避免后台/降级元数据污染 LLM prompt。新增/更新 Python 与 Product Shell contract/Jest source tests；bundle 已 rebuild。验证：focused tests PASS；`bash scripts/verify.sh` PASS（2234 unit + 17 acceptance + 92% cov）；oracle 复审先 block 两个边界（placeholder 早完成、retry 旧 run_id），修复后 `approve with notes`；最终 follow-up 复审 approve，无新 blocker。JS Jest 因本地缺 `jest`/`node_modules` 未能运行，已用 source/bundle syntax、Python contract tests 与 full verify 兜底。

- 2026-05-18 (LLM timeout interaction repair)：修复 Product Shell PDF 投料后 `run-ask` 超时却被展示为成功、报告落成空模板的问题。根因链路确认：PDF drop/extract 正常，但 `opencode-api + deepseek-v4-pro` 在默认 120s read timeout 内没有返回可用内容（receipt `raw_response_path=no_response`），Product Shell 又默认传 `--fallback-to-ask`，旧 `run_ask()` 将 deterministic fallback frontdoor 记为 `success`，导致 UI 显示完成而报告仅含 `_LLM:` 占位模板。本轮修复：Product Shell `run-ask` 默认追加 `--lean` 并缩小 ask prompt profile，减少长 PDF 上下文导致的慢路径；`run_ask()` fallback frontdoor 语义改为 `degraded`，生成的 artifact 改写为中文“LLM 未完成”说明并写入 `llm_status=timeout_or_unavailable` / `delivery_mode=deterministic-fallback` / backend/model/failure reason；Product Shell timeline/notice/LLM health 对 deterministic fallback 显示 `LLM timeout/degraded`，不再伪装 success；报告标题和文件名通过 `human_query_title()` 提取用户问题，去掉自动 prompt、raw path、timestamp/hash 泄露；普通用户首屏隐藏“炉子动态”，仅 Advanced 显示。验证：focused Python `tests.test_runner tests.test_decision_grade_report tests.test_app_shell` PASS（118 tests）；acceptance `bash scripts/run_acceptance.sh` PASS（17 tests，已刷新 prompt replay hash fixtures）；`bash scripts/verify.sh` PASS（2209 unit + 17 acceptance + 92% cov）；JS `node --check` PASS；Product Shell Jest 因本地 `package-lock.json` 与 `package.json` 版本不一致、无 `node_modules` 未执行，已用 bundle/source syntax + Python/acceptance/full verify 兜底。真实 vault Product Shell `main.js` 与 repo bundle 是同一文件/同一 inode，`shell-status` smoke PASS。

- 2026-05-18 (Agent OS slimdown AOS-004 Knowledge compounding proof gate)：按 AOS-004 将“知识复利”从产品叙事落成可复算 proof gate。`scripts/dogfood_maturity_gate.py` 新增 `knowledge_compounding_proof` snapshot/report：从 `.aiwiki/state/manifest.json` + `wiki/sources/*.md` 复算 `raw_to_wiki_count`，复用 `aiwiki.metrics_io`/`compute_metrics` 计算 `output_file_back_rate` 与 `elixir_reuse_count`，扫描 `output/**/*.md` frontmatter 的 `derived_from/source_files` 识别 judgment/decision/elixir 复用，并用去重后的 `output/control` receipts 与 `.aiwiki/state/execution-receipts.jsonl` 精确匹配 output path；只有匹配到同一 artifact、且 operation 属于 `ask` / `run-ask` / `file-back` 且非失败/回滚状态的 trace/provenance-backed sample 才 `pass`，否则输出 `not-yet` 和 `missing_evidence`，不把机制存在误判为复利发生。新增 tests 覆盖：无 sample 时 not-yet、fixture 中 JSON receipt + history duplicate 去重后 receipt-backed sample pass、revert receipt 不能作为成功 sample、旧 run receipt 缺 proof 时 summary 兼容并报告 not-yet。真实 dogfood vault 仅执行安全 `collect --write`，写入 `output/control/maturity-gate/snapshot-20260518T012658Z.json`：`raw_to_wiki_count=25`、`judgment_or_elixir_reuse_count=22`、`output_file_back_rate=0.2909`、`receipt_backed_actions=116`、`human_required_exception_count=16`，但 `compounding_sample=null`，因此 verdict=`not-yet`，缺口为 `trace_provenance_backed_compounding_sample`；未修改历史 receipt、未降低 maturity 阈值、未引入外部 backend/凭据。Focused `tests.test_dogfood_maturity_gate` PASS（21 tests）。下一步跑 full verify、qa-review、刷新 AOS-004 gates 并用 `run_plan.sh` 收口。

- 2026-05-18 (Agent OS slimdown AOS-003 private re-export retirement)：按 AOS-003 `Kernel shim retirement and hub slimming` 执行首轮削薄。审计结论：`app.py` 是 README 承诺保留的 `aiwiki.app` 外部兼容 shim，不能删；`app_content.py`、`app_compile.py`、`app_memory_surfaces.py` public/tested facade 仍被 runtime/tests/scripts 使用，不能整文件退役。经 oracle 复核，选择最小安全候选：退役 `aiwiki.app_memory_surfaces` 上 `_machine_memory_query_payload_hash` 与 `_route_anchor_candidates` 两个 private underscore query-helper re-export；真实 owner `aiwiki.app_memory_query` 入口保留，public/tested seam 如 `build_machine_memory_query_routes` 与 `render_machine_memory_graph_html` 保留。新增 `docs/Furnace AOS-003 Compat Shim Audit.md` 按 `delete-now` / `keep-stable` / `split-later` 记录证据与决策，README runtime map 同步说明 private query helpers 留在 owner module。新增回归测试确认 private facade import 预期失败、owner import 可用、public facade survivor 仍可导入。未触碰 `runner/alchemy.py`、`app_protocol.py`、`app_lifecycle.py`、`app_surfaces.py`，未改 CLI、Product Shell、receipt/audit/revert schema。验证：focused private re-export tests PASS；`tests.test_app_misc` PASS（55 tests）；full `bash scripts/verify.sh` PASS（2204 unit + 17 acceptance + 92% cov）。oracle qa-review `APPROVE`，无 blocker；唯一文案 nit 已折叠。下一步刷新 AOS-003 gate artifacts，并用 `run_plan.sh` 推进到 AOS-004。

- 2026-05-18 (Agent OS slimdown AOS-002 Product surface convergence)：按 AOS plan 第二个 milestone 收敛默认产品面，不删除 legacy/operator 命令、不新增 alias/命令/子命令/Product Shell 视图/按钮/状态字段。实现：`src/aiwiki/cli/parsers.py` 将顶层 CLI description 从旧 `knowledge compiler scaffold` 改为炼丹炉 local-first knowledge agent runtime，并把默认 help/usage 展示收敛到 `{drop,today,metrics,advanced}`；完整 legacy/operator 命令仍保持顶层 parseable 且完整保留在 `aiwiki advanced --help`。`README.md` 新增 CLI command taxonomy：`primary` = `drop`/`today`，`advanced` = `metrics`/`advanced ...`，`operator/internal` = `compile`、`run-nightly`、`planner-log-list`、`l3-proposal-generate` 等 legacy top-level commands；明确 AOS-002 只调整展示和文档，不删除旧命令、不新增 alias。Product Shell 代码未改，因当前默认命令注册已 advanced-gated，本轮无 UI-owned state、无隐式 backend routing、无 shell-summary/schema/runtime delta。新增 CLI 回归测试覆盖默认 help product-first、legacy top-level/backward-compatible parse choices，以及 `advanced --help` 仍暴露完整 operator surface。验证：focused `PYTHONPATH=src python3 -m unittest tests.test_cli` PASS（148 tests）；`bash scripts/run_acceptance.sh` PASS（17 acceptance）；full `bash scripts/verify.sh` PASS（2203 unit + 17 acceptance + 92% cov）。oracle qa-review `APPROVE`，无 blocker；唯一测试 nit 已折叠。下一步刷新 AOS-002 gate artifacts，并用 `run_plan.sh` 推进到 AOS-003。

- 2026-05-16 (Agent OS kernel slimming plan AOS-001)：用户要求按上一轮评估推荐路线执行四件事：冻结扩张、产品面收敛、削薄内核、证明复利；先生成 `docs/` plan，再多轮 review，review 通过后按 harness 自动闭环推进每个 milestone。本轮新增 `docs/Furnace Agent OS Slimdown Plan.md`，并同步 `.codex/plans/active.md` 为下一阶段执行计划：`AOS-001 Expansion freeze and plan handoff` → `AOS-002 Product surface convergence` → `AOS-003 Kernel shim retirement and hub slimming` → `AOS-004 Knowledge compounding proof gate`。核心 freeze ledger：不扩 L3 auto-adopt/self-editing、不扩 Judgment autonomy、不新增 Product Shell 交互/视图/按钮/状态字段、不新增 CLI 命令/alias/operator lane；唯一允许例外是 help/docs/grouping/排序/文案类收敛。AOS-003 明确不能以纯 audit 空转完成；若无安全 shim 可删，必须 stop + re-plan；首轮禁止触碰 `runner/alchemy.py`、`app_protocol.py`、`app_lifecycle.py`、`app_surfaces.py` 等大 hub。AOS-004 要求 count metrics 之外至少给出 1 条 trace/provenance-backed compounding sample。`HARNESS_DIR=.codex bash scripts/materialize_contract.sh --plan-file .codex/plans/active.md --contract-file /tmp/opencode/aos-001-contract.md --milestone AOS-001` 已验证 plan 可 materialize；`cmp -s .codex/plans/active.md docs/Furnace\ Agent\ OS\ Slimdown\ Plan.md` PASS。oracle 第一轮 review `REQUEST_CHANGES`，blockers 为 AOS-002 alias/Product Shell 扩面口子、AOS-003 允许 audit 空转、AOS-003 scope 触碰大 hub；已全部折叠。oracle 第二轮 review `APPROVE`，无 blocking findings，建议进入 `materialize + closed_loop`。当前 `.codex/contracts/active.md` 已物化为 `AOS-001`，本轮只做 plan handoff / freeze enforcement，不改 runtime 行为、不删 CLI、不改 prompt/maturity gate；下一步刷新 gate artifacts 并执行 harness closed-loop。

- 2026-05-16 (Product Shell artifact-first chat UX polish)：用户追问“炼丹炉 UI 参考 ChatGPT 是否合理且最优；若合理，继续推进到产品标准并结合炼丹炉特色”。判断结论：ChatGPT 式 UI 作为“对话入口 + 等待反馈 + 结果承接”的交互隐喻合理，但炼丹炉不是纯 chatbot；最优方向不是复制 ChatGPT，而是 `artifact-first agent chat`：聊天气泡承接过程，本地报告/回执/provenance 才是事实交付。本轮新物化 `.codex/contracts/active.md`，只做 Product Shell UI polish，不改 CLI pipeline / backend thread memory / token streaming / `prompts/ask.md` / maturity gate。实现：`render_today.js` 将 done AI 气泡升级为 `.furnace-artifact-card`，展示 artifact kind、标题、路径、snippet fallback/动态 snippet、meta chips，并把“打开报告 / 查看回执 / 引用此报告追问 / 完成”动作沉到卡片下方；running/received 增加 `.furnace-progress-steps` 安全阶段文案；failed/escalated 改为 inline exception card，突出阻断原因与重试/Exception Queue。`plugin.js` 新增 `readWorkspaceSnippet(relativePath, length)`，仅在 repo root 内读取文件、剥 frontmatter/标题、截断后临时展示，不写入 UI SoT；新增 `quoteFileToComposer(relativePath)`，把 `引用报告：<path>` 显式注入 composer，用文件引用替代隐式多轮 memory。`styles.css` 新增 artifact/snippet/meta/progress/exception 样式；`main.js` 已 rebuild（9031 lines）。测试：focused Jest `universal-input-interaction.test.js` 9 PASS；Product Shell full `bash build.sh && npm test` 94 PASS；Python static `tests.test_product_shell_pending_card` 37 PASS；`git diff --check` clean；full `bash scripts/verify.sh` PASS（2200 unit + 17 acceptance + 92% cov）。designer QA `ses_1d6472917ffesU43GdN56U0xhq` `APPROVE`：认为该方案命中 ChatGPT 交互隐喻，同时保留炼丹炉 local-first file SoT、receipt/provenance 与 Exception Queue 特色；无 hidden CoT、无 UI-owned SoT、无绝对路径越界读取。Residual：仍无 true token streaming；“追问”是显式文件引用，不是隐式 thread memory；长报告阅读仍交给 Obsidian 文件视图。

- 2026-05-15 (TDA-005 Human-required metric and maturity gate extension)：按 TDA plan 最后一轮推进“首屏 human-required / routine debt 指标化”，把 TDA-001 的 exception-only Today 分桶与 TDA-002 的 auto-resolution receipts 纳入 maturity 可复算指标。本轮物化 `.codex/contracts/active.md` milestone `TDA-005`，在 `scripts/dogfood_maturity_gate.py` 新增 `human_required_report`：从 machine-memory action state、`.aiwiki/state/execution-receipts.jsonl` 与 `output/control/execution-receipts/auto-resolution/*.json` 复算 `human_required_count`、`auto_resolved_count`、auto-resolution receipt/history operation counts 与 human-required reason counts；同时用 `build_today_feed(..., audience="primary")` 观察真实首屏 review entries，并通过 `src/aiwiki/today_feed.py::primary_review_bucket_keys()` / 新增 `routine_review_bucket_keys()` 区分真异常与 routine denylist，计算 `primary_exception_count`、`routine_primary_debt_count`、`exception_count`。QA 首轮指出 `routine_primary_debt_count` 不能用 primary allowlist 自证正确，否则 routine bucket 被误加回首屏时会被算成 exception；已修为独立 routine denylist 检测，并新增回归测试临时把 `overdue_actions` 加回 `_PRIMARY_REVIEW_BUCKETS`，断言 `routine_primary_debt_count=5`、`primary_exception_count` 仍只算 `pending_judgments`。`collect_metrics()` 现在把该报告写入 snapshot；`summarize_run_receipts()` 暴露 top-level `human_required_count` / `routine_primary_debt_count` / `exception_count` / `auto_resolved_count`；`operational_maturity` 只新增 anomaly budget `max_routine_primary_debt_count=0`，不降低既有 failed runs、L3、judgment exception/failure 阈值，也不改变历史三天 receipt verdict 语义。dogfood 只读 collect：`human_required_count=16`、`primary_exception_count=1`（`pending_judgments`）、`routine_primary_debt_count=0`、`exception_count=17`、`auto_resolved_count=0`、`auto_resolution_receipt_count=16`、`auto_resolution_operation_counts={escalate:16}`，`prompts/ask.md` sha256 仍为 `6f66cdf888eeafb51f2e74a7116e6933dd1ac4dbed0f1b4f7bb8707178369d91`。dogfood `summarize --days 3` 因 Day1-Day3 历史 receipts 早于 TDA-005、缺 `human_required_report`，新增 top-level counts 为 0/empty，`operational_maturity.status` 仍 PASS 且新预算无 violation；这保持 historical verdict 不被 retroactively 改写，未来新 run receipt 会携带该指标。验证：focused ruff PASS；`tests.test_today_feed tests.test_dogfood_maturity_gate` 69 tests PASS；`bash scripts/verify.sh` PASS（2199 unit + 17 acceptance + 92% cov）。QA：oracle `ses_1d678832fffeBFXnADFQ4hgyYI` 首轮 `REQUEST_CHANGES` 后二轮 `APPROVE`，确认指标能抓到 routine debt 回流首屏的 policy 漂移。全 TDA plan 的 TDA-001~TDA-005 已实现，下一步只剩 gate/closed-loop 收口与下一个方向评估；本轮未改 `prompts/ask.md`，未放宽 maturity gate 阈值，未把 human-required / escalated debt 伪装成 resolved。

- 2026-05-15 (TDA-004 Safe run notes artifact for visible reasoning progress)：按 TDA plan 第四轮推进“可见进度笔记”，把 Chat-style Product Shell 的进度反馈从纯 UI fake stage 升级为 runtime 可追溯的安全 control artifact。本轮物化 `.codex/contracts/active.md` milestone `TDA-004`，新增 `src/aiwiki/execution/run_notes.py` 与 `src/aiwiki/app_state.py::run_notes_path()`，将每次 ask/run-ask 的外部化阶段笔记写入 `output/control/runs/<run_id>/thinking.md`，并在 output artifact frontmatter、runtime history、shell `recent_outputs` / `recent_runs` 中暴露 `run_id` 与 `run_notes_path`。`ask_question()` 写 `deterministic-ready` notes；`run_ask()` 在 LLM success 写 `llm-complete` notes，在 fallback-to-ask 写 `deterministic-fallback` notes；notes 只记录协议、格式、产物路径、source/concept 计数、coarse stages 与 receipt ref，明确不是 hidden model reasoning、internal instructions、backend payloads 或 wiki fact source。安全修复：`run_id_for_artifact()` 使用完整 artifact ref（如 `output/reports/...` vs `output/slides/...`）避免同 stem 覆盖同一 `thinking.md`；`_safe_note_text()` 对 vault root 与通用绝对路径做脱敏，防止问题文本中的 `/srv/...` 等本地路径持久化；`run_ask()` 构造 LLM prompt 前剥离 artifact frontmatter 中的 `run_id` / `run_notes_path`，避免 control metadata 改变 prompt hash 或被投喂给模型。Product Shell 只消费 runtime 返回/summary reconciliation 的 `runNotesPath` / `runId`，不自建 SoT；pending bubble 中新增 `<details>` “查看进度笔记”和“打开进度笔记”按钮，边界文案为“只包含外部化阶段记录，不包含模型内部过程”。测试覆盖：run notes frontmatter/文件生成、绝对路径脱敏、跨 output 目录 run_id 冲突、LLM prompt 不含 run notes metadata、shell recent outputs/runs 暴露 run fields、Product Shell serialization/reconcile/render。Acceptance `M6.1b case_happy_run_ask` golden 已按 intentional CLI payload 扩展刷新。QA：oracle `ses_1d678832fffeBFXnADFQ4hgyYI` 首轮指出 run_id collision 与路径脱敏 blocker，修复后二轮 `APPROVE`。验证：`py_compile` / `node --check` PASS；focused runner/app_shell tests 51 PASS；Product Shell Jest 94 PASS；`tests.test_product_shell_pending_card` 36 PASS；acceptance replay focused 2 PASS；`bash scripts/verify.sh` PASS（2196 unit + 17 acceptance + 92% cov）。本轮未改 `prompts/ask.md`，未改 maturity gate 阈值/逻辑，未保存 hidden CoT，progress notes 保持 `output/control/` control artifact，不进入 `wiki/` 事实层。下一步进入 `TDA-005`：human-required metric and maturity gate extension。

- 2026-05-15 (TDA-003 Chat-style Product Shell M-Chat.1 UI-only shell)：按 TDA plan 第三轮推进“首屏参考 ChatGPT 式对话框，但不暴露 hidden CoT、不新增 runtime SoT”。本轮物化 `.codex/contracts/active.md` milestone `TDA-003`，只改 Obsidian Product Shell UI：`render_home.js` 将 Universal Input 移到主视图底部作为 Conversation Composer；`render_input.js` 增加 `furnace-conversation-composer` 语义类并把提交中提示改为“进度会出现在上方对话流”；`render_today.js` 将 pending submissions 从旧卡片改为 `.furnace-conversation-stream`，每条由用户气泡 + AI 状态气泡组成，支持附件 pills、running/received skeleton shimmer、安全阶段文案（如“正在整理材料与上下文”“已接收，正在排队生成报告”），done 状态原地展示 result card 并保留“打开报告 / 查看回执 / 完成”，failed 提供重试，escalated 提供“打开异常队列”；`styles.css` 使用 shell-local `position: sticky; bottom: 0`，避免 global fixed 遮挡 Obsidian，并新增 bubble/result/shimmer 样式；`main.js` 已由 `bash build.sh` 重建。父会话修复了旧 Python contract tests 对 `furnace-pending-card` 的过期断言，改为锁定 chat-style DOM/classes；新增/更新 Jest 覆盖 composer 底部位置、running safe label、done/failed/escalated bubbles、Exception Queue 入口。验证：`tests.test_product_shell_pending_card` 35 tests PASS；focused Product Shell Jest 19 tests PASS；Product Shell full `bash build.sh && npm test` PASS（94 Jest tests，`main.js` 8852 lines）；`bash scripts/verify.sh` PASS（2192 unit + 17 acceptance + 92% cov）。designer QA `ses_1d6472917ffesU43GdN56U0xhq` 最终 `APPROVE`，确认无 hidden CoT、无 UI-owned runtime SoT、无 CLI/runtime pipeline 变更、无 global fixed 遮挡；唯一 non-blocking nit 是 done branch 的 actions 目前在 result card 上方，可后续 UI polish。下一步进入 `TDA-004`：safe run notes artifact for visible reasoning progress，把真实外部化进度笔记接入而不是展示模型原始思维链。

- 2026-05-15 (TDA-002 machine-memory action auto-resolution policy + receipts)：按 TDA plan 第二轮推进“常规 machine-memory action debt 不应停留在首屏人工 backlog；可安全自动处理的动作必须有 receipt/audit/revert，不安全的动作必须显式 exception 化”。本轮物化 `.codex/contracts/active.md` milestone `TDA-002`，实现保守 auto-resolution policy：只有 `add-source-concept-link` 与 `refresh-citation-snapshots` 在 `accepted` 且 policy allow/low-risk 时可自动 apply；`monitor-bridge-concept` / `split-overloaded-concept` / `expand-singleton-concept` / `connect-isolated-source` 等 review-first/manual/语义判断类不再被 `nightly_health()`、`apply-action --all-accepted-low-risk` 或新 `auto-resolve-actions` 误当低风险自动 apply。新增 CLI `auto-resolve-actions --dry-run|--accepted-only|--limit|--note`，dry-run 只输出 policy decision；actual 对 accepted low-risk 复用既有 dry-run bundle + apply receipt/revert path，对其他 active pending/review/manual actions 写 state-only escalation receipt 到 `output/control/execution-receipts/auto-resolution/<action>.json`，状态落 `deferred`，保留 `human_required=true`、`human_required_reason=semantic_judgment_required`、`revert_supported=false`、`human_recovery_path`、receipt history、audit mirror、runtime history 与 wiki log。幂等性：已 `deferred` 且带 `human_required_reason` 的 action 后续自动跳过，避免重复刷 receipt/audit；人工 `review-action --status accepted`、后续 apply/revert 会清理旧 exception metadata，避免污染真实 resolved/revert 语义。dogfood vault `/home/tim/danlu/炼丹炉` 先 dry-run：`evaluated=16`、`would_apply=0`、`would_escalate=16`；actual 执行后 `applied=0`、`escalated=16`、`failed=0`，16 个 review-first/manual machine-memory actions 全部明确 exception 化并各有 receipt；复跑 dry-run 为 `skipped=16`、`reason_code=already_human_required_exception`。post-actual `today --json` 首屏 `needs_review` 从 `review:escalated_actions` + `review:pending_judgments` 收敛为仅 `review:pending_judgments`（1 项），`L3 Proposals` 仍为空；`review-queue --bucket machine_memory_actions --json` 仍显示 16 项 `status=deferred`、`can_review=true`、`can_apply=false`，证明 debt 未被删除或伪 resolved，而是带原因进入 exception/review queue。验证：focused `tests/test_app_runtime.py tests/test_cli.py tests/test_execution_compat.py` 254 tests PASS；`tests.test_mm_action_batch_receipt_tx` 9 tests PASS；full `bash scripts/verify.sh` PASS（2192 unit + 17 acceptance + 92% cov）。qa-review 经 oracle `ses_1d678832fffeBFXnADFQ4hgyYI` 两轮 REQUEST_CHANGES 后修复 deferred churn、stale exception metadata、compat seam、unused import；剩余 nits（auto-apply 独立 runtime event、inactive historical metadata）为非阻塞 residual。本轮未改 `prompts/ask.md`，未改 maturity gate 阈值/逻辑，未自动 apply 高风险/manual/semantic actions。下一步进入 `TDA-003`：Chat-style Product Shell M-Chat.1 UI-only shell。

- 2026-05-15 (TDA-001 Today exception queue readiness)：按用户“首屏不应是 backlog dump；常规处理债应自动处理或降级，首屏只留真异常”的产品原则，覆盖 `.codex/plans/active.md` 为 `Engineering Plan — Today exception queue + conversation-first shell`，并物化 `.codex/contracts/active.md` milestone `TDA-001`。本轮只收敛首屏语义，不自动处理真实 dogfood vault 中的 upgraded/overdue actions，不新增 LLM policy，不实现 chat UI / `thinking.md`，不改 `prompts/ask.md`，不改 maturity gate 阈值/逻辑。实现：`src/aiwiki/today_feed.py` 与 `.obsidian/plugins/furnace-product-shell/src/today_feed.js` 的 primary review bucket policy 移除 routine `overdue_actions` / `overdue_reviews` / `ready_actions`，保留 `counter_evidence_candidates` / `escalated_actions` / `escalation_candidates` / `judgment_review_actions` / `pending_decisions` / `pending_judgments`；Product Shell activity timeline 同步只补真异常 review backlog，并尊重 `today_snooze`，避免 routine backlog 通过 timeline 回流首屏；`main.js` 已由 `bash build.sh` 重建。测试新增/更新覆盖 routine bucket 降级、exception bucket 保留、operator feed 仍可见 routine、Python/JS mirror 分桶一致性、timeline 不显示 routine/unknown/snoozed exception backlog。只读 dogfood `today --json` 验证：primary `needs_review` 不再含 overdue routine debt，仅剩 `review:escalated_actions`（3 项）与 `review:pending_judgments`（1 项）这类 exception queue；`L3 Proposals` 仍为空。验证：focused Python Today tests 64 tests PASS；focused Product Shell Jest 37 tests PASS；`bash scripts/verify.sh` PASS（2183 unit + 17 acceptance + 92% cov）。qa-review 由 oracle `ses_1d68f26d1ffeyfo0cUUmrn5EcE` APPROVE；其 nits 中 exception 保留测试与 timeline snooze 已折叠，未调用的 `renderNeedsDecisionSection()` full-backlog 文案作为非阻塞 residual。下一步进入 `TDA-002`：machine-memory action auto-resolution policy + receipts，真实处理 upgraded/overdue routine debt，并保留 receipt/audit/revert 或明确 exception reason。

- 2026-05-15 (dogfood maturity Day3 自动 receipt + 三天 PASS)：`aiwiki-dogfood-maturity.timer` 在 `2026-05-15 08:15 CST` 自动触发成功，`aiwiki-dogfood-maturity.service` exit 0，写入真实 Day3 receipt `/home/tim/danlu/炼丹炉/output/control/maturity-gate/run-20260515T001502Z.json`（`generated_at=2026-05-15T00:15:02Z`，`status=pass`）；timer 下一次计划为 `2026-05-16 08:15 CST`。随后执行 `PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py summarize --root /home/tim/danlu/炼丹炉 --days 3` 得到真实三天 maturity verdict：`status=pass`、`receipt_count=3`、`days=[2026-05-13, 2026-05-14, 2026-05-15]`、`consecutive_days=true`、`status_counts.pass=3`、`failed_runs=[]`、`prompt_hash_changed_runs=[]`、`semantic_path_observed=true`、`judgment_review_new_receipts=5`、`judgment_review_exception_rate=0.0`、`judgment_review_failure_rate=0.0`、`l3_candidate_delta=-259`、`l3_effective_candidate_count=0`、`l3_debt_report.state_counts={rejected:282,reverted:2}`、`l3_dedupe_or_noise_ratio=1.0`、`backlog_total_delta=-256`、`operational_maturity.status=pass`、`operational_maturity.reason="all gates and anomaly budget pass"`、`operational_maturity.human_only_exceptions=true`、`budget_violations=[]`。`prompts/ask.md` sha256 仍为 `6f66cdf888eeafb51f2e74a7116e6933dd1ac4dbed0f1b4f7bb8707178369d91`；本轮未 apply prompt proposal，未修改 `prompts/ask.md`，未修改 `scripts/dogfood_maturity_gate.py` 阈值/逻辑。Today 首屏 `L3 Proposals` 显示 `(no L3 proposals need attention)`，剩余人工关注为 3 个升级动作、3 个逾期动作，以及建议指标 `elixir_reuse_count ↑` / `output_file_back_rate ↓` / `proposal_acceptance_rate ↓`。结论：三天 unattended dogfood maturity 证据已真实 PASS；这不等于自动扩张 L3/Judgment 范围，下一步仍应优先处理剩余 review debt 或另起 contract。

- 2026-05-14 (L3 prompt proposal debt triage：阶段1+2 reject-only 收口)：用户授权先阶段1再阶段2清理历史 L3 prompt proposal backlog。本轮物化 `.codex/contracts/active.md` 为 `L3 proposal debt triage`，执行 vault 治理写操作但不改 runtime code、不改 maturity 阈值、不 apply prompt。基线：`prompts/ask.md` sha256=`6f66cdf888eeafb51f2e74a7116e6933dd1ac4dbed0f1b4f7bb8707178369d91`，已落盘 `prompt_proposal` candidate=259，live collect `effective_preview_candidate_count=21`，Day2 receipt summary 仍 warn/not-yet。阶段1：对 259 个历史 `prompt_proposal` candidate 全部执行 `review proposal <id> --status rejected --note "legacy L3 prompt proposal debt triage; reject-only; do not apply prompts/ask.md"`，保留 proposal/audit/provenance，不删除证据；验证 live `review proposals --kind prompt_proposal --state candidate --json` 返回 `[]`，`prompts/ask.md` hash 未变。阶段2：`l3-proposal-generate --apply --limit 1000` 只 materialize 未落盘 effective preview debt 21 个（其余 already_exists/not_eligible），随后全部执行 `review proposal <id> --status rejected --note "generated preview L3 prompt candidate triage; reject-only; insufficient signal for prompts/ask.md mutation"`；未执行任何 `apply <proposal_id>`。最终 live collect：`l3_debt_report.effective_preview_candidate_count=0`、`duplicate_existing_count=281`、`dedupe_or_noise_ratio=1.0`、`state_counts={rejected:282,reverted:2}`、`l3_proposal_attention=0`；`today` 首屏 `L3 Proposals` 显示 `(no L3 proposals need attention)`；`prompts/ask.md` hash 仍未变。剩余首屏 debt：升级动作 3、逾期动作 3、machine memory actions 16、concept backlog 30、review/revisit concepts 30；maturity 仍需等待 Day3 receipt，因为 `summarize --days 3` 读最近 run receipts，Day2 receipt 快照不会 retroactively 反映本次 live cleanup，Day3 自动 run 才会把 L3 debt=0 写入 maturity receipt。

- 2026-05-14 (dogfood maturity Day2 自动 receipt)：`aiwiki-dogfood-maturity.timer` 在 `2026-05-14 08:15 CST` 自动触发成功，`aiwiki-dogfood-maturity.service` exit 0，写入真实 Day2 receipt `output/control/maturity-gate/run-20260514T001501Z.json`（`generated_at=2026-05-14T00:15:01Z`，`status=pass`）。timer 下一次计划为 `2026-05-15 08:15 CST`。当前 `summarize --days 3`：`status=warn`、`receipt_count=2`、`days=[2026-05-13, 2026-05-14]`、`consecutive_days=false`（未满 3 天）、`status_counts.pass=2`、`failed_runs=[]`、`prompt_hash_changed_runs=[]`、`backlog_total_delta=-18`、`judgment_review_new_receipts=5`、`judgment_review_exception_rate=0.0`、`judgment_review_failure_rate=0.0`、`l3_generated_total=0`、`l3_effective_candidate_count=21`、`operational_maturity.status=not-yet`、`budget_violations=[effective_l3_candidates]`。结论：自动化已证明 Day2 能无人值守产 receipt；仍不能宣称成熟，Day3 后需要看 `receipt_count=3` / `consecutive_days=true`，且当前新增的 effective L3 candidates=21 是 Day3 verdict 前的 blocker 观察点，不能通过调阈值绕过。

- 2026-05-13 (dogfood maturity 自动 timer 接入)：用户确认 Day2/Day3 应自动跑后，本轮在 M1 证据收口基础上补齐本机 user-level 自动化：新增 `scripts/run_dogfood_maturity.sh`（显式要求 `AIWIKI_DOGFOOD_VAULT`，默认等价 maturity runbook：preview/l3=1000、compile=0、no semantic lint；非 `AIWIKI_DOGFOOD_MATURITY_FORCE=1` 时按 UTC 自然日检查已有 `run-*.json`，同日 receipt 存在即 skip），新增 `systemd/aiwiki-dogfood-maturity.service.template` / `.timer.template`，并接入 `scripts/install_user_service.sh` / `scripts/uninstall_user_service.sh`。已执行 `bash scripts/install_user_service.sh` 安装当前用户 timer：`aiwiki-dogfood-maturity.timer` active/waiting，Next=`2026-05-14 08:15 CST`（`00:15 UTC`，避免本地 00:15 落入前一 UTC 自然日）；env 在 `~/.config/aiwiki/aiwiki-dogfood-maturity.env`，vault=`/home/tim/danlu/炼丹炉`，不含 secrets。手动 `systemctl --user start aiwiki-dogfood-maturity.service` 成功且只输出 skip：已有 Day1 `output/control/maturity-gate/run-20260513T030123Z.json`，未写同日新 receipt；`summarize --days 3` 仍为 WARN/NOT-YET：`receipt_count=1`、`days=[2026-05-13]`、`consecutive_days=false`、`operational_maturity.status=not-yet`。验证：`bash -n` 三个 service scripts PASS；focused `tests.test_app_misc tests.test_app_runtime tests.test_deploy_defaults` 116 tests PASS；`bash scripts/verify.sh` PASS（2180 unit + 17 acceptance + 92% cov）；oracle qa-review `APPROVE_WITH_NITS` 无 blocker（nits：uninstall 不删 env、默认 vault 是 machine-local、可继续加强 force/filename-day 测试；已折叠 machine-local 提示与 force/filename-day 断言）。后续无需手动跑 Day2/Day3；只需在 2026-05-14 08:15 CST 后检查 timer 日志与 `summarize --days 3`。

- 2026-05-13 (三天 unattended dogfood maturity 证据收口 M1)：按下阶段建议物化新 plan/contract：`.codex/plans/active.md` / `.codex/contracts/active.md` 现改为 `三天 unattended dogfood maturity 证据收口`，主线只做 proof-before-expansion，不扩 L3/Judgment/Product Shell 工程范围，不修改 maturity 阈值，不伪造自然日。M1 当前可完成项：固定 Day2 runbook 为 `set -a && source .envrc.dogfood && set +a; PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py run --root "$AIWIKI_DOGFOOD_VAULT" --preview-limit 1000 --l3-limit 1000 --compile-limit 0 --no-semantic-lint`；固定 Day3 verdict 为 `PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py summarize --root /home/tim/danlu/炼丹炉 --days 3`。本日只读 summarize 结果保持 NOT-YET/WARN：`status=warn`、`receipt_count=1`、`days=[2026-05-13]`、`consecutive_days=false`、`judgment_review_new_receipts=5`、`l3_generated_total=0`、`l3_skipped_total=520`、`l3_already_exists_total=260`、`l3_not_eligible_total=260`、`operational_maturity.status=not-yet`、budget violations 为空；证明当前不能宣称全自动成熟。验证：`tests.test_dogfood_maturity_gate` 14 tests PASS；`bash scripts/verify.sh` PASS（2178 unit + 17 acceptance + 92% cov）；oracle 复核先因 gate artifacts 沿用上一轮而 REQUEST_CHANGES，已刷新 M1 专属 `.codex/gates/qa-review.md` / `.codex/gates/qa-runtime.md`；`HARNESS_DIR=.codex bash scripts/closed_loop.sh --require-contract` PASS。M2/M3 阻塞于真实 wall-clock 自然日：到 Day2/Day3 才可执行写入 `run` receipt；同日重复 run 不计入成熟证明。

- 2026-05-13 (Product Shell UX checklist 自动化闭环)：按 `docs/Furnace Product Shell UX Test Checklist.md` 物化本轮 harness plan/contract（`.codex/plans/active.md` / `.codex/contracts/active.md`），将整份 checklist 收敛为可自动化 gate + 人工 UX residual。@explorer 审计确认现有覆盖主要缺 checklist §7 高收益项；本轮新增 Jest/jsdom 行为测试 `universal-input-interaction.test.js`：覆盖 `renderUniversalInput` 成功后 pending received/输入与附件清空/按钮恢复，失败后 pending failed/输入与附件保留，拖拽纯 URL 文本填 textarea 不误当文件，Today 无 summary/空 feed/有 pending 三分支，`shell-summary -> buildTodayFeed() -> DOM`，首屏只挂 Universal Input + Today + Advanced（不回退独立 DropZone/AskBox/旧 operator 噪声），长标题 DOM 文本非空，`llm_status.configured=false` 时 UI 显示 LLM 未配置但输入/CTA 仍可操作，以及 `recent_raw_inputs` 行为级 reconcile 可把 received pending 标 raw done。Python contract 扩展：`extractPrimaryPath` 覆盖 checklist 所列 path-like keys；`llm_status` configured/unconfigured JSON shape 稳定；unconfigured 环境不阻塞 deterministic `ask` / `drop note` dispatch；smoke script 的 `run-ask` backend resolution failure 走 deterministic ask fallback，write-path smoke 会显示 `note_path/stored_path`。验证：targeted Jest 8 tests PASS；full Product Shell Jest 90 tests PASS；targeted Python 57 tests PASS，后续补 reviewer blocker 后 `tests/test_llm_check_render.py tests/test_product_shell_smoke.py` 45 tests PASS；`bash scripts/verify.sh` PASS（2178 unit + 17 acceptance + 92% cov）；`bash scripts/product_shell_smoke.sh` PASS（当前环境 `opencode-api` 不可用，按脚本降级验证 deterministic ask fallback）；`bash scripts/product_shell_smoke.sh --with-note-write` PASS，drop note 返回 `raw/inbox/product-shell-smoke-*.md`。人工 UX residual：真实 Obsidian light/dark theme、窄窗口、侧栏折叠/展开和实际视觉密度仍需人工或未来视觉自动化确认，本轮不伪造 §6 全 PASS。

- 2026-05-13 (Product Shell 真实投料 + 提问闭环收口)：本轮把三段用户可见问题合并进 active contract 并闭环：外部普通本地 PDF/image 可通过 `drop_pdf/drop_image` 导入（复制到受控 tmp 后继续执行 PDF magic/size、image MIME/size/OCR/vision 校验；`file://` 仍拒绝 vault 外；`drop_repo` 仍不放开外部目录；uppercase `HTTPS://` 不误判本地路径）；Obsidian pathless browser `File` 由 Product Shell 写入 vault 内 `.aiwiki/tmp/product-shell-drop/`，pending retry 保存 resolved path，不再传裸文件名；Universal Input 在附件/URL/PDF/image/repo/note-like payload + 非空问题时先 drop 全部材料，再单次调用 `run-ask --format report --fallback-to-ask` 生成报告，纯投料不触发 LLM、纯问题保持原路径。qa-review 首轮抓出 3 个 blocker：contract scope 过窄、问题误传为 drop `--title`、`original_path` 可能进 LLM prompt；已修复：`.codex/contracts/active.md` 扩成完整范围，auto-ask drop 阶段不再传 title，`collectMaterialPathsFromPayload` 排除 `original_path` 并补行为测试。验证：Product Shell targeted Jest 11 tests PASS + full Jest 84 tests PASS + `node --check main.js` PASS；targeted Python 231 tests PASS；`bash scripts/verify.sh` PASS（2172 unit + 17 acceptance + 92% cov）；oracle rereview `APPROVE_WITH_NITS`（nits：`auto-ask` retry 死分支、tmp cleanup、ask 失败重试会重复 drop，均已作为 residual）；`.codex/gates/qa-review.md` / `.codex/gates/qa-runtime.md` 已写；`HARNESS_DIR=.codex bash scripts/closed_loop.sh --require-contract` PASS。

- 2026-05-13 (全自动成熟度收口 gate + dogfood 首轮)：新增 repo-local `scripts/dogfood_maturity_gate.py`（`collect/run/summarize`）和 `tests/test_dogfood_maturity_gate.py`（11 tests），把“连续 3 天 unattended nightly”物化为可复跑 maturity gate：`run` 写 `output/control/maturity-gate/run-*.json` receipt，采样 before/after `review_backlog_counts`、L3 proposal/generation、planner-log mode/decision、judgment-review receipt、`prompts/ask.md` sha256；`summarize --days 3` 按自然日去重取每天最新 receipt，校验连续 3 天、backlog 不升、L3 candidate 不升/去重、judgment review receipt 增量、prompt hash 不变和关键字段完整，避免同日 3 次误报成熟。真实 vault `/home/tim/danlu/炼丹炉` 首轮执行：`source .envrc.dogfood` + `dogfood_maturity_gate.py run --preview-limit 1000 --l3-limit 1000 --compile-limit 0 --no-semantic-lint` PASS，receipt `output/control/maturity-gate/run-20260513T030123Z.json`；judgment review receipt 15→20（+5，均有可复审 receipt），L3 `generated_count=0 / skipped_count=520 / already_exists=260 / not_eligible=260`，`prompts/ask.md` sha256 保持 `6f66cdf8...` 不变；`summarize --days 3` 当前 WARN（只有 1/3 自然日 receipt，非失败）。qa-review 经 oracle 两轮 REQUEST_CHANGES 修复后最终 `APPROVE_WITH_NITS`（唯一 nit 已补强断言）；`bash scripts/verify.sh` PASS（2159 unit + 17 acceptance + 92% cov）。后续两天复跑同命令，第三天执行 `PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py summarize --root /home/tim/danlu/炼丹炉 --days 3` 判定成熟度。

- 2026-05-12 (F-INV-19 accept→apply 真链路 + manual vs 自动对比收口)：F-INV-19 走完 5 步真链路。Step 1 `aiwiki apply auto-sig-20260512-8211e0ab1a7f` PASS（state=accepted, before=`6f66cdf8...`, after=`94c68cc6...`），CLI 表面校正：`review proposal --status` 词表只 `{rejected}`，accept 语义合并入 `aiwiki apply` 单事件（contract 文案在 N1 nit fold 时已改为 `l3-proposal-apply` 事件 + 说明 accept 合并）。Step 2 落盘验证：`prompts/ask.md` 2 → 22 行（原 stub + 20 行 auto-proposal block），sha256 与 receipt 一致。Step 3 runtime-history 3 条事件 create/apply/revert 全在。Step 4 revert smoke PASS：`restored_hash` 完美回到 `before_hash`，文件 22→2 行 stub。Step 5 receipt 段写入 vault `output/reports/dogfood-receipt-investing-v2.1.md`（147→273 行）。**主要发现**：auto generator 产 trace metadata block（planner decision + evidence ref）而非真 prompt diff，apply 后 ask.md 混入"planner decision 上下文"对 ask 行为无改进；F-INV-18a oracle 留的 N2 nit 与 F-INV-19 contract Residual Risks 段被实证。本轮 revert 已 deterministic 闭环，污染未持久；建议 v2.2 加 F-INV-20 改 `_automatic_l3_prompt_content` 模板。verify PASS（unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review（ses_1e31ddb30ffeRnDX2wDWA1MMPg）APPROVE_WITH_NITS 无 must-fix，3 nice-to-have：N1 contract 文案 fold；N2 revert receipt `after_hash` 字段语义偏绕（src/ 改动留 v2.2）；N3 立 F-INV-20 修 generator。closed_loop PASS。**v2.1 plan 四 milestone 全部收口**（NEW-5 `eba6fd1` → F-INV-18a `2f5c1c9` → F-INV-18 `347ea6e` → F-INV-19 本轮）。下一步等用户拍板：开 v2.2 plan（含 F-INV-20 + F-INV-18a N1 测试断言 nit + dogfood envrc backend 修） 或先 commit 收尾。

- 2026-05-13 (P0 harness 物化与 proof-before-expansion 收口)：已把 `.codex/plans/active.md` 物化为 P0 contract（`三天 maturity gate 收口`），复跑 focused tests 从 11→12（新增 prompt hash changed fail-path 直测）和 `bash scripts/verify.sh` PASS（2160 unit + 17 acceptance + 92% cov）。`summarize --root /home/tim/danlu/炼丹炉 --days 3` 仍为 WARN：`receipt_count=1`、`days=[2026-05-13]`、`consecutive_days=false`、judgment review +5、L3 `generated=0 / skipped=520 / already_exists=260 / not_eligible=260`，未误报三天成熟。qa-review oracle PASS，无 blocking；nits 已折叠：P0 `Out Of Scope` 正确物化、prompt hash fail-path 补测、后续新 receipt 同时写 `settings.skip_l3_generate` 与 `settings.apply_l3_generate` 以兼容首日字段名。P0 结论保持：当前只能证明 Day1 真实 receipt 与 gate 不误报，不能宣称全自动闭环已成熟；Day2/Day3 继续复跑 `set -a && source .envrc.dogfood && set +a; PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py run --root "$AIWIKI_DOGFOOD_VAULT" --preview-limit 1000 --l3-limit 1000 --compile-limit 0 --no-semantic-lint`，第三天再跑 `PYTHONPATH=src python3 scripts/dogfood_maturity_gate.py summarize --root /home/tim/danlu/炼丹炉 --days 3`。

- 2026-05-13 (P1 L3 debt report 收敛)：P1 contract 已物化为 `L3 收敛与去重硬化`，在 `scripts/dogfood_maturity_gate.py` 增加只读 `l3_debt_report`，不写 `prompts/*.md` / `schema/policies/*`，不放开 L3 auto-apply。报告把 L3 候选拆成 preview eligible / not eligible / existing duplicate / low evidence / effective attention：真实 vault read-only collect receipt `output/control/maturity-gate/snapshot-20260513T044137Z.json` 显示 `preview_candidate_count=520`、`preview_eligible_count=260`、`preview_not_eligible_count=260`（`requires_execute_mode`）、`duplicate_existing_count=260`、`low_evidence_candidate_count=259`、`effective_preview_candidate_count=0`、`effective_attention_count=0`、`dedupe_or_noise_ratio=1.0`、`attention_noise_ratio=1.0`。focused tests 仍 PASS（12 tests，扩展 duplicate/low-evidence/not-eligible 断言）；`bash scripts/verify.sh` PASS（2160 unit + 17 acceptance + 92% cov）。P1 结论：L3 治理债主要是重复/低证据/不可执行噪音，不是可以安全自动 apply 的真有效候选；P2 只能推进 Judgment lane，L3 仍保持关闭。

- 2026-05-13 (P2 Judgment lane 受限放开指标)：P2 contract 已物化为 `Judgment lane 受限放开`，仍不自动生成新 judgment、不改写 thesis、不取消 limit。`src/aiwiki/runner/auto_adopt.py` 给 judgment review 增加 `limit`、`confidence_counts`、`conclusion_counts`、`exception_count/rate`、`exception_queue`，把 `weakened/refuted/low-confidence/error/unparsed/receipt_history_corrupt/failed` 明确上浮为异常；execution receipt 新增 `scan_generated_at/reviewer_model/conclusion/confidence` 元数据，保留 `review_id` 与 target path。`AIWIKI_NIGHTLY_AUTO_ADOPT_JUDGMENTS_LIMIT` 可显式调 daily limit，默认仍 5，最小 1。maturity gate 新增 `judgment_lane_report`，真实 vault read-only collect `output/control/maturity-gate/snapshot-20260513T045448Z.json`：`reviewed=5 / total_candidates=15 / failed=0 / exception_count=0 / failure_rate=0.0 / exception_rate=0.0 / receipt_count=20 / unique_review_ids=20 / unique_target_paths=6`，兼容旧 receipt（旧 20 条缺 conclusion/confidence，但最新 nightly item 补充 `upheld/high=5` 统计）。focused tests PASS（`tests.test_auto_adopt` + maturity gate 共 39 tests），覆盖 weakened/refuted/low-confidence/error/unparsed；后续继续 full verify + qa-review + closed_loop。

- 2026-05-13 (P3 operational maturity 指标收口)：P3 contract 已物化为 `人只看异常运行指标与异常预算`，`scripts/dogfood_maturity_gate.py summarize` 新增 `operational_maturity` summary：`human_only_exceptions`、anomaly budget、latest 指标和 3/7/14 trend windows。README 已改口径：人只看异常是成熟度目标，不是当前默认事实。修复 qa-review blocker：`summarize --days 3` 的主判定仍只看最近 3 个自然日 receipt，但 `trend_windows["7"]` / `["14"]` 现在独立按真实 7/14 自然日 receipt 计算，不再被 3 日主窗口截断；新增回归测试构造连续 8 天 receipt，断言主 `receipt_count=3`、trend 7=7、14=8。真实 vault summarize 当前仍 WARN/NOT-YET：`receipt_count=1`、`days=[2026-05-13]`、`consecutive_days=false`、`human_only_exceptions=false`、budget violations 为空；原因是缺 Day2/Day3 连续证明，不是异常预算失败。验证闭环：`tests.test_dogfood_maturity_gate` 14 tests PASS；`bash scripts/verify.sh` PASS（2165 unit + 17 acceptance + 92% cov）；oracle rereview `APPROVE_WITH_NITS`（nits：7/14 window 可补 days/consecutive_days、delta 断言可更强）；`HARNESS_DIR=.codex bash scripts/closed_loop.sh --require-contract` PASS。P0-P3 harness 均已收口，但不能宣称全自动成熟；后续自然日继续复跑 dogfood gate，第三天以 `summarize --days 3` 判定。

- 2026-05-13 (外部本地 PDF/image 导入根治)：修复 Product Shell 拖入/选择 vault 外 PDF 后后端报 `not within /home/tim/danlu/炼丹炉` 的低级路径问题。根因是前端传 Electron `file.path` 绝对路径，而 `drop_pdf/drop_image` 的 binary 收集层强制 `safe_resolve_within(..., root)`。本轮在 `src/aiwiki/drop.py::_collect_binary_to_tmp` 放开普通本地单文件 PDF/image：`Path(source).expanduser().resolve()` + `is_file()` 后复制到受控 tmp，再沿用既有 PDF size/%PDF magic、image MIME/size/OCR/vision 校验，`original_path` 记录 resolved path；`file://` URI 仍走 `safe_resolve_within` 并拒绝 vault 外文件，避免 URI 通道放开；`http/https` scheme 改为 `urlparse(...).scheme.lower()`，修掉 uppercase `HTTPS://` 被误判本地路径的问题。`drop_repo` 外部目录仍拒绝（递归读取风险不放开）；`drop_note` 允许 vault 外文本文件的既有行为新增显式测试记录。CLI help 同步说明 PDF/image 支持 vault 外本地路径、repo 为 vault-local path。排查到前端仍有 `runLauncherCommand` 手写 split 和 Product Shell 文案未同步两个 residual，未作为本轮 blocker。验证：targeted `tests.test_drop_safety tests.test_drop tests.test_cli_universal_input tests.test_cli` 183 tests PASS；`bash scripts/verify.sh` PASS（2170 unit + 17 acceptance + 92% cov）；oracle qa-review `APPROVE_WITH_NITS`（唯一 nit：Product Shell 文案同步）；closed_loop 待本条后执行。

- 2026-05-13 (Product Shell pathless File 附件根治)：用户复测后错误从 `not within <vault>` 变成 `Source not found: 20260513_...pdf`，证明上轮只修了“有绝对路径但在 vault 外”的后端分支，没修当前 Obsidian 环境 `File.path` 缺失导致 UI fallback 到裸 `file.name` 的分支。根因链路：`render_input.js` 只保存 `{name,path,type}`，提交 `file.path || file.name`；运行时加载的是 build 产物 `main.js`，只改 `src` 不生效。本轮新增 `resolvePluginFileSource(plugin, file)`：有有效 absolute/nested path 直接用；无 `file.path` 但有 `arrayBuffer()` 时，把内容写入 vault 内 `.aiwiki/tmp/product-shell-drop/<stamp>-<safeName>`，文件名清洗含 `/\\:*?"<>|` 与控制字符，再把真实路径交给 CLI。Universal Input paste/drop、dropzone PDF/Image、DropFileModal/DropImageModal picker 均接入该 resolver；pending retryArgs 保存 resolved path，不再保存裸浏览器 File name；`bash build.sh` 已同步 `main.js`。新增 Jest 行为测试真实模拟 pathless File：验证写入 vault tmp、内容正确、危险文件名不入路径；并锁定 absolute path 快路径不调用 `arrayBuffer()`。验证：Product Shell Jest 77 tests PASS + `node --check main.js` PASS；targeted Python `tests.test_product_shell_universal_input tests.test_drop_safety tests.test_drop tests.test_cli_universal_input tests.test_cli` 186 tests PASS；`bash scripts/verify.sh` PASS（2170 unit + 17 acceptance + 92% cov）；oracle qa-review code pass（nits：tmp cleanup 策略、retryArgs 字段名可更清晰）；closed_loop 待本条后执行。

- 2026-05-12 (F-INV-18 真投料主验收 PASS — L3 自动周期跑通)：F-INV-18 contract 物化后，三 step 真投料阻塞在「fresh trace 只有 medium signal，旧 trace 才有 high signal」陷阱（参见上一轮 explorer audit `ses_1e36f8bf3ffej48DaoY6pp4jDe`）。@explorer 复用 `ses_1e36f8bf3ffeEhYm8UFj2xiKQr` audit signals 子系统 → 5 个 router-eligible (kind, severity) pair 里只有 `elixir_dependency_break high` 是 deterministic + 入口直接（`src/aiwiki/execution/alchemy.py:1567-1582` demote → `_collect_dependent_elixir_ids` 833-867 → `signals/adapters.py:382-457` 固定 severity=high）。@explorer ses `ses_1e3677104ffecH26cfD9dNAdPx` 调查 vault 依赖图：只有 `b8dadbd3.derived_from` 指向 `97f9dc26`，无人指向 `b8dadbd3` → 只 demote 主丹能产 break。用户决策 B'：接受 v2 主丹 settled→candidate 不可逆 vault 写入，F-INV-18 闭环后 alchemy-promote 重提升回 settled。执行 `alchemy-demote --elixir-id elixir-ai-thesis-2024-97f9dc26 --note "F-INV-18 deterministic high-signal trigger; will promote back after milestone closes"` → execution-receipts.jsonl 写 `subject_kind=elixir_demotion / dependency_breaks=[{break_reason:source_demoted, dependent_elixir_id:elixir-vs-thesis-b8dadbd3}]`。`signals-replay --source archive` → 新 signal `sig-20260512-8211e0ab1a7f / kind=elixir_dependency_break / severity=high / trace_id=dfd72548-1513-48d4-a02a-fa683679bf00`（fresh trace ≠ 历史 `9a396669`）。`planner-log-replay --execute` → 1 record `decision=generate-proposal / mode=execute / side_effects_allowed=true / reason_codes=[elixir_dependency_break_observed, proposal_recommended, execute_mode_requested]`。`l3-proposal-generate --apply` → `output/_proposals/prompt/auto-sig-20260512-8211e0ab1a7f.md` 落盘，markdown 含 F-INV-18a 模板实质字段（`signal_id / trace_id=dfd72548-... / decided_at / dedupe_key / reason_codes` 三个 reason 全在 + Evidence references bullet）。**F-INV-18 主验收 6 条全 PASS**：(1) fresh trace 命中 generate-proposal ✓；(2) candidate `eligible=true blockers=[]` ✓；(3) proposal markdown 真落盘 ✓；(4) trace_id ≠ 历史 `9a396669` ✓；(5) markdown 含 F-INV-18a 模板字段 ✓；(6) 投料链路 alchemy-demote → execution-receipts.jsonl → signals-replay (archive) → planner-log-replay (execute) → l3-proposal-generate (apply) 完整 ✓。dogfood envrc `opencode-api` backend 解析失败（不在 `[codex-cli, copilot-cli, claude-cli]` 列表）使得 `run-nightly` 不可走，但 F-INV-18 真投料链路用 deterministic CLI 链替代，不依赖 LLM。下一步：写 v2.1 receipt 段 + 跑 verify + qa-review + finalize → 收 F-INV-18；后续 alchemy-promote 重提升主丹 settled；进 F-INV-19。

- 2026-05-12 (F-INV-18a 修 `_automatic_l3_prompt_content` 收口陷阱 2)：F-INV-18 audit 阶段经 @explorer 复用 ses_1e3c9ad51ffej48DaoY6pp4jDe 确认陷阱 2（b）命中——`src/aiwiki/execution/l3_proposals.py:410-421` 旧 `_automatic_l3_prompt_content()` 只在 `prompts/ask.md` 末尾追加 6 行 HTML 注释占位（`<!-- aiwiki:auto-proposal:start --> / signal_id: ... / Review ... / end -->`），完全不含 signal 摘要 / judgment 上下文 / evidence_refs / 操作建议；F-INV-18 success criteria 第 5 条「proposal 内容不是空洞」必然 FAIL。用户决策 A：拆 F-INV-18a 修函数（Recommended）。`.codex/plans/active.md` Milestone Index 插入 F-INV-18a，写完整 milestone 段（goal / context / success criteria 6 条 / scope / execution plan 8 步 / chosen approach / alternatives rejected / verification plan / fail gate / residual risks）+ 物化 `.codex/contracts/active.md`。@fixer 实施：`src/aiwiki/execution/l3_proposals.py` (+36/-4) 把 `_automatic_l3_prompt_content` 签名改为 `(current_content, *, candidate: dict[str, Any], planner_log_path: str)`，输出 deterministic markdown 模板（`## Auto-generated L3 proposal review` 标题 + `### Planner decision` 5 字段列表 `signal_id/trace_id/decided_at/dedupe_key/reason_codes` + `### Evidence references` bullet `<planner_log_path>#<signal_id>`），保留 `<!-- aiwiki:auto-proposal:start -->` / `end` 边界 byte 一致 + 尾部换行，缺失字段写 `(unknown)` / 空 reason_codes 写 `(none)`；同步唯一调用点 `generate_l3_proposals_from_planner` line 378-388 透传 `candidate` + `preview.get("planner_log_path")`，不动 `create_l3_proposal()` 任何参数。`tests/test_l3_proposals.py` (+50) 新增 `test_automatic_l3_prompt_content_includes_record_fields`（fixture record 全字段断言）+ `test_automatic_l3_prompt_content_handles_missing_fields`（空字段占位断言）。**不引入 LLM**，纯 string template，保证 deterministic + 可测。verify PASS（unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review（复用 ses_1e3d77e8fffe72lJpBGL7OQTEJ）单轮 PASS：6 success criteria 全 APPROVE / 4 红线全 PASS / 0 BLOCK / 2 NIT（N1：现有集成测试只断 boundary 未断 patch.content 字段，F-INV-18 顺手补；N2：模板成 proposed full replacement prompt 一部分，F-INV-19 真 apply 时需评估 prompt 污染）；closed_loop PASS。下一步进 F-INV-18：物化 contract → 真材料周期 → planner-log `mode=execute` 落盘 → fresh trace candidate → auto-adopt → proposal 落 `output/_proposals/`。

- 2026-05-12 (P4-INV-NEW-5 dogfood doc + CLI smoke 收口 F-INV-17 入口可见性)：v2.1 plan 起草 + 首 milestone 收尾。`.codex/plans/active.md` 三 milestone 串：NEW-5（execute-mode 入口可见性）→ F-INV-18（一次完整自动周期）→ F-INV-19（accept→apply + manual vs 自动对比），Global 嵌入 oracle ora-26 评估出的三个陷阱缓解（陷阱 1 = `AIWIKI_NIGHTLY_AUTO_ADOPT_L3` 不生成 candidate / 陷阱 2 = `_automatic_l3_prompt_content` 可能空洞 / 陷阱 3 = 878 候选均为单一历史 trace）。NEW-5 经 @explorer audit 重大发现：底层管线全通——`aiwiki planner-log-replay --execute` CLI flag 已存在（`src/aiwiki/cli/parsers.py:931-940` + `dispatch.py:582-595`）、planner-log `mode` 字段已存在（`src/aiwiki/planner/log_writer.py:125-138` 12 个 keys）、`requires_execute_mode` 解锁逻辑已正确（`src/aiwiki/execution/l3_proposals.py:310-315` `mode == "execute"` 即解锁）、既有 tests 已覆盖 `mode="execute"`（`tests/test_planner_log.py:426,1436`）；NEW-5 真实缺口在发现性 + dogfood 文档 + 端到端 CLI smoke。用户决策 A：纯 doc + smoke test，**不动 src/**。实施：`docs/Furnace Investing Dogfood Plan.md` 新增 §2.8 「执行模式（execute-mode）开启与 F-INV-18 复现」，含三入口职责区分（`planner-log-replay --execute` 前置条件 / `l3-proposal-generate --apply` 真正 candidate 生成入口 / `AIWIKI_NIGHTLY_AUTO_ADOPT_L3 + run-nightly` 已有 proposal auto-apply）+ 行为差异 + 风险边界（明确覆盖 `.aiwiki/state/` / `output/_proposals/` / `wiki/indexes/log.md` 经 `append_wiki_log` / `prompts/*.md` 或 `schema/policies/*` 含 wiki 资产层）+ F-INV-18 复现 5 步示例 + 成功判据（通过 `signal_id`/`evidence_refs` 反查 planner-log `trace_id` ≠ 历史 `9a396669-...`，因 proposal 本身不存 `source_trace_id`）。`tests/test_planner_log.py` 在 `TestCLI` 加 `test_cli_execute_flag_writes_execute_mode_records_to_disk` 真 e2e smoke：调 `main(["--root", root, "planner-log-replay", "--execute"])` + case_basic fixture，断言 `.aiwiki/state/planner-log.jsonl` 真落≥1 条 `mode=execute` record（无 mock）。**不动 src/**；verify PASS（unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review r1 FAIL（ses_1e3d77e8fffe72lJpBGL7OQTEJ）抓出 4 个 [BLOCK]：(1) §2.8 流程错误把 `run-nightly` 当 generator / (2) candidate generation vs auto-adopt 概念混淆 / (3) 风险边界遗漏 `wiki/indexes/log.md` 与 `prompts/*.md` 写入 / (4) `source_trace_id` 字段不存在；全 4 个 fix 后 r2 APPROVE。closed_loop PASS。**v2.1 首 milestone NEW-5 done**。下一步 F-INV-18：用 NEW-5 doc 入口在真 vault 跑 audit `_automatic_l3_prompt_content()` 真实输出 → 决策门 → 跑真材料完整自动周期。
- 2026-05-12 (Investing Dogfood v2 §2.7 收官，v2 升 6/7→7/7)：补做 v2 §2.7 L3 prompt proposal 闭环，走 **manual fixture path**。`aiwiki l3-proposal-create --kind prompt_proposal --proposal-id prop-inv-dogfood-v2-ask-confidence --target-file prompts/ask.md --pattern manual_fixture` 创建 candidate（提议 investing protocol `confidence_level` 按来源密度 + `counter_evidence_provenance` 自适应，根据 v2 实跑 752 页/16.5MB 真年报仍标 low 的 friction）；`aiwiki review proposals` 队列显示 v2 candidate；`aiwiki review proposal ... --status rejected --note "..."` reject 闭环。落盘四层完整：`output/_proposals/prompt/prop-inv-dogfood-v2-ask-confidence.md`（2007B）+ `.aiwiki/state/l3-proposals.json` state=rejected + `.aiwiki/state/runtime-history.jsonl` 双事件 `l3-proposal-create` / `l3-proposal-reject` + audit-mirror。**F-INV-17 实证**：v2 投料后 `l3-proposal-generate --dry-run` 扫到 878 planner records / 20 candidates returned，全部 `eligible: false` blocker=`requires_execute_mode`；`--apply --limit 5` `generated_count: 0`；所有 candidates 都引用历史 trace `9a396669-ab7d-4123-ad2c-0e5350fb05d0`（v0/v1 时代），v2 投料 0 触发新 candidate。根因 = `mode=observe_only` 默认，planner-log 中 `mode=execute` 记录 = 0。**v2.1 转 P4-INV-NEW-5**：investing protocol nightly auto-adopt L3 默认 opt-in / dogfood 文档说明 `mode=execute` 切换路径 / `shell-status` 或 `review proposal-generation` 暴露当前 mode。F-INV-18/19 N/A（因 F-INV-17 阻塞，自动路径未走，accept→apply 质量对比留 v2.1）。`output/reports/dogfood-receipt-investing-v2.md` 追加「§2.7 收官（2026-05-12）」段 + 7 步 flow 改 7/7 + L3 proposal 数 1。**v2 dogfood 完整 7/7 收官**，后续转 v2.1 + 多轮 dogfood。下一步等用户拍板：开 v2.1 plan（含 F-INV-NEW-5 + NEW-4 nits residuals + 真 accept→apply 质量对比）或先启动多轮 dogfood。
- 2026-05-12 (Investing Dogfood v2 收官 receipt 段追加)：`/home/tim/danlu/炼丹炉/output/reports/dogfood-receipt-investing-v2.md` 追加「v2 收官（2026-05-12）」段：5/5 摩擦点 + NEW-4 nits 闭环表（commits `10d7b74` / `cf0b512` / `275a522` / `96e8b76` / `05a1c58` / `fe9d68f`）+ gate 状态（每 milestone verify PASS + qa-review fresh-session 4 APPROVE / 1 APPROVE_WITH_NITS nits 已清 + trace 测试 23→25）+ 范围保持（未动 src/ 对外接口 / CLI / 既有 schema 字段 / 无新 protocol/backend/lane/UI/依赖）+ 结论（F-INV-1~16 复检状态保持 / §2.7 L3 proposal 转 v2.1 / 后续启动多轮 dogfood）。**v2 复审通过**：5 NEW + 11-revisit 全 done，历史 F-INV-* 状态保持。下一步等用户拍板 v2.1 L3 proposal plan 与多轮 dogfood 测试的执行顺序。
- 2026-05-12 (P4-INV-11-revisit counter_evidence_provenance 收口 F-INV-11-revisit)：promote receipt + settled elixir frontmatter 加 `counter_evidence_provenance: "real" | "none_found"` 字段。`src/aiwiki/execution/alchemy.py` `promote_elixir` 在 `_validate_promote_gate` 后基于已校验 frontmatter 静态判定：`counter_evidence_items == ["NONE_FOUND"]` → `"none_found"`，其他 → `"real"`；写入 settled frontmatter（与 `promoted_at` 同一 update）+ 传给 `build_elixir_promotion_receipt`。Tombstone（superseded candidate plane）刻意不带新字段——避免 promote-time 语义污染 candidate plane；receipt 已持久记录 provenance 无信息丢失。`src/aiwiki/app_execution.py` `build_elixir_promotion_receipt` 加必填参 `counter_evidence_provenance: str`，写入 `bundle.counter_evidence_provenance`（**追加字段**，向后兼容）。`tests/test_alchemy.py` 扩 `test_promote_receipt_records_counter_evidence_gate_fields`（real 路径双断言 receipt + settled frontmatter）+ 新增 `test_promote_marks_none_found_provenance`（none_found 路径 + 显式断言 candidate/tombstone `assertNotIn`）。D-3 acceptance golden `tests/fixtures/acceptance/D3/case_elixir_stage3_compounding/expected/files/.aiwiki__state__execution-receipts.jsonl.golden` refresh：bundle 追加 `counter_evidence_provenance: "none_found"` + primary_path_sha256 漂移（settled frontmatter 多字段，预期），secondary_path_sha256 不变正好证明 tombstone 未污染。verify PASS（2137 unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review APPROVE_WITH_NITS（ses_1e42429f1ffejENGS2kiX9COpa，两 nit 非阻塞：N1 receipt builder 未校验 provenance 取值集合 / N2 未来 demote 路径应 `pop("counter_evidence_provenance", None)`）。closed_loop PASS。**dogfood v2 五个摩擦点 P4-INV-NEW-1/2/3/4 + P4-INV-11-revisit 全部 done，v2 收官**。
- 2026-05-12 (P4-INV-NEW-4 trace 识别 derived 资产 kind 收口 F-INV-NEW-4)：`src/aiwiki/trace.py` `_classify` 在 concept 之后、judgment 之前加 `wiki/derived/` / `./wiki/derived/` / `derived-` 前缀 → `"derived"`；`_resolve_any` 路由加 `derived` 分支；新增 `_resolve_derived(root, asset_id, *, direction, depth, visited)`：用 `_find_curated(root, "wiki/derived", asset_id)` 找文件，parents (up/both) 走 `citations`（含 `#sha256` anchor fragment 剥离后再 `_resolve_any`），children (down/both) 扫 `wiki/elixirs/` + `output/_candidates/elixirs/` 的 `derived_from`，命中后作为 elixir child；metadata 仅当 frontmatter `protocol` 非空时挂载（与既有 sparse `to_dict` 行为一致）。新增 helper `_derived_ref_matches(ref_value, derived_rel, derived_id)`：剥 quote/anchor 后 match `derived_rel` / 文件名后缀 / id 子串。`tests/test_trace.py`：`_seed_minimal_vault` 加 `wiki/derived/derived-test.md`（citations 含 anchor `#sha256`）+ 把 candidate elixir `derived_from` 追加 `wiki/derived/derived-test.md`；`_case_classify_recognizes_known_prefixes` 加 3 个 derived 断言（path / `./` / bare id）；新增 4 个 case：`derived up → source → raw` / bare-id / `derived down → elixir` / `elixir up parents 含 derived`。D-3 acceptance golden `tests/fixtures/acceptance/D3/case_elixir_stage3_compounding/expected/stdout/05-trace-up.json` refresh：旧 golden 把 `wiki/derived/*.md` 标 `kind: unknown` + `not_found: true`，refresh 后改为 `kind: derived` + 有效 `label/path`（预期正向变化），diff `1 file, +8/-10`。verify PASS（2135 unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review APPROVE_WITH_NITS（ses_1e43d1fceffehHOsyzrX2VyjQ7，两个 non-blocking nit：`source down → derived` 反向边 + children 去重，留 follow-up）。下一步 P4-INV-11-revisit：promote receipt 加 `counter_evidence_provenance: "real"|"none_found"`。
- 2026-05-12 (P4-INV-NEW-3 `--include-elixir` doc/CLI 一致性收口 F-INV-NEW-3)：doc-only 修正。`docs/Furnace Investing Dogfood Plan.md` §2.6 §188 行移除错误的 `aiwiki ask "..." --include-elixir <elixir-id> --protocol investing`（CLI 中 `--include-elixir` 仅在 `alchemy-start` / `alchemy-distill` 注册，`ask` parser 不支持），改为 `aiwiki ask ... --protocol investing` + 注释「ask 当前不接 `--include-elixir`，旧丹复用通过下一步 alchemy-start `--include-elixir` 注入」；同步修正 §195 F-INV-14 描述把入口标注为 `alchemy-start/-distill --include-elixir`。决策候选 A（doc-only）vs B（给 ask 加 `--include-elixir`）：选 A，理由 KISS + Global Constraint「不动 CLI 表面」+ query-time compounding 与 alchemy 持久链复用语义不同，B 留 follow-up。**不动任何 src/**；`grep -rn "ask.*--include-elixir" docs/ README.md` 残留 check 通过。verify PASS（2131 unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review APPROVE_WITH_NITS（ses_1e44ceaccffeNbBjaKsgb5HvQk，nit：§187 注释措辞已 fold）。下一步 P4-INV-NEW-4：trace 识别 `wiki/derived/*.md` 资产 kind。
- 2026-05-12 (P4-INV-NEW-2 run-compile fail-fast per-job receipt 收口 F-INV-NEW-2)：`run-compile` 三条 fail-fast 分支（source page / concept page / concept-rewrite proposal）落 per-job JSON receipt 到 `output/control/execution-receipts/run-compile-<action_id>.json`，按 alchemy / file-back / l3-proposal-apply 既有 pattern。`src/aiwiki/runner/workflows.py` 新增 `_unique_run_compile_action_id(root, started_at_ms)`（`run-compile-<epoch_ms>` + `-<n>` 冲突递增）+ `_write_run_compile_failure_receipt(root, *, subject_kind, subject_id, target_file, source, item_audit, item_result, exc, started_at_ms, duration_ms, used_profile, item_retry_profile, fallback_stages, fallback_reason, extra=None) -> str` helper，`atomic_write_text` + `json.dumps(... ensure_ascii=False, indent=2, sort_keys=True)`，全包 try/except — write 失败 best-effort log warning，**绝不掩盖** 原 LLMError/RuntimeError。Receipt schema：`version=1` / `kind="execution-receipt"` / `generated_by="aiwiki-run-compile"` / `operation="compile"` / `status="failed"` / `action_id` / `applied_at` / `subject_kind ∈ {source_page, concept_page, concept_rewrite_proposal}` / `subject_id` / `target_file` / `source`(source-page only) / `prompt_profile` / `retry_prompt_profile` / `duration_ms` / `error_class` / `error_message` / `fallback_stages` / `fallback_reason` / `llm_audit` / `response_id` / `usage` / `receipt_path` / `revert_supported=false`；concept / rewrite 分支 extra 字段 `source_pages` / `concept_page` / `quality_priority` / `quality_issues`。`_stamped_record` 在三条 fail 分支的 event dict 内追加 `receipt_path` 字段（不动其它字段，**只追加**），打通 `.aiwiki/logs/llm-receipts.jsonl` ↔ `output/control/execution-receipts/run-compile-*.json` 交叉引用。**成功路径不写 per-job JSON**（保持 acceptance fixture byte-stable + vault footprint）。`tests/test_run_compile_fail_fast.py` 加 4 个 test：source-page fail e2e 写 receipt + 全字段 / 成功路径 0 receipt / receipt-dir 被文件占据时原异常不被掩盖 / parametrized helper test 覆盖 3 个 subject_kind 的 wiring + extra 字段。verify PASS（unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review r1 APPROVE_WITH_NITS（contract JSONL target 措辞 + 缺 concept/rewrite call-site 覆盖）→ r2 APPROVE_WITH_NITS（仅 contract 文档 nit subject_kind 三种漏一种，已修）（ses_1e45bb9c3ffexSd9b8FmqEhtyw）。下一步 P4-INV-NEW-3：`--include-elixir` 文档与 CLI 一致性收敛。
- 2026-05-12 (P4-INV-NEW-1 adaptive run-compile timeout 收口 F-INV-NEW-1)：`run-compile` 在 `create_client` 前按 pending 队列里最大 raw 字节自适应估算 per-call timeout。`src/aiwiki/runner/workflows.py` 新增 `_compute_adaptive_compile_timeout(root, pending)` helper，常量 `BYTES_PER_PAGE=30_000` / `SECONDS_PER_PAGE=60` / floor 240 / ceil 1800，公式 `clamp(ceil(max_bytes / 30_000) * 60, 240, 1800)`。语义：env `AIWIKI_LLM_TIMEOUT` 显式设置 → 返回 `None`（env 永远赢，`LLMConfig.from_env` 沿用）；pending 为空 → `None`；pending 非空但所有 stored_path 都不可 stat / 越出 vault root（含 abs path / `..` 穿越） → 回退 floor 240s。Containment guard 用 `root.resolve()` + 每条 `raw_path.resolve()` + `relative_to(root_resolved)`。Helper 在 paths filter 之后执行，只看真正会喂给 LLM 的子集。**不动** `LLMConfig.timeout_seconds=120` 默认 / CLI surface / receipt/audit/runtime-history schema / 现有 backend。`tests/test_runner.py` 加 6 个 adaptive test（empty→None / large→1800 cap / small→240 floor / env override→None / all-unstatable mixed (missing+abs+`..`+empty+non-dict)→floor / 3-entry largest-wins→600s）。`docs/Furnace Investing Dogfood Plan.md` §2.3 加 adaptive 公式 + 三条规则。verify PASS（unit + 17 acceptance + 92% cov + ruff clean）；fresh oracle qa-review r1 REQUEST_CHANGES（docstring/docs 与 empty-pending 行为不一致 + 缺 containment guard + 缺 unstatable/largest-wins test）三处全修后 r2 APPROVE_WITH_NITS（ses_1e46b7d9affebphzcdKdjaolc9，唯一 nit：可选 integration test patch create_client，不阻塞）。下一步 P4-INV-NEW-2：run-compile fail-fast 写 execution-receipt。
- 2026-05-12 (Investing Dogfood v2 真实研报实跑 5/7 闭环)：3 份真 PDF（韦尔 274p / 宁德 229p / 恒瑞 249p）走完 §2.1-§2.6。backend=opencode-api/deepseek-v4-pro；run-compile 1211s（`AIWIKI_LLM_TIMEOUT=600`）→ 3 source pages 全 compiled；3 file-back judgment 全 confirmed；2 settled elixir（主丹 `elixir-ai-thesis-2024-97f9dc26` + 复利丹 `elixir-vs-thesis-b8dadbd3`，`derived_from` 跨丹引用 + trace --direction up 递归可见）。Step 4 drift_scan stale_judgments 闭环验证 PASS：手工 backdate 韦尔 `last_reviewed` 至 2 天前，`STALE_JUDGMENT_DAYS=1` + `AIWIKI_NIGHTLY_DETERMINISTIC_ONLY=1` 触发 nightly → `drift_aging.signals_appended=1`，新 signal 落 `.aiwiki/state/signals.jsonl`（kind=drift, severity=medium, judgment_refs=[韦尔]）。Receipt 落盘：`output/reports/dogfood-receipt-investing-v2.md`（vault），file-back→review confirmed。**4 个新摩擦点 F-INV-NEW-1~4**：(1) `AIWIKI_LLM_TIMEOUT` default 120s 对中文年报严重不足；(2) `run-compile` fail-fast 未落 execution-receipt；(3) `ask --include-elixir` 文档与 CLI 不一致（实际入口在 alchemy-start）；(4) `trace` 不识别 derived 资产 kind。**F-INV-11 仍开放**：counter_evidence 走 NONE_FOUND 兜底通过，非真材料反证。§2.7 L3 proposal 留 v2.1。
- 2026-05-12 (D L3 proposal apply→revert acceptance Gap D 收口)：L3 governance lane create→apply→revert 三步链上提到 acceptance 层。新 fixture `tests/fixtures/acceptance/D/case_l3_proposal_apply_revert/`（seed `prompts/test-prompt.md` + 7 byte-stable golden：target / `.aiwiki/state/l3-proposals.json` / 2 receipts (apply/revert) / runtime-history / audit / wiki log）。`tests/acceptance/case_runner.py` 加 `_run_l3_proposal_apply_revert(vault, *, target_file, new_content, proposal_id, kind)` helper 真调三函数（非 stub），并 `_copy_case_and_fix_clock_from` 补 `monkeypatch.setattr("aiwiki.execution.l3_proposals.utc_now", ...)`（module-local binding via `from aiwiki.app_utils import utc_now`）+ `monkeypatch.delenv("AIWIKI_DISABLE_AUTOMATION", raising=False)`（kill switch 不应影响 acceptance）。`test_acceptance_no_stop_line_violations` 给 D 子树加精确例外（`rel == case or rel.startswith(case + "/")`），仅放过 `l3-proposal-apply` 一词，其余四个 forbidden 词全树仍禁。测试除 7 golden 字节相等外，断言 hash invariants：`revert.restored_hash == apply.before_hash` + `apply.after_hash != revert.restored_hash`。acceptance 16 → 17；verify PASS（unit + 17 acceptance + 92% coverage + ruff clean）；fresh oracle qa-review 一轮 REQUEST CHANGES 三个 finding 全修后 APPROVE（ses_1e535c7a9ffeuU1bt4wGevlX0m）。**Gap A/B/C/D（PROGRESS slim / drift_scan / drop_url / L3 proposal apply-revert）全部收口**。
- 2026-05-12 (C drop_url acceptance Gap C 收口)：`drop_url` end-to-end materialization 上提到 acceptance 层。`tests/fixtures/acceptance/C/case_drop_url/` 新建（空 vault root + 4 byte-stable golden：`raw/inbox/agent-architecture-survey.md` / `wiki/indexes/log.md` / `.aiwiki/state/runtime-history.jsonl` / `.aiwiki/state/audit.jsonl`）。`tests/acceptance/case_runner.py` 加 `_run_drop_url(vault, monkeypatch, *, url, title=None, fetched=None)` helper（patch `aiwiki.drop._fetch_url` 唯一外部边界 + 默认 fetched payload `image_urls=[]` 避开 asset 不稳路径）。`test_drop_url_writes_raw_note_and_logs` 断言 result dict shape (material/note_path/final_url/asset_paths=[]) + 4 golden。范围窄：drop_pdf/image/repo 由现有 unit test 覆盖。acceptance 15 → 16；verify PASS（2125 unit + 16 acceptance + 92% coverage + ruff clean）；fresh oracle qa-review APPROVE 一次过（ses_1e570f42affeFj4yatk0zyZ6QF）。
- 2026-05-12 (B drift/aging acceptance Gap B 收口)：`drift_scan` 三个扫描器（stale judgments / changed evidence / dependency breaks）上提到 acceptance 层。`tests/fixtures/acceptance/B/case_drift_scan/` 新建，含 1 stale judgment + 1 elixir w/ drifted+stale citation_snapshots（`raw/evidence.md` digest 故意错 + `raw/missing.md` 不存在）+ 1 elixir w/ broken `derived_from`，golden 覆盖 `.aiwiki/state/{drift-aging.json, signals.jsonl, runtime-history.jsonl, audit.jsonl}` 字节稳。`tests/acceptance/case_runner.py` 加 `_run_drift_scan(vault, monkeypatch, now=...)` helper（函数级直调避开 CLI 不存在 + nightly 无关 determinism；显式 `delenv AIWIKI_STALE_JUDGMENT_DAYS` + `monkeypatch uuid.uuid4 version=4` 在 helper 内部，避免 leak 到 collector path）。acceptance 14 → 15；verify PASS（2125 unit + 15 acceptance + 92% coverage + ruff clean）；oracle qa-review APPROVE after fixes（ses_1e58d3070ffemOz03PdcVDGNxv）。
- 2026-05-12 (D-3 acceptance Gap P1 收口)：D-3「金丹复利」从 unit-only 上提到 acceptance 层。新增 fixture `tests/fixtures/acceptance/D3/case_elixir_stage3_compounding/`，跑 5 步 CLI 链（alchemy-start/distill/finalize/promote + trace up），断言新丹 `derived_from` 含旧丹 + derived anchor / promote bundle 含 counter_evidence / `trace up` 递归 parents / 3 state JSONL byte-stable。`tests/acceptance/case_runner.py` 加 monkeypatch 把 `app_execution.datetime` + `execution.alchemy.datetime` 也 patch 成 `_FixedDateTime`（之前只 patch `utc_now`，promote receipt epoch_ms 走 `datetime.now(timezone.utc)` 泄漏）。acceptance 13 → 14；verify PASS（2125 unit + 14 acceptance + 92% coverage + ruff clean）；oracle qa-review PASS（ses_1e5c14353fferejeB7qf8aa4uG）。
- 2026-05-12 (SC-010 Structural Consolidation Sweep 完结)：清理 SC-008 fresh oracle 两条 non-blocking follow-up。`src/aiwiki/drop.py` `_LOGGER` 定义上移到模块顶部 constants 区；`tests/test_drop_phases.py` 新增 integration test `test_drop_pdf_finally_cleanup_logs_warning_on_rmtree_failure`（prefix-selective rmtree wrapper 不污染测试环境）。verify PASS；fresh oracle qa-review PASS（ses_1e5fbd4cfffek862Ps5zD09BDA）。**Structural Consolidation Sweep（SC-001..SC-010）整体收官**。完整 SC-001..SC-010 详情见 `archive/rounds/structural-consolidation-sweep.md`。
- 2026-05-11 Product UX Rectification Sweep (R97~R108 EP-001..EP-007) 详情见 `archive/rounds/ep-001-ep007-product-ux-sweep.md`。
- 2026-05-09：Product Shell + runtime LLM provider profile 整理完成；默认 route `opencode-api/deepseek-v4-pro`；NVIDIA NIM fallback 保持 `nvidia-nim-api/openai/gpt-oss-120b`；verify PASS（2001 unittest + 13 acceptance，coverage 92%），Product Shell Jest PASS（55 tests）。

## Milestone Quick Index

> 每行只保留世代 / Milestones / 一句主旨 / 状态。完整实现细节、Stop Lines、Residual Risks 见 `archive/rounds/milestone-quick-index-detail.md`。

| 世代 | Milestones | 状态 |
|---|---|---|
| **P4 Dogfood-driven** (2026-04-28) | P4-1a~1d, P4-2~6, P4-9, P4-11, P4-15 | ✅ 全部 done |
| **D 系列** (2026-04-30) | D-1~D-4 + D-3 R1 + D-4 v0/v1 | ✅ 全部 done |
| **P4-INV** (Round 57-59) | P4-INV-1~4 | ✅ 全部 done |
| **Post-R61 改进** (2026-05-03) | harness 增量升级 / QA review 启用 / plans-merge / Round 62/63 UI Polish | ✅ Round 62/63 done / 其余 🔄 |
| **Round 64-66 UX Earnest** (2026-05-03) | 命名去时间戳 / 拖放修复 / 面板精简 / L3 自动采纳 / 图谱锚点 / 导航简化 | ✅ 全部 done |
| **Round 67 Auto-adopt Hardening** (2026-05-04) | judgment review / L3 audit / nightly aggregation / strict JSONL | ✅ done (`6711efd`) |
| **Round 67.5 Acceptance Fixture Refresh** (2026-05-04) | M6.1b prompt_hash drift refresh | ✅ done (`284f8af`) |
| **Round 68 Progress Slimming** (2026-05-04) | PROGRESS 三层瘦身 / rounds archive / stop_line_audit lint | ✅ done (`2c408f9`) |
| **Round 69 Atomic State I/O** (2026-05-04) | `atomic_write_text` + `atomic_append_jsonl` helpers / 4 saver 替换 | ✅ done (`7ee3ab8`) |
| **Round 70 Receipt JSONL 事务化** (2026-05-04) | 12 JSONL writers 全量原子化 / mm revert 双 receipt + `reverts/` 子目录 | ✅ done (`950f291`) |
| **Round 71 Fetch & Path 安全** (2026-05-04) | `safe_fetch` + `safe_resolve_within` / SSRF / repo symlink 跳过 / Playwright route 拦截 | ✅ done (`a6074b9`) |
| **Round 72 Lock 高优先级缺锁补齐** (2026-05-04) | drop_* 五入口 + nightly_health + receipt_history 全加 `runtime_write_lock` | ✅ done (`addd53d`) |
| **Round 73 LLM/notify HTTP 安全** (2026-05-04) | `safe_fetch` 扩 POST + headers + redirect strip auth / llm.py + notify.py 切换 | ✅ done (`c0cf944`) |
| **Round 74 L3 事务化 + audit auto-revert** (2026-05-04) | `apply_l3_proposal` 后半 5 步 TX / `L3PostApplyAuditError` 携带证据 / auto_reverted runtime_history | ✅ done (`b6a64f5`) |
| **Round 75 receipt_history TX** (2026-05-04) | `_durable_truncate` / `ReceiptHistoryAuditError` + `ReceiptHistoryRollbackError` / R74 残余关闭 | ✅ done |
| **Round 76 runtime_history TX + audit-mirror 上移** (2026-05-04) | `_durable_truncate` + AuditMirror* 上移 `app_utils` / R75 API 保持 | ✅ done |
| **Round 77 LLM receipt TX** (2026-05-04) | `_append_llm_receipt` snapshot-then-rollback / 复用 AuditMirror* | ✅ done |
| **Round 78 age audit single-file TX** (2026-05-04) | `_durable_restore_or_remove` / `_write_age_audit` snapshot bytes | ✅ done |
| **Round 79 auto_adopt 顶层 lock** (2026-05-04) | 4 个 `auto_adopt_*` 入口加 `@runtime_write_operation` / reentrant | ✅ done |
| **Round 80 safe_fetch response close** (2026-05-04) | urlopen response 用 `with` 包；R71-R73 残余关闭 | ✅ done |
| **Round 81 citation snapshot path guard** (2026-05-04) | `safe_resolve_within` + wiki/judgments\|decisions 白名单 | ✅ done |
| **Round 82 citation revert guard 对称收口** (2026-05-04) | `revert_machine_memory_action` citation 分支复用同 helper | ✅ done |
| **Round 83 safe_fetch DNS pinning + host allowlist** (2026-05-05) | pinned-IP connect / proxy 禁用 / SNI 保留 / opt-in allowlist | ✅ done |
| **Round 84 fail-soft 收口 + 事实层 strict read 迁移** (2026-05-05) | notify 双层 fallback / `load_runtime_history_strict` / 6 callers 切 strict | ✅ done (`c94cc87`) |
| **Round 85 history JSONL strict migration** (2026-05-05) | execution policy/receipt history strict variants / fact-layer 切 strict | ✅ done (`ea08d6e`) |
| **Round 86 safe_fetch 多 IP fallback** (2026-05-05) | R83 pinning 可用性回归修复 / resolver 顺序循环 TCP | ✅ done (`b345ecf`) |
| **Round 87 R85/R86 non-blocking 小补丁** (2026-05-05) | history strict 测试断言收紧 / HTTPS fallback `wrap_socket` 单次 | ✅ done (`0891a44`) |
| **Round 88 PM-UX 三件套** (2026-05-05) | Today 空态 CTA / "处理中" 卡 state machine / 面板文案白话化 | ✅ done |
| **Round 89 PM/UX 信任闭环 + 文案统一** (2026-05-05) | pending 持久化 / 状态机两段式 / Today→今天 / groupSpecs 中文化 | ✅ done |
| **Round 90 提交→状态→结果 闭环** (2026-05-05) | Today "刷新炉子" + last-updated / pending 行动卡 / done TTL 7d | ✅ done |
| **Round 91 Advanced 抽屉信息架构** (2026-05-05) | dev banner 外置 / 三组可折叠 section + 持久化 | ✅ done |
| **Round 92 Alchemy Apply Lock + Receipt Atomicity** (2026-05-05) | runner/alchemy.py 7 入口加 lock / 7 receipt write 切 atomic / preview 透传 | ✅ done |
| **Round 92.1 Alchemy Lock Audit (Tight)** (2026-05-05) | 3 残余 unlocked writers 加 decorator / `_walk_preview_lock_status` 双函数 | ✅ done |
| **Round 92.2 Machine-Memory Action TX** (2026-05-05) | `apply/revert_machine_memory_action` snapshot/rollback / 3 apply_mode / `MachineMemoryActionReceiptError` | ✅ done |
| **Round 92.3 Drop Input Safety** (2026-05-05) | 本地 ingestion 边界：PDF 50MB / image 25MB + MIME 白名单 / repo max_files normalize | ✅ done |
| **Round 92.4 Protocol/Manifest 单文件原子写** (2026-05-05) | 6 处 protocol/manifest JSON 切 `atomic_write_text` | ✅ done |
| **Round 92.5 Cache Layer Fail-Soft** (2026-05-05) | SQLite query cache 8 boundary fail-soft / 5 compile build-state save fail-soft | ✅ done |
| **Round 92.6 Lock Audit Wide** (2026-05-05) | 7 个 SoT/state writer 入口加 lock 保护（Tight 5 + Standard 2 conditional） | ✅ done |
| **Round 92.7 Deploy/Service Defaults Hardening** (2026-05-05) | systemd installer 默认 deny-by-default / `AIWIKI_VAULT` 强制 / lock timeout / remote repo opt-in | ✅ done |
| **Round 92.8 Feed Parity (Universal Input)** (2026-05-12) | `recent_raw_inputs` summary + plugin reconcile / extractPrimaryPath 扩展 / 不新增 FeedKind | ✅ done |
| **Round 92.9 DETERMINISM compile gate** (2026-05-10) | compile log timestamp-insensitive gate | ✅ done (`f153589`) |
| **Round 92.10 TEST SPLIT test_app.py** (2026-05-10) | 7984 行拆 7 个 focused 文件 / 总 test count 不变 | ✅ done (`00b41ad`) |
| **Round 92.11 PERF Benchmark** (2026-05-10) | measurement-only `compile_wiki` benchmark / 不接 CI gate | ✅ done (`0944726`) |
| **Round 93.0 autonomy_policy fail-closed** (2026-05-11) | malformed → `CorruptStateError` / `load_autonomy_policy` 切 strict | ✅ done (`7e43e4e`) |
| **Round 93.1 material-archive TX 雏形** (2026-05-11) | `apply/revert_archive_action` 早期 receipt-tier TX；R95.1 接续 | ✅ done (`37c9341`) |
| **Round 93.2 compile-artifact 原子写** (2026-05-11) | compile pipeline 派生 artifact 全切 `os.replace` 原子路径 | ✅ done (`6d2cee6`) |
| **Round 93.3 ingest subprocess timeout** (2026-05-11) | `drop.py` ingest subprocess 加 `timeout=` / `IngestionTimeoutError` | ✅ done (`137aa4e`) |
| **Round 94.0 automation.json 原子写** (2026-05-11) | `atomic_write_text` + `fsync=True` / `load_automation_state` strict + corrupt 回收 | ✅ done (`0ddc2c7`) |
| **Round 94.1 drift_scan → append_runtime_history** (2026-05-11) | drift_scan 改走 `append_runtime_history` 复用 R76 TX + audit-mirror | ✅ done (`6bf39e0`) |
| **Round 94.2 ingest_source raw/ atomic + orphan tmp 跳过** (2026-05-11) | raw write 切 atomic + fsync / raw scanner 过滤 `*.tmp.*` | ✅ done (`4ba29e9`) |
| **Round 94.3 mm-action read-then-write fail-closed** (2026-05-11) | `apply/revert_machine_memory_action` 切 `load_json_document_strict` | ✅ done (`a85f599`) |
| **Round 94.4 concept rewrite apply/revert TX** (2026-05-11) | file + state pair TX / reversed write-order rollback | ✅ done (`ecef27f`) |
| **Round 94.5 L3 Proposal apply/revert TX** (2026-05-11) | file + receipt + state triple TX / `L3{Apply,Revert,Reject}{ReceiptError,HalfWriteError}` | ✅ done (`b093241`) |
| **Round 95.1 archive apply/revert TX** (2026-05-11) | receipt+state pair TX / phase-2 derived audit swallow-with-warning（设计保留） | ✅ done (`a49ead8`) |
| **Round 95.2 drop.py raw note + asset atomic** (2026-05-11) | raw note + asset 全切 `atomic_write_text` / `atomic_write_bytes` + `fsync=True` | ✅ done (`0731823`) |
| **Round 95.3 L3 Phase-2 False-Write Truncate** (2026-05-11) | `_durable_truncate(path, snapshot_size)` 截回 commit 前长度 | ✅ done (`70ff772`) |
| **Round 95.4 Nightly Audit Reconciler** (2026-05-11) | `reconcile_execution_receipts` nightly 检 false-success / 仅扫 `revert` | ✅ done (`3679079`) |
| **Round 96.0 alchemy receipt persistence TX** (2026-05-11) | `_persist_receipt_transactionally` 收口 receipt-tier TX 范式 | ✅ done (`4b3e32f`) |
| **Round 96.1 alchemy review_apply TX** (2026-05-11) | `run_alchemy_review_apply` receipt-tier TX | ✅ done (`8a4dd91`) |
| **Round 96.2 alchemy lane primitive Receipt TX** (2026-05-11) | `_run_receipted_lane_primitive` wrapper TX | ✅ done (`44100e2`) |
| **Round 96.3 alchemy distill/propose_apply TX** (2026-05-11) | `run_alchemy_distill_proposal_apply` / `run_alchemy_propose_apply` TX | ✅ done (`1db7925`) |
| **Round 96.4 create_l3_proposal TX** (2026-05-11) | state + page + history + audit + wiki-log 全包 receipt-tier TX | ✅ done (`4df35ea`) |
| **Round 96.5 mm_action_batch Receipt-Tier TX** (2026-05-11) | `apply_machine_memory_actions_batch` snapshot/rollback 全包 | ✅ done (`5aa4a92`) |
| **Round 96.6/96.7 L3 Phase-2 Audit Swallow DROP** (2026-05-11) | fresh oracle pre-design DROP；决议归档 `.codex/contracts/archive/R96.6/96.7-*.md` | ❌ dropped |
| **Round 96.8 apply_l3_proposal Rollback 跨流一致** (2026-05-11) | 外层 rollback 顺序 reversed write-order；补 `runtime_history_path` 还原 | ✅ done (`f2c9f5a`) |
| **Round 96.9 L3 Apply Phase-2 Audit Reconciler 扩流 DROP** (2026-05-11) | fresh oracle pre-design DROP；决议归档 `.codex/contracts/archive/R96.9-*.md` | ❌ dropped |
| **Round 97 Decision-grade Report Skeleton** (2026-05-11) | `output_format=report` 升级到 6 区块固定骨架 + `_validate_report_sections` | ✅ done |
| **Round 98.1 Decision-grade Report Bullet Minimums** (2026-05-11) | 每段 `- ` bullet 下限 + 拒 `_LLM:` placeholder 残留 / fence-aware bullet counter | ✅ done (`ae9e19f`) |
| **Round 98.2 Report Citation Integrity** (2026-05-11) | `## 引用` dedup + body ⊆ citations 校验 / fence-aware path 提取 | ✅ done |
| **Round 98.3 Report Strictness Hardening** (2026-05-11) | Phase 0 unclosed fence 拒绝 + Phase 1.5 duplicate required H2 拒绝 | ✅ done |
| **Post-P4 D-2 PROGRESS Slim** (2026-05-12) | Round 73-92.7 + Milestone Quick Index detail 切档归档 / PROGRESS 117KB→18.5KB | ✅ done (`c472677`) |
| **Post-P4 B drift_scan Acceptance** (2026-05-12) | drift_scan 三扫描器函数级 acceptance / 4 byte-stable golden / 15 cases | ✅ done (`54d7e2c`) |
| **Post-P4 C drop_url Acceptance** (2026-05-12) | drop_url end-to-end materialization acceptance / 4 byte-stable golden / 16 cases | ✅ done (pending commit) |
| **Post-P4 D-3 Acceptance Gap P1** (2026-05-12) | 金丹复利 5 步 CLI 链 acceptance / 3 state JSONL byte-stable | ✅ done (`8cd2b25`) |
| **Post-P4 B Drift/Aging Acceptance Gap B** (2026-05-12) | drift_scan 三扫描器函数级 acceptance / 4 artifact byte-stable | ✅ done |


## 状态 — 当前活跃 3 轮

### Round 92.8 — Feed Parity (Universal Input pending closure) — 完成

- **目的**：闭合 P2 fresh oracle scoping 排为 SHIP first 的用户可见 feed-parity bug——Universal Input 投料（URL/PDF/image/repo/note）成功后，pending 卡长期停在 "已接收，等待生成报告"，因为 (1) `drop_*` 返回 payload 含 `note_path` 但 plugin `extractPrimaryPath()` 只识别 `path/output_path/receipt_path/state_path/index_path/report_path`；(2) shell summary 不暴露 raw ingest，`reconcilePendingSubmissions()` 永远 miss raw drop。
- **实现**：
  - `src/aiwiki/app_shell/summary.py:145-166`：新增 `_build_recent_raw_inputs(root, *, limit=8)` helper，从 `load_runtime_history(root)`（NON-strict，关键：strict 会 raise 破坏 fail-soft）过滤 `event_type == "raw-added"`，倒序取 8 条，映射 `stored_path / original_path / source_type / title / occurred_at / protocol`，全部 `str() or ""` coercion；body wrap broad `try/except (OSError, ValueError, TypeError, json.JSONDecodeError, KeyError)` 返回 `[]` 不 raise。`build_shell_summary` 在 line 241 预计算，line 312 dict literal 插入 `"recent_raw_inputs": recent_raw_inputs` 紧跟 `recent_runs`。
  - `src/aiwiki/today_feed.py:415-440`：新增 `_build_raw_input_entries(summary, today_date)` helper，仅当 `recent_raw_inputs` 是 list 时迭代；空 `stored_path` 跳过；`_date_part(occurred_at) != today_date` 跳过；构造 `FeedEntry(kind="action", title=f"已投料：{title or original_path or stored_path}", summary=f"已接收 {source_type or '材料'}，等待编译/刷新", target=stored_path, ...)`。`build_today_feed` line 82-83 在 `_build_action_entries` 之后追加。**关键**：未新增 FeedKind，复用 `kind="action"`（priority 6 最低），避免 schema 改动。
  - `.obsidian/plugins/furnace-product-shell/src/plugin_helpers.js:178`：`extractPrimaryPath()` `candidateKeys` 数组扩展为 `["path", "output_path", "receipt_path", "state_path", "index_path", "report_path", "note_path", "stored_path", "asset_path"]`，**新增 keys 排末尾**保留既有 ask/run path 优先级。
  - `.obsidian/plugins/furnace-product-shell/src/plugin.js:2150-2213`：`reconcilePendingSubmissions()` `rawCands = summary.recent_raw_inputs`，guard 允许 raw-only proceed；`matchAgainst` `fields` 数组扩展 `stored_path / original_path / note_path`；hit 顺序保持 outputs → receipts → raw fallback；命中 raw → `target = "raw"` + `targetPath = String(hitCand.stored_path || hitCand.path || "")` → `markPendingSubmissionDone(entry.id, "raw", targetPath)`。`markPendingSubmissionDone` 不 whitelist target 字符串（plugin.js:2085-2095 直接 `entry.reconcileTarget = String(reconcileTarget)`），"raw" 不 no-op。注释 comment 同步更新（plugin.js:2082）。
  - `.obsidian/plugins/furnace-product-shell/src/today_feed.js:58-59,286-305`：JS mirror 同步新增 `buildRawInputEntries`，被 plugin 加载，与 Python 渲染契约一致。
  - `tests/test_feed_parity.py` 10 测试 + `load_tests` bridge：4 summary（drop history 收录 + 全字段映射 / 无 history 空 / 损坏 JSONL fail-soft 返回 [] / 12 events 限 8 条 most-recent-first / 混 event_type 仅 raw-added）+ 3 today_feed（today date 渲染 kind=action / 昨天跳过 / 空 stored_path 跳过）+ 2 JS grep（plugin_helpers.js candidateKeys 含三新 key / plugin.js reconcile 含 `recent_raw_inputs` + `target = "raw"` + `stored_path` token）。
- **验证**：`bash scripts/verify.sh` PASS — **1848 unittest**（+10）+ 13/13 acceptance + branch coverage 92% / ruff + compileall clean。
- **qa-review**：fresh oracle session `ses_2055cf69dffeSzSAEpOCXG597D` PASS（首次 retry，非阻塞 concerns：JS 测试是 grep 级 vs 真执行级 / `markPendingSubmissionDone` 注释 comment 已 fix）。End-to-end 链路 trace 完整：user → Universal Input → `aiwiki drop <payload>` → `dispatch.py` rewrite → `drop_*` → `_append_raw_added_history` JSONL → `build_shell_summary` `recent_raw_inputs` → plugin reconcile `rawCands` → match via `stored_path` → `markPendingSubmissionDone("raw", stored_path)`。
- **Stop Lines**：0 `drop_*` 返回签名改 / 0 CLI signature / 0 `runtime_history` 写语义 / 0 schema / 0 lock primitive / 0 新 FeedKind（`today_feed.FeedKind` Literal 不变）/ 0 `general` default protocol / 0 LLM defaults / 0 notify defaults / 0 `safe_fetch` allowlist / 0 Universal Input 命令字符串（plugin.js:2239 仍 `["drop", normalizedPayload]`）。
- **Migration**：纯加层；既有 dogfood vault 重启 plugin 后 raw drop pending 卡可被 reconcile 自动 mark done；既有 ask/run path 优先级不变。
- **Residual Risks**：JS grep 测试只覆盖 token 存在，不覆盖顺序 / 行为变化 → 未来 plugin 大改时回归防护偏弱；可在后续 round 补 node 真执行测试（DEFER 不在本轮 scope）。
- **归档**：contract `.codex/contracts/archive/round-92-feed-parity.md`。


---

> 更早的 round 详情请参考 `archive/rounds/round-*.md` / `archive/rounds/p4-*.md`，或读 `archive/rounds/index.json`。
