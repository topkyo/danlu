"""Lint and nightly health helpers.

Import owner modules directly (``app_linting.core``, ``app_linting.nightly``,
``app_linting.repair``, …). This package ``__init__`` is documentation-only —
no re-export façade and no compat monkeypatch seams.
"""
