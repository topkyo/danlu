"""Product Shell summary, rendering and control surfaces.

Import owner modules directly (``app_shell.summary``, ``app_shell.meta``,
``app_shell.controls``, …). This package ``__init__`` is documentation-only —
no re-export façade and no compat monkeypatch seams.
"""
