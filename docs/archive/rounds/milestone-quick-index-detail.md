# Milestone Quick Index — Full Detail

- **Archived from**: `PROGRESS.md` 第 28-101 行（Milestone Quick Index 表完整原文）
- **Reason**: D-2 PROGRESS slim 收尾，把每行 milestone 描述里塞的完整 round 总结、Stop Lines、Residual Risks 切到这里，PROGRESS.md 表保留一句话主旨 + 状态
- **Pointer back**: `PROGRESS.md` `Milestone Quick Index` 表 / `archive/rounds/index.json` round_id `milestone-quick-index-detail`

---

## Milestone Quick Index

| 世代 | Milestones | 状态 |
|---|---|---|
| **P4 Dogfood-driven** (2026-04-28) | P4-1a~1d, P4-2~6, P4-9, P4-11, P4-15 | ✅ 全部 done |
| **D 系列** (2026-04-30) | D-1~D-4 + D-3 R1 + D-4 v0/v1 | ✅ 全部 done |
| **P4-INV** (Round 57-59) | P4-INV-1~4 | ✅ 全部 done |
| **Post-R61 改进** (2026-05-03) | harness 增量升级 / QA review 启用 / plans-merge / **Round 62/63 UI Polish** | ✅ Round 62/63 done / 其余 🔄 |
| **Round 64-66 UX Earnest** (2026-05-03) | 文件命名去时间戳 / 拖放修复 / 面板精简 / L3 自动采纳 / 图谱锚点链接化 / 导航树简化 / ask UI 锚点收敛 | ✅ 全部 done |
| **Round 67 Auto-adopt Hardening** (2026-05-04) | judgment review / L3 audit / nightly aggregation / strict JSONL load | ✅ done (`6711efd`) |
| **Round 67.5 Acceptance Fixture Refresh** (2026-05-04) | M6.1b prompt_hash drift refresh / fixture helper 文档化 | ✅ done (`284f8af`) |
| **Round 68 Progress Slimming** (2026-05-04) | PROGRESS 三层瘦身 / rounds archive / index.json / stop_line_audit lint | ✅ done (`2c408f9`) |
| **Round 69 Atomic State I/O Foundation** (2026-05-04) | atomic_write_text + atomic_append_jsonl helpers / 4 saver 替换 / 21 unit tests / R69.5 fixture refresh 归并 | ✅ done (`7ee3ab8`) |
| **Round 70 Receipt JSONL 事务化 + Revert 双 receipt** (2026-05-04) | 12 JSONL writers 全量原子化 / atomic_append_line 原语 / machine_memory revert 双 receipt + reverts/ 子目录 / receipt_path override / mock seam 清除 / R70.5 fixture refresh 归并 | ✅ done (`950f291`) |
| **Round 71 Fetch & Path 安全** (2026-05-04) | safe_fetch + safe_resolve_within helpers / drop.py SSRF (private IP 表 + IPv4-mapped + redirect 复检) / 越界检查 / repo symlink 跳过 / Playwright page.route subresource 拦截 / 22 安全测试 | ✅ done (`a6074b9`) |
| **Round 72 Lock 高优先级缺锁补齐** (2026-05-04) | drop_* 五入口 + nightly_health + append_execution_receipt_history 全加 runtime_write_lock / 抽 _unlocked helper 保签名 / 3 lock coverage 测试 | ✅ done (`addd53d`) |
| **Round 73 LLM/notify HTTP 安全** (2026-05-04) | safe_fetch 扩展 POST + headers + redirect strip auth / llm.py 三处 + notify.py webhook 切换 / _LLM_MAX_BYTES 10MB + _NOTIFY_MAX_BYTES 1MB / 7 安全测试 | ✅ done (`c0cf944`) |
| **Round 74 L3 事务化 + audit 失败 auto-revert** (2026-05-04) | apply_l3_proposal 后半段 5 步事务化 / target byte-equal write_bytes(snapshot) / _persist_l3_proposal_page atomic_write_text + proposal deep-copy revert / L3PostApplyAuditError 携带 failed_step+before/after_hash+target_file+action_id+deleted_receipt_path / auto_adopt_l3 标记 auto_reverted 写完整 runtime_history 严重事件 / L3RevertError 二级失败 / 9 测试 | ✅ done (`b6a64f5`) |
| **Round 75 receipt_history 事务化** (2026-05-04) | append_execution_receipt_history 改 snapshot-then-rollback / `_durable_truncate` (open r+b + truncate + flush + fsync) / ReceiptHistoryAuditError + ReceiptHistoryRollbackError / R74 partial 残余风险关闭 / 5 unit + 1 集成 | ✅ done |
| **Round 76 runtime_history 事务化 + audit-mirror helper 上移** (2026-05-04) | `_durable_truncate` + AuditMirror* 上移 app_utils / app_execution alias 保持 R75 API / append_runtime_history snapshot-then-rollback + runtime lock / 5 unit | ✅ done |
| **Round 77 LLM receipt 事务化** (2026-05-04) | `_append_llm_receipt` snapshot-then-rollback / 复用 AuditMirror* + `_durable_truncate` / 不扩 lock 边界 / 5 unit | ✅ done |
| **Round 78 age audit single-file 事务化** (2026-05-04) | `_durable_restore_or_remove` / `_write_age_audit` snapshot bytes + restore/remove / audit-mirror 主线收口 / 5 unit | ✅ done |
| **Round 79 auto_adopt 顶层 lock 收口** (2026-05-04) | 4 个 `auto_adopt_*` 顶层入口加 `@runtime_write_operation` / reentrant lock 验证 / 4 unit | ✅ done |
| **Round 80 safe_fetch response close** (2026-05-04) | `safe_fetch` urlopen response 用 with 包住 read+return / 3 close-path tests / R71-R73 残余关闭 | ✅ done |
| **Round 81 citation snapshot path guard** (2026-05-04) | `citation-snapshot-refresh` 加 `safe_resolve_within` + wiki/judgments|decisions 白名单 / 4 unit | ✅ done |
| **Round 82 citation revert guard 对称收口** (2026-05-04) | `revert_machine_memory_action` citation 分支复用同 helper / R81 follow-up 单点 1 行 | ✅ done |
| **Round 83 safe_fetch DNS pinning + host allowlist** (2026-05-05) | custom HTTP/HTTPS connection pinned-IP connect / proxy 禁用 / SNI 保留 / opt-in allowlist via env / 11 unit | ✅ done |
| **Round 84 fail-soft 降级路径收口 + 事实层 strict read 迁移** (2026-05-05) | notify.py 双层 fallback-of-fallback logger.warning + sanitized metadata / `load_runtime_history_strict` / 6 个事实层 best-effort→strict 切换 / CorruptStateError 自然传播到 CLI / 8 strict 迁移测试 + notify 双层 warning 测试 | ✅ done (`c94cc87`) |
| **Round 85 history JSONL strict migration** (2026-05-05) | execution policy/receipt history JSONL best-effort loader 显式 warning / strict variants / fact-layer callers 切 strict / 8 migration tests | ✅ done (`ea08d6e`) |
| **Round 86 safe_fetch 多 IP fallback** (2026-05-05) | R83 pinning 可用性回归修复 / `_PinnedHTTP[S]Connection` 按 resolver 顺序循环 TCP / 全失败抛 OSError 不混入 FetchPolicyError / handler 接 list / 4 fallback tests | ✅ done (`b345ecf`) |
| **Round 87 R85/R86 non-blocking 小补丁清理** (2026-05-05) | history strict 测试 path:N 断言收紧 / receipt strict missing+non-dict edge tests / HTTPS fallback wrap_socket 单次 + server_hostname 锁定 / redirect 后 IP list 重建测试 / 实现 0 改动 | ✅ done (`0891a44`) |
| **Round 88 PM-UX 三件套** (2026-05-05) | Today 空态 CTA「投一份材料」聚焦 universal input / 提交后"处理中"卡片（runtime-only push/done/failed/reset state machine + reconcile 时间戳门槛 + 字段扩展 + 短指纹 exact + 超窗保留 + dedupe）/ 用户层面板文案白话化（plugin/constants/render_execution/render_review/render_primitives/health-state，"shell-summary.json" → "数据还没准备好"等）/ pending 卡片样式 + pulse / 12 契约测试 / oracle qa-review 2 轮全 PASS | ✅ done |
| **Round 89 PM/UX 信任闭环 + 文案统一** (2026-05-05) | pending 持久化（settings.persistedPendingSubmissions + serialize/hydrate + TTL 24h running→failed + cap 8）/ 状态机两段式（running → received "已接收，等待生成报告" → done(target=outputs\|receipts) "报告已生成"\|"已记录"）/ markReceived/markDone 互斥防御（防 reconcile race & 重复 setTimeout）/ 失败卡通用 hint + 重试统一收口 markReceived / Today→今天 闭环（标题 + 3 处 input hint）/ groupSpecs 中文化（新报告/系统动态/需要你确认/已完成/下一步建议）/ Advanced drawer "开发者诊断信息"分隔横条 / 23 pending 测试 + 9 today_feed 测试 + 48 jest / oracle qa-review 2 轮全 PASS | ✅ done |
| **Round 91 Advanced 抽屉信息架构** (2026-05-05) | dev banner 外置（不再嵌外层 Advanced details）/ 三组可折叠 section（系统状态/运行与历史/开发者操作）/ 折叠态持久化 `settings.advancedSectionsExpanded.{status,history,devops}` 默认全收 / `getAdvancedSectionExpanded` + `async setAdvancedSectionExpanded`（own-object 防 DEFAULT_SETTINGS 浅拷贝污染）/ summary helpers：协议 {p}·LLM {l}·同步 {s} / 最近运行 {n} 条·待审 {r}·待执行 {e} / 编译·同步·协议切换·日志 / renderHistorySectionBody 含 Recent Runs+Review+Execution 三入口 + Latest LLM run 摘要 / renderLegacyAdvancedPanel 去外层 details body 平铺 / 11 翻译键 + .furnace-advanced-section* 样式 / 99 pytest+13 acceptance+48 jest 全过 / oracle qa-review 2 轮 PASS | ✅ done |
| **Round 90 提交→状态→结果 闭环** (2026-05-05) | Today 顶部"刷新炉子"+last-updated（getLastSummaryRefreshLabel 4 档：刚刚/N 分钟前/N 小时前/N 天前 + clamp 未来时间）/ pending received/running 卡加"刷新状态"按钮 / done 卡不再 4s 自动消失 → 行动卡（outputs:打开报告 / receipts:查看回执 / 完成）/ openPendingDoneTarget helper：openWorkspacePath 改返 boolean，失败退化到 Outputs Hub / Recent Runs / HOME.md + Notice / refreshShellSummaryCommand 始终 fallback loadShellSummaryFromDisk（保证 reconcile）/ done TTL 7d hydrate（finishedAt 缺失退 startedAt）/ reconcilePath 进序列化 / +10 R90 测试（46 pending+today / 13 acceptance / 48 jest 全过）/ oracle qa-review 3 轮 PASS | ✅ done |
 | **Round 96.9 L3 Apply Phase-2 Audit Reconciler 扩流 (DROP)** (2026-05-11) | 候选：扩 `audit_reconciliation.SUCCESS_OPERATIONS` 让 nightly 扫 `apply` 行检出 R96.8 phase-2 audit swallow 残留 / fresh oracle pre-design DROP：现 reconciler 扫 `execution-receipts.jsonl` 行，phase-2 swallow 形态压根不写 audit-only false-success 行 → 单行 `frozenset({"apply"})` 是 ritual 不解决 gap / 0 改动 / 决议归档 `.codex/contracts/archive/R96.9-AUDIT-RECONCILER-APPLY-SCOPE-DROP.md` | ❌ dropped |
| **Round 98.1 Decision-grade Report Bullet Minimums** (2026-05-11) | R97 之后给 `output_format == "report"` 加每段 `- ` bullet 下限：`关键证据 ≥3` / `反证与不确定性 ≥1` / `行动建议 ≥1` / `下次观察信号 ≥1` / `引用 ≥1`；`## 结论` 不计（段落不是 bullet 列表）/ 严格拒绝 `_LLM:` placeholder 残留（fence 外）/ `src/aiwiki/runner/prompts.py` 新 `_REPORT_SECTION_BULLET_MINIMUMS` + `_REPORT_PLACEHOLDER_MARKER` 常量；扩 `_validate_report_sections` 三遍扫描（H2 顺序 / `_LLM:` 拒绝 / per-section bullet count）+ 新 `_count_report_bullets()`（column-0 `- `、fence 外、跳过空 bullet/numbered/sub-bullet/`_LLM:`）/ 错误消息固定模板 `"Report section <heading> needs at least <n> '- ' bullets; found <m>"` 与 `"Report contains unfilled placeholder marker '_LLM:'"` / R97 self-roundtrip 测试改语义为 skeleton-only structure 检查（不再过 filled validator，因 skeleton 含 `_LLM:`）/ `tests/test_decision_grade_report.py` 加 9 个新 case（bullet 不够 / 0 bullet / numbered list / sub-bullet / fence 内 bullet / `_LLM:` 残留 / fence 内 `_LLM:` 允许 / 空 bullet / 常量锁）共 22 cases / 5 个 `_VALID_REPORT_BODY` stub 补齐每段 bullet（test_runner / test_execution / test_app_runtime / test_app_shell + test_app_misc 新加本地常量替换 ask_question skeleton 回灌）/ `test_runner.py:1641` inline `valid_report` 补 bullet / acceptance fixture `M6.1b/case_happy_run_ask/backend_responses/0001-7c70bf0b9482cc29.json` 补 evidence bullet + 反证段改 bullet / verify.sh PASS（**2034 unit** + 13 acceptance + 92% coverage + ruff clean）/ fresh oracle qa-review PASS-WITH-NOTES（`ses_1ea76c300ffe9nG3xo7ex0HpvS`），Note 3（empty-dash 测试断言不精准）已修；Note 1（重复 required H2 不拒）/ Note 2（unclosed fence 未测试）记为后续 round / 0 `_validate_output_markdown`/`render_report` 签名 / 0 `output_format` enum / 0 frontmatter schema / 0 `prompts/ask.md` / 0 LLM backend / 0 raw/wiki/mm/receipt/Today feed/Product Shell / 0 protocol-specific report 模板分化 / 0 新增 dep | ✅ done (`ae9e19f`) |
| **Round 98.2 Report Citation Integrity** (2026-05-11) | R98.1 之后给 `output_format == "report"` 加 Phase 4 citation integrity 校验：(1) `## 引用` block 内 `wiki/sources/*.md` 路径去重；(2) Body ⊆ Citations — body slice (`## 结论`+1 ~ `## 引用`) 内出现的所有 citation path 必须出现在 `## 引用` 列表里 / `src/aiwiki/runner/prompts.py` 新 `_REPORT_CITATION_PATTERN = re.compile(r"wiki/sources/[a-z0-9._-]+\.md")` + `_extract_report_citations(lines)` helper（fence-aware，与 `_count_report_bullets` 共享 toggle 语义）；扩 `_validate_report_sections` Phase 1 用 `matched_positions` 记录每个 required H2 按序匹配位置；Phase 4 完全基于 `matched_positions`（不复用 `section_ranges` 避免 setdefault first-occurrence bug）现算 citations next-H2 边界 → 排除 skeleton 末尾 `## 参考` 段中 `compact_source_link_lines` 渲染的 raw paths / 错误消息：`"Report ## 引用 has duplicate citation path: <path>"` / `"Report body cites path not listed under ## 引用: <path>"`（按 body 出现顺序首个 missing）/ `tests/test_decision_grade_report.py` 加 12 cases（multi-citation pass / body 重复引用 pass / body fenced 引用排除 pass / skeleton-layout w/ `## 参考` pass / 含 dot-dash-underscore-digit 文件名 pass / `## 参考` dup 不触发 dedup pass / `## 引用` dup reject / body path missing reject / first-missing in body order / 末段 bullet 紧邻 `## 引用` 也被捕获 / `## 参考` extra path pass / **stray duplicate `## 引用` 在 `## 结论` 前** adversarial reject）共 34 cases / 5 个既有 `_VALID_REPORT_BODY` stub 与 acceptance fixture 无需改动（既有 body=source-1 ⊆ citation=source-1 baseline 已合规）/ verify.sh PASS（**2034 unit** + 13 acceptance + 92% coverage + ruff clean）/ fresh oracle qa-review 2 轮：第 1 轮 FAIL（捕到 `section_ranges.setdefault()` first-occurrence 与 `matched_positions` ordered match 混用导致 stray duplicate H2 下 false-accept），第 2 轮 fix 后 PASS-WITH-NOTES（`ses_1ea616e81ffeiu0VAqiraEOYXS`），note 是 Phase 3 仍受 R98.1 Note 1 deferred 影响但 R98.2 slicing 已安全 / 0 `_validate_output_markdown`/`render_report` 签名 / 0 `render_report` skeleton 文本 / 0 `output_format` enum / 0 frontmatter schema / 0 `prompts/ask.md` / 0 LLM backend / 0 raw/wiki/mm/receipt/Today feed/Product Shell / 0 protocol-specific report 分化 / 0 新增 dep（stdlib `re`） | ✅ done |
| **Round 98.3 Report Strictness Hardening** (2026-05-11) | R98.2 之后补 R98.1/R98.2 deferred notes：(1) Phase 0 unclosed fenced code block 拒绝；(2) Phase 1.5 duplicate required H2 拒绝（strict mode：任何 `_REPORT_REQUIRED_SECTIONS` 重复都 reject）/ `src/aiwiki/runner/prompts.py:_validate_report_sections` 扩为 6 阶段（0 fence → 1 order/missing → 1.5 duplicate → 2 `_LLM:` placeholder → 3 bullet minimums → 4 citation integrity）；Phase 0 复用 Phase 1 现有 H2 扫描末态 `in_fence`（不另起 pass）；Phase 1.5 扫 `h2_positions` 中 required H2 计数，首个 duplicate 抛 `"Report has duplicate required section: {title}"`；Phase 0 错误消息 `"Report has unclosed fenced code block."` / R98.2 stray test 重写（保名 `test_r982_stray_duplicate_citations_heading_before_conclusion_not_bypassed`，断言改到 Phase 1.5 错误）/ 新增 5 个 case：`test_r983_duplicate_conclusion_section_rejects` / `test_r983_duplicate_non_required_heading_passes`（如 `## 参考` 重复仍合法）/ `test_r983_closed_fence_in_body_still_passes`（fence 内塞 `## 结论` 与 `_LLM:` 验证 fence-aware 跳过）/ `test_r983_unclosed_triple_backtick_fence_rejects` / `test_r983_unclosed_tilde_fence_rejects` 共 39 cases / Phase 3 `section_ranges.setdefault()` first-occurrence 注释更新（Phase 1.5 提前拦截后自动安全，无需独立修）/ verify.sh PASS（**2051 unit** + 13 acceptance + 92% coverage + ruff clean）/ fresh oracle qa-review PASS-WITH-NOTES（`ses_1ea467094ffewysXptePJcIGi8`），3 个 deferrable notes：N1 closed-fence 正例补强（已吸收：fence 内塞 `## 结论` + `_LLM:`）/ N2 prompts.py:964 过期注释清理（已吸收）/ N3 `_validate_output_markdown` precheck 顺序非本轮 scope（defer）/ 0 `_count_report_bullets` / 0 `_extract_report_citations` / 0 `_REPORT_CITATION_PATTERN` / 0 `_REPORT_PLACEHOLDER_MARKER` / 0 `_REPORT_REQUIRED_SECTIONS` / 0 `_REPORT_SECTION_BULLET_MINIMUMS` / 0 `_validate_output_markdown` 前置 precheck 顺序 / 0 引入 markdown parser / 0 raw/wiki/mm/receipt/Today feed/Product Shell | ✅ done |
| **Round 97 Decision-grade Report Skeleton** (2026-05-11) | `output_format == "report"` 升级到 6 区块固定骨架（`## 结论` / `## 关键证据` / `## 反证与不确定性` / `## 行动建议` / `## 下次观察信号` / `## 引用`）+ 末尾 `## 参考` 段合并 `_协议输出偏置：_` / `_优先来源：_` / `_优先概念：_` / `prompts/ask.md` 扩 ~45 行新增 Required Sections + 引用规则（仅 `format: report` 触发，slides/figure/decision-memo/sop 保持原约定）/ `src/aiwiki/runner/prompts.py` 新 `_REPORT_REQUIRED_SECTIONS` 常量 + `_validate_report_sections()`（line-anchored H2 + 跳过 fenced code block + exact equality + cursor 顺序）/ `src/aiwiki/app_queries.py:render_report` 重组 6 区块 placeholder skeleton，机器记忆 focus_lines 改放 `## 关键证据` 下作 `_机器记忆提示：_` 注释块（仅非默认 fallback 时输出）/ `tests/test_decision_grade_report.py` 新建 13 cases（正例 / 缺 3 段 / 乱序 / 无 frontmatter / 无 wiki/sources / 4 个非-report format / render_report self-roundtrip / 常量长度断言 / inline body / fenced code / lookalike 长标题）/ 5 既有 fixture 文件加 `_VALID_REPORT_BODY` 常量替换最小 stub / 3 acceptance fixture rename + 重写 response_text 满足 6 段 / verify.sh PASS（**2025 unit** + 13 acceptance + 92% coverage + ruff clean）/ fresh oracle qa-review 3 轮 PASS-WITH-NOTES（`ses_1ead559e9fferDRADMvXvxPkYP`）/ 0 `output_format` enum / 0 `render_report`/`_validate_output_markdown` 签名 / 0 frontmatter schema / 0 LLM backend / 0 raw/wiki/mm/receipt/Today feed/Product Shell / 0 协议-specific report 模板分化 / 0 新增 dep | ✅ done |
 | **Round 96.8 apply_l3_proposal Rollback 跨流一致** (2026-05-11) | `src/aiwiki/execution/l3_proposals.py:668-712` 外层 rollback 顺序 reversed write-order(audit → runtime-history → execution-receipts)，补齐 `runtime_history_path` 还原（R96.8 前 rollback 遗漏导致 audit/receipt 还原但 runtime-history append 残留）/ 异常 chain `raise SiteError(...) from tx_exc`；half-write `raise SiteHalfWriteError("... tx_error=...; rollback_error=...") from rollback_exc` / `tests/test_l3_proposal_tx.py` 新 class +N 测试覆盖 audit 阶段失败时三流齐还原 / verify.sh PASS（**2103 unit** + 13 acceptance + 92% coverage）/ fresh oracle qa-review PASS（`ses_1eb1bd5d5ffeb1YZGLrWXRSuti`）/ 0 schema / 0 lock / 0 `apply_l3_proposal` 公共签名 / 0 phase-2 happy-path append 顺序 / 0 universal `append_audit` / 0 `_audit_record` builder | ✅ done (`f2c9f5a`) |
 | **Round 96.7 L3 Phase-2 swallow 改 universal audit 路径 (DROP)** (2026-05-11) | 候选：phase-2 derived audit append 改走 universal `append_audit` 而非 swallow-with-warning / fresh oracle pre-design DROP：`_audit_record` builder + universal append 是 cross-cutting hardening，紧耦合 phase-2 swallow 语义；扩到这个范围属于新 feature 不是 R96.6 follow-up；contract 主张应作为独立 round（暂不立项）/ 0 改动 / 决议归档 `.codex/contracts/archive/R96.7-L3-PHASE2-UNIVERSAL-AUDIT-DROP.md` | ❌ dropped |
 | **Round 96.6 L3 Phase-2 Audit Swallow Hardening (DROP)** (2026-05-11) | 候选：把 `apply_l3_proposal` phase-2 audit append 从 swallow-with-warning 升级为 receipt-tier TX 保护 / fresh oracle pre-design DROP：phase-2 swallow 是故意设计（derived audit 失败不应回滚已落 SoT），强行包 TX 等于改语义 + 不可逆破坏；当前 audit 流缺失靠 R96.9-候选式 reconciler 后补；single-file write 加仪式属 ritual / 0 改动 / 决议归档 `.codex/contracts/archive/R96.6-L3-PHASE2-AUDIT-SWALLOW-DROP.md` | ❌ dropped |
 | **Round 96.5 mm_action_batch Receipt-Tier TX** (2026-05-11) | `apply_machine_memory_actions_batch` snapshot/rollback：execution-receipts.jsonl / audit.jsonl / runtime-history.jsonl + machine-memory-actions.json + 每 action receipt JSON 全包；rollback 顺序 reversed write-order；`MachineMemoryBatchReceiptError` + `MachineMemoryBatchHalfWriteError` 异常 site-specific / `tests/test_mm_action_batch_receipt_tx.py` 全分支 / verify.sh PASS / fresh oracle qa-review PASS / 0 schema / 0 公共签名 | ✅ done (`5aa4a92`) |
 | **Round 96.4 create_l3_proposal TX** (2026-05-11) | state + page + history + audit + wiki-log 全包 receipt-tier TX；`_persist_l3_proposal_page` + `save_l3_proposal_state` + `append_audit` + `append_wiki_log` snapshot+rollback；exception chain & half-write 异常 site-specific / 全分支测试 / verify.sh PASS / 0 schema | ✅ done (`4df35ea`) |
 | **Round 96.3 alchemy distill/propose_apply TX** (2026-05-11) | `run_alchemy_distill_proposal_apply` / `run_alchemy_propose_apply` receipt-tier TX：execution-receipts + audit + runtime-history + state files snapshot/rollback；异常 site-specific `Alchemy*ReceiptError` + `Alchemy*HalfWriteError` / `tests/test_alchemy_distill_propose_apply_tx.py` 全分支 / verify.sh PASS / 0 lane primitive 公共签名 | ✅ done (`1db7925`) |
 | **Round 96.2 alchemy lane primitive Receipt TX** (2026-05-11) | `_run_receipted_lane_primitive` wrapper TX：receipt JSON + execution-receipts + audit + runtime-history snapshot/rollback；child primitive artifacts 不触碰（独立事实）；`AlchemyLanePrimitiveReceiptError/HalfWriteError` / `tests/test_alchemy_lane_primitive_receipt_tx.py` / verify.sh PASS / 0 child artifact 改动 / 0 `_run_receipted_lane_primitive` 公共签名 | ✅ done (`44100e2`) |
 | **Round 96.1 alchemy review_apply TX** (2026-05-11) | `run_alchemy_review_apply` receipt-tier TX：execution-receipts + audit + runtime-history + `_materialize_alchemy_review_queue` 产生的 state 文件全包；reversed write-order rollback；`AlchemyReviewApplyReceiptError/HalfWriteError` / `tests/test_alchemy_review_apply_tx.py` / verify.sh PASS / 0 `_materialize_alchemy_review_queue` 内部签名 | ✅ done (`8a4dd91`) |
 | **Round 96.0 alchemy receipt persistence TX** (2026-05-11) | `_persist_receipt_transactionally` 收口共享 receipt-tier TX 范式：phase-1 destructive 共同 try/except + rollback；phase-2 audit/derived per-step warning；child primitive artifacts 不动；`AlchemyReceiptError/HalfWriteError` 基类 / `tests/test_alchemy_receipt_tx.py` / verify.sh PASS / 0 receipt JSON schema / 0 `_persist_receipt_transactionally` 公共签名 | ✅ done (`4b3e32f`) |
 | **Round 95.4 Nightly Audit Reconciler** (2026-05-11) | `src/aiwiki/execution/audit_reconciliation.py:reconcile_execution_receipts` nightly 扫 `execution-receipts.jsonl` 检出 false-success 残留（receipt 写完但 audit 落空）/ `SUCCESS_OPERATIONS=frozenset({"revert"})` 仅扫 revert 行 / 调用挂在 `runner/workflows.py:1294-1302` nightly 流 / `tests/test_audit_reconciliation.py` / verify.sh PASS / 0 lock / 0 SoT 改动 | ✅ done (`3679079`) |
 | **Round 95.3 L3 Phase-2 False-Write Truncate** (2026-05-11) | `apply_l3_proposal` phase-2 rollback 路径：检测到 audit/history 在 phase-1 commit 后写入失败时，用 `_durable_truncate(path, snapshot_size)` 把 jsonl 截回 commit 前长度，避免遗留 false-success 行 / `_durable_truncate` open r+b + truncate + flush + fsync / 全分支测试 / verify.sh PASS / 0 phase-2 happy-path 顺序 | ✅ done (`70ff772`) |
 | **Round 95.2 drop.py raw note + asset atomic** (2026-05-11) | `drop.py` raw note (`raw/notes/...`) + asset (`raw/assets/...`) 写入全切 `atomic_write_text` / `atomic_write_bytes` / `fsync=True`（事实层 raw 必须 fsync）/ 既有 `_assert_file_size` / MIME / PDF magic 检查保留 / 测试覆盖 partial-write crash 模拟 / verify.sh PASS / 0 `drop_*` 签名 / 0 FeedKind / 0 raw-added history 写语义 | ✅ done (`0731823`) |
 | **Round 95.1 archive apply/revert TX** (2026-05-11) | `src/aiwiki/execution/archive.py` `apply_archive_action` / `revert_archive_action` receipt+state pair TX：execution-receipts + audit + runtime-history + archive state json 全包 snapshot/rollback；reversed write-order；phase-2 derived audit swallow-with-warning（设计保留）；`material_archive_action_id` 不变；`ArchiveApplyReceiptError/HalfWriteError` + `ArchiveRevertReceiptError/HalfWriteError` 异常 site-specific / 全分支测试 / verify.sh PASS / 0 `material_archive_action_id` schema / 0 phase-2 swallow 行为 | ✅ done (`a49ead8`) |
 | **Round 94.5 L3 Proposal apply/revert TX** (2026-05-11) | `apply_l3_proposal` / `revert_l3_proposal` / `reject_l3_proposal` file + receipt + state triple TX：proposal page + execution-receipts + audit + runtime-history + state json snapshot/rollback；phase-1 destructive 共同 try/except；reversed write-order；`L3ApplyReceiptError/HalfWriteError` + `L3RevertReceiptError/HalfWriteError` + `L3RejectReceiptError/HalfWriteError` / 全分支测试 / verify.sh PASS / 0 `apply_l3_proposal` 公共签名 / 0 `L3PostApplyAuditError` schema / 0 `L3RevertError` schema | ✅ done (`b093241`) |
 | **Round 94.4 concept rewrite apply/revert TX** (2026-05-11) | concept rewrite apply/revert file + state pair TX：concept page + execution-receipts + audit + runtime-history + state snapshot/rollback；reversed write-order；异常 site-specific / 全分支测试 / verify.sh PASS / 0 schema | ✅ done (`ecef27f`) |
 | **Round 94.3 mm-action read-then-write fail-closed** (2026-05-11) | `apply_machine_memory_action` / `revert_machine_memory_action` 内 read-then-write 路径加 `load_json_document_strict` 切换：corrupt state 不再静默被覆盖；R94 系列 fail-closed 主线 / `CorruptStateError` 自然传播到 CLI；测试覆盖 corrupt fixture / verify.sh PASS / 0 `apply_machine_memory_action` 公共签名 | ✅ done (`a85f599`) |
 | **Round 94.2 ingest_source raw/ atomic + orphan tmp 跳过** (2026-05-11) | `ingest_source` raw/ 写入切 `atomic_write_text` / `atomic_write_bytes` + `fsync=True`；raw scanner（compile / drop dedupe / drift）`os.walk` 过滤 `*.tmp.*` 与 hidden tmp suffix，避免 partial-write 残留被当作 raw 事实 / 测试构造 orphan tmp fixture / verify.sh PASS / 0 raw schema / 0 raw-added history 写语义 | ✅ done (`4ba29e9`) |
 | **Round 94.1 drift_scan → append_runtime_history** (2026-05-11) | `drift_scan` 改走 `append_runtime_history` 而非裸 jsonl write，复用 R76 事务化 + audit-mirror；保留事件 schema / 测试覆盖；verify.sh PASS / 0 `append_runtime_history` 签名 | ✅ done (`6bf39e0`) |
 | **Round 94.0 automation.json 原子写** (2026-05-11) | `automation.json` 写入切 `atomic_write_text` + `fsync=True`，杜绝 torn-write；`load_automation_state` 配合 strict + corrupt 回收路径；测试覆盖 partial-write crash / verify.sh PASS / 0 automation schema | ✅ done (`0ddc2c7`) |
 | **Round 93.3 ingest subprocess timeout** (2026-05-11) | `drop.py` 所有 ingest 相关 `subprocess.run(...)` 加显式 `timeout=`（PDF 抽取 / image OCR / vision / repo clone 之外的辅助命令），统一 `_INGEST_SUBPROCESS_TIMEOUT_SEC` 常量；`TimeoutExpired` 抛 `IngestionTimeoutError` 不静默；测试 patch subprocess 验证 timeout 传参 / verify.sh PASS / 0 `drop_*` 签名 | ✅ done (`137aa4e`) |
 | **Round 93.2 compile-artifact 原子写** (2026-05-11) | compile pipeline 派生 artifact 写入全切 `os.replace` 原子路径（`fsync=False` derived/replay-safe），覆盖 `compile/content_step.py` / `runtime_step.py` / `output_step.py` 5 处 build-state save 之外的剩余裸 write_text / 测试覆盖；verify.sh PASS / 0 compile_wiki / lint_wiki 签名 / 0 nightly_health 内部 | ✅ done (`6d2cee6`) |
 | **Round 93.1 material-archive TX + audit 保留** (2026-05-11) | `execution/archive.py` 早期 material-archive 操作改走 receipt-tier TX 雏形；audit 行为保持完整记录 / `tests/test_archive_tx.py` 早期版本；R95.1 接续做最终 receipt+state pair TX / verify.sh PASS / 0 `material_archive_action_id` schema | ✅ done (`37c9341`) |
 | **Round 93.0 autonomy_policy fail-closed** (2026-05-11) | `autonomy_policy` 文件 malformed / unreadable 时不再静默走默认，改为 `CorruptStateError` 抛出；`load_autonomy_policy` 切 `load_json_document_strict`；CLI 自然传播；测试 corrupt fixture / verify.sh PASS / 0 `set_flag` 签名 / 0 autonomy schema | ✅ done (`7e43e4e`) |
 | **Round 92.11 PERF Benchmark (measurement-only)** (2026-05-10) | 新增 measurement-only `compile_wiki` benchmark：`scripts/bench/compile_wiki_bench.py` + fixture 数据集 + 输出 JSON 报告 / 不接入 CI gate / 不动 compile pipeline / verify.sh PASS / 0 compile_wiki 签名 / 0 lint_wiki 签名 | ✅ done (`0944726`) |
 | **Round 92.10 TEST SPLIT test_app.py** (2026-05-10) | 7984 行 `tests/test_app.py` 拆分到 7 个 focused 文件（按 domain 分类：compile / runtime / drop / shell / protocol / 等）；保持 import 入口与 `load_tests` bridge；总 test count 不变 / verify.sh PASS（unit count 不变 + 13 acceptance + 92% coverage）/ 0 生产代码改动 | ✅ done (`00b41ad`) |
 | **Round 92.9 DETERMINISM compile gate** (2026-05-10) | compile log timestamp 在 gate 比对时不敏感：`scripts/verify.sh` 的 compile-status 对比改 timestamp-insensitive；`compile_wiki` 输出 log 加确定性 gate 防回归 / 测试覆盖 timestamp diff fixture / verify.sh PASS / 0 compile_wiki 签名 | ✅ done (`f153589`) |
 | **Round 92 Alchemy Apply Lock + Receipt Atomicity** (2026-05-05) | runner/alchemy.py 7 个顶层 apply/auto/lane 入口加 `@runtime_write_operation`（judge_apply / judge_proposal_apply / distill_apply / review_apply / propose_apply / lane_apply / auto）/ 7 处 receipt JSON write 切到 `atomic_write_text` / 4 个 `run_alchemy_*_preview` + 4 个 `preview_*_primitive` 加 `allow_current_writer_lock` 透传参数 / apply 内部 5 处 preview + lane_apply preview + auto 内 lane_dry_run 全部 force True / `_normalize_preview_lock_status` 30 行递归 helper 把 `held_by_current_process` 还原为 `available + would_acquire` 保 receipt schema / acceptance fixture 兼容 / `_run_receipted_lane_primitive` 不加装饰器靠外层 reentrant lock / 新建 `tests/test_alchemy_apply_lock.py` 10 测试（7 入口 lock 持有 + outer-lock reentrant + atomic_write_text failure + grep guard）/ verify.sh all green（1776 unit + 13 acceptance + coverage 92%）/ fresh isolated oracle qa-review PASS | ✅ done |
 | **Round 92.1 Alchemy Lock Audit (Tight)** (2026-05-05) | R92 non-blocking observations 合并轮：runner/alchemy.py 3 残余 unlocked writers 加 `@runtime_write_operation`（`run_alchemy_judge_propose` / `run_alchemy_legacy_migration_apply` / `run_alchemy_superseded_cleanup_apply`）/ `_normalize_preview_lock_status` 重构为 `_walk_preview_lock_status(value, in_lock_subtree)` 双函数：只在 `lock` key 子树下改写 `held_by_current_process → available + would_acquire`，不再误伤同形非-lock dict / `tests/test_alchemy_apply_lock.py` +6 测试（3 新装饰器 lock probe + 1 lane→primitive nested depth>=2 真实探针 patching `run_alchemy_review_preview` 让 review_apply 装饰器先递增 depth + 2 normalize 收紧 unit）/ grep guard 从固定字符串扩到 receipt-like 变量正则 `\b(\w*receipt\w*)\.write_text\(json\.dumps\(`/ verify.sh all green（1782 unit + 13 acceptance + coverage 92%）/ fresh oracle qa-review round 1 FAIL（lane test vacuous）→ round 2 全 PASS | ✅ done |
 | **Round 92.7 Deploy/Service Defaults Hardening** (2026-05-05) | R92-DEPLOY-DEFAULTS Tight scope 5 项收紧 deploy/service-side 危险默认（不影响本地 CLI UX）：1) `scripts/install_user_service.sh:60,64-68,74,78-82,116-118` 5 个 auto-adopt env (`AUTO_APPLY_LIGHT` / `AUTO_ADOPT_L1/L2/L3/JUDGMENTS`) + `AIWIKI_NIGHTLY_FALLBACK_ENABLED` 默认 `1` → `0` deny-by-default + 安装末尾 echo 提示如何手动启用 / 2) `scripts/run_watch.sh:4-13` + `scripts/run_nightly.sh:4-13` 强制要求 `AIWIKI_VAULT`，未设 stderr 错误 + exit 1，移除 repo-root fallback / 3) `scripts/run_nightly.sh:21` `AIWIKI_NIGHTLY_FALLBACK_ENABLED` 默认 `0` / 4) `src/aiwiki/app_utils.py:43-47,140-150,160-176` `runtime_write_lock` 加 timeout：`_LOCK_TIMEOUT_DEFAULT_SEC=300` + `LockTimeoutError(RuntimeError)` + `_resolve_lock_timeout()` env parse + clamp `[1,3600]` invalid 退默认 + `LOCK_NB` retry loop with `time.monotonic()` deadline + handle.close() on raise no FD leak / reentrant depth-counter 路径在 retry loop 之前返回不受影响 / 5) `src/aiwiki/drop.py:420-423,1288-1310` remote URL (`http(s)://` / `git@` / `git://`) 需 `AIWIKI_ALLOW_REMOTE_REPO_DROP=1` 否则 `ValueError` + `_clone_repo` `subprocess.run(timeout=60)` + `TimeoutExpired` cleanup partial dir / 新建 `tests/test_deploy_defaults.py` 11 测试 + `load_tests` bridge：4 shell guard（`bash -n` + grep 默认值 + AIWIKI_VAULT 错误）+ 4 lock（patched `fcntl.flock` BlockingIOError → `LockTimeoutError` / 真实 subprocess holder OS-level flock contention 验证 1s timeout / reentrant 不超时 / `_resolve_lock_timeout` env parse + clamp 边界）+ 3 remote drop（env 缺失 `ValueError` / env 设了进 `_clone_repo` / `TimeoutExpired` cleanup） / 调整既有：`tests/test_app.py:6295-6402` service script 断言、`tests/test_drop.py:164-181` remote clone 显式 opt-in / verify.sh PASS（**1838 unit**(+11) + 13 acceptance + 92% coverage）/ fresh oracle qa-review round 1 CONCERNS（lock timeout test patched-only，未验证真实 OS-level contention 与 contract 描述不一致）→ 补 real subprocess holder 测试 patch 行为 + OS flock contention 双覆盖 → 实际执行结果用户接受 / 0 `general` default protocol / 0 interactive prompt / 0 audit/receipt 自动 prune / 0 本地 CLI 行为改动 / 0 `safe_fetch` allowlist / 0 LLM backend/model defaults / 0 notify defaults / 0 schema / 0 lock primitive 重入语义 / 0 `_LLM_MAX_BYTES` / drop file size cap / 0 CLI signature / Migration: 既有 dogfood systemd 用户重装后 auto-adopt 默认全 off，需手动 `AUTO_ADOPT_L1=1 ./scripts/install_user_service.sh` 启用 | ✅ done |
 | **Round 92.6 Lock Audit Wide** (2026-05-05) | R92/R92.1 alchemy lock audit 扩到全仓：识别 7 个未受 `@runtime_write_operation` 保护的 SoT/state writer 入口（Tight 5 + Standard 2）/ 5 decorator: `app_compile_ops.py:309 set_active_protocol` / `metrics_history.py:33 append_snapshot` / `autonomy_policy.py:119 set_flag` / `runner/automation.py:22 auto_process_once` / `app_shell/meta.py:214 write_shell_summary`，全部首参 `root: Path` / 2 conditional `with runtime_write_lock(root):` 仅 `apply=True` 分支：`execution/audit_preview.py:177 backfill_universal_audit_stream` / `planner/rollback.py:82 apply_planner_log_rollback_marker` — preview/dry-run 路径不取锁 / 重入安全：`auto_process_once` → `compile_wiki` / `lint_wiki` / `run_compile` / `run_lint` / `write_shell_summary` from `compile_wiki` / `shell_status` / `ask_question` 全同 root key，复用 `_RUNTIME_LOCKS` depth counter / 新建 `tests/test_lock_audit_wide.py` 9 测试 + `load_tests` bridge：5 decorator probe（patch 内部 helper assert `_RUNTIME_LOCKS[str(root.resolve())]["depth"] >= 1`）+ 2 conditional × apply=True held / apply=False not-held / verify.sh PASS（**1827 unit**(+9) + 13 acceptance + 92% coverage）/ fresh oracle qa-review PASS（`ses_20711d54affeLPsXBHWzpDeL2f`，1 round）/ 0 lock primitive 语义改动 / 0 alchemy / drop_* / auto_adopt_* / nightly_health / receipt schema / CLI signature / `app_protocol.py` scaffold / `content/io.py` / `runner/receipts.py:_append_jsonl_log` 改动 / 0 lock on read-only / preview path | ✅ done |
 | **Round 92.5 Cache Layer Fail-Soft** (2026-05-05) | `src/aiwiki/app_cache.py` SQLite query cache 全 fail-soft：模块 logger + `_log_cache_fault(label, exc)` helper / 8 boundary 包 try/except 仅捕 `(sqlite3.Error, OSError, json.JSONDecodeError, TypeError)`：`sync_query_cache` / `_load_rows`(narrow `JSONDecodeError, TypeError`) / `load_query_cache_snapshot` / `load_cached_query_result` / `save_cached_query_result` / `_merge_cache_status` / `record_query_cache_event` — corrupt sqlite db / corrupt payload_json (`cache_query_results` / `cache_nodes` / `cache_edges`) / write commit failure 全部 warn + miss / skip-write 不抛 / `src/aiwiki/app_state.py` cache-status boundary：`load_cache_status:632-637` + `save_cache_status:678-683` 仅捕 `OSError`（不动通用 `load_json_document` / `save_json_document` / `atomic_write_text`）/ 5 compile build-state save callsite 直接调生产 `write_json_document_if_changed_ignoring_generated_timestamps(...)` + try/except `OSError` warning：`compile/content_step.py:53-59` concept / `runtime_step.py:408-414` machine-memory + `:575-581` ranking / `output_step.py:356-362` output-pack + `:427-433` domain-pilot / 新建 `tests/test_cache_failsoft.py` 12 测试 + `load_tests` bridge（corrupt result/nodes/edges payload + 非法 sqlite db 字节 + `_connect_cache` patch sqlite3.OperationalError + `save_json_document` patch OSError + 5 build-state OSError patch 各 compile step 模块 namespace 真实生产路径 + grep guard 扫禁 broad `except Exception` / `except BaseException`）/ verify.sh PASS（**1818 unit**(+12) + 13 acceptance + 92% coverage）/ fresh oracle qa-review round 1 CONCERNS（`__module__` 测试感知分支泄漏 + edges payload 未直测）→ fix：移除全部 5 callsite 的 `__module__` 分支 + 新增 corrupt edges 测试 + 重写 build-state 测试 patch 真实生产 helper at compile step namespace → round 2 PASS（`ses_2072c8fdfffe0FXemS421lGPid`）/ 0 SoT fail-soft / 0 `load_json_document_strict` / `CorruptStateError` / `atomic_write_text` 改动 / 0 `.aiwiki/cache/machine-memory-graph.json` 改动 / 0 lock 语义 / 0 receipt / manifest schema / 0 LLM raw response | ✅ done |
 | **Round 92.4 Protocol/Manifest Single-File Atomic Writes** (2026-05-05) | `src/aiwiki/app_protocol.py` 5 处 + `src/aiwiki/app_compile_ops.py` 1 处 protocol/manifest JSON state 写入从裸 `write_text(json.dumps(...))` / `json.dump(...)` 迁到 `atomic_write_text`：runtime.yaml default schema (`:1700`) / runtime.yaml scaffold schema (`:1760`) / initial protocol.json (`:1774`) / normalized protocol.json (`:1808`) / manifest.json (`:1995`，`json.dump` block→`atomic_write_text + json.dumps`) / active protocol switch (`app_compile_ops.py:312`) / 保留 indent=2 / sort_keys=True / ensure_ascii=False (runtime schema) 全部原始 kwargs / 新建 `tests/test_protocol_manifest_atomic.py` 5 测试 + `load_tests` bridge（save_manifest / set_active_protocol / load_protocol_state normalization / load_protocol_runtime_schema default 各 patch `os.replace` raise 验旧 bytes preserved + 无 tmp 残留 + grep guard 扫两文件禁 `write_text(json.dumps(` / `json.dump(`）/ verify.sh PASS（**1806** unit (+5) + 13 acceptance + 92% coverage）/ fresh oracle qa-review round 1 FAIL（pytest-style 测试未被 unittest discover 收集，count 1801 != 1806）→ fix `load_tests` adapter（复用 R92.2 模式）→ round 2 PASS（`ses_2075a2334ffe4zJfZUFXRcFUpP`）/ 0 schema / lock / multi-file TX / markdown scaffold / generic residual writer 改动 | ✅ done |
 | **Round 92.3 Drop Input Safety (Bounded Local Ingestion)** (2026-05-05) | `src/aiwiki/drop.py` 加本地 ingestion 边界：常量 `_LOCAL_PDF_MAX_BYTES` 50MB / `_LOCAL_IMAGE_MAX_BYTES` 25MB / `_SUPPORTED_IMAGE_MIME_TYPES` 白名单（png/jpeg/gif/webp/svg+xml）/ helpers `_assert_file_size` + `_assert_pdf_asset`（5 字节 `%PDF-` magic）+ `_assert_supported_image_mime`（错误消息列允许集）+ `_normalize_repo_max_files`（1..1000）/ `drop_pdf:240-241` 在 `_extract_pdf_text` 前 size+magic / `drop_image:318-319` 在 OCR + `_analyze_image_asset` 前 MIME+size / `drop_repo:416` 入口 normalize max_files / `_repo_key_files:1344-1348` 加 residual 注释 / `cli/parsers.py` drop-pdf/drop-image/`--max-files` help 文案补限制 / `tests/test_drop_safety.py` 扩 7 测试（image rejection 双 patch 验证 vision 不可达 + MIME 错误断言列允许集 + repo max_files 边界参数化）/ verify.sh PASS（1801 unit + 13 acceptance + 92% coverage）/ fresh oracle qa-review round 1 CONCERNS（4 P2: image rejection 未验 vision / MIME 错误未列允许集 / CLI help 未文档化 / `_repo_key_files` walk 不受 max_files 约束 residual）→ round 2 PASS（`ses_2078a47c5ffeT39FAKQvK3cwCx`）/ 0 root containment 改动 / 0 safe_fetch / SSRF / LLM HTTP / receipt schema / atomic write helper 改动 | ✅ done |
 | **Round 92.2 Machine-Memory Action TX** (2026-05-05) | `apply_machine_memory_action` / `revert_machine_memory_action` 加 snapshot/rollback 事务边界，覆盖 3 种 apply_mode（manual-link-state / citation-snapshot-refresh / resolve-monitor）/ 新增错误类型 `MachineMemoryActionReceiptError`（普通事务失败）/ `MachineMemoryActionHalfWriteError`（rollback 自身失败 loud）/ helper `_snapshot_file_bytes` + `_restore_file_bytes`(atomic tmp+os.replace, None→unlink) + `_rollback_snapshots` / 进入 try 前 snapshot 4 核心路径（receipt_path / `.aiwiki/state/execution-receipts.jsonl` / `.aiwiki/state/audit.jsonl` / `.aiwiki/state/machine-memory-actions.json`）+ mode-specific（manual-links.json 或 citation page bytes）/ mutation + receipt JSON write + history append + actions.json save 全部 try 内，except → rollback all + 抛 ReceiptError；rollback 失败抛 HalfWriteError / receipt JSON 与 citation page revert write 全切 `atomic_write_text` / `split-overloaded-concept` auto-retire 留 best-effort 在 try 内，rollback 不覆盖（contract residual） / 新建 `tests/test_machine_memory_action_transaction.py` 12 测试（manual-link/citation/resolve-monitor 各自 happy + rollback 真实分支 + 真实 `_restore_file_bytes` 失败 → HalfWriteError + 源码 atomic-write guard）/ verify.sh PASS（1794 unit + 13 acceptance + coverage 92%）/ fresh oracle qa-review round 1 CONCERNS（测试命名误导 + 缺真实 citation/resolve-monitor 分支覆盖）→ round 2 PASS（`ses_207a58043ffe71a5R252pG0Mn3`） | ✅ done |
