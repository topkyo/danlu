# 引用规则

- 在编译层和输出层里优先引用 vault 路径（`wiki/sources/*.md`、`wiki/judgments/*.md`、`wiki/elixirs/*.md` 等）。
- 联网调研后的 Ask 报告可引用完整 `https://` URL；每条 URL 须可审计、可点击。
- 能保留回到 `raw/` 的文件路径溯源时，尽量保留。
- 没有 vault 路径或可审计 `https://` URL 支撑的综合结论不能写成事实。
- 如果证据薄弱、不完整或互相冲突，要明确写出来。
